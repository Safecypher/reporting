---
phase: quick-260823-l9c
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/dashboard/reconciliation-status.ts
  - lib/dashboard/reconciliation-status.test.ts
  - components/dashboard/reconciliation-billing-table.tsx
  - components/dashboard/reconciliation-inventory-table.tsx
autonomous: true
requirements: [QUICK-260823-l9c]

must_haves:
  truths:
    - "A user scanning the /reconciliation tables can tell OK / Needs review / Mismatch rows apart at a glance by a status-colored left border and subtle background tint"
    - "The status color is never the only signal — the existing StatusBadge text remains in every row"
    - "Billing and inventory main data rows use the exact same status→row-style mapping (DRY, single helper)"
    - "Colors come only from existing brand tokens (--success / --warning / --error / destructive), no new arbitrary colors"
    - "The distinct gap-row (D-07) and always-amber APIGEE sub-table (D-10) treatments are unchanged"
  artifacts:
    - path: "lib/dashboard/reconciliation-status.ts"
      provides: "reconciliationStatusToRowClassName(status) pure helper returning Tailwind classes"
      contains: "reconciliationStatusToRowClassName"
    - path: "lib/dashboard/reconciliation-status.test.ts"
      provides: "Unit test asserting each status maps to its token classes"
      contains: "reconciliationStatusToRowClassName"
  key_links:
    - from: "components/dashboard/reconciliation-billing-table.tsx"
      to: "lib/dashboard/reconciliation-status.ts"
      via: "cn(...reconciliationStatusToRowClassName(row.original.status)) on TableRow"
      pattern: "reconciliationStatusToRowClassName"
    - from: "components/dashboard/reconciliation-inventory-table.tsx"
      to: "lib/dashboard/reconciliation-status.ts"
      via: "cn(...reconciliationStatusToRowClassName(row.original.status)) on TableRow"
      pattern: "reconciliationStatusToRowClassName"
---

<objective>
Add status-based row styling to the two `/reconciliation` main data tables (billing-vs-verifications and card-inventory) so OK / Needs review / Mismatch rows stand out at a glance: a status-colored left border plus a subtle background tint per row, driven by the same status enum the StatusBadge uses.

Purpose: Faster visual triage of reconciliation flags — a reviewer sees which days need attention before reading each badge, supporting the core "make any discrepancy immediately visible" value.
Output: One shared pure helper (`reconciliationStatusToRowClassName`) + its unit test, wired into both table components. No new colors, no color-only signal (badge text stays).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/STATE.md

<interfaces>
<!-- Existing contracts the executor uses directly — no codebase exploration needed. -->

From lib/dashboard/reconciliation-status.ts (pure module — no DOM/clock/network):
```typescript
export type ReconciliationStatus = "ok" | "needs_review" | "mismatch";
// Existing sibling helper — new helper sits beside it, same shape/switch style:
export function reconciliationStatusToBadge(status: ReconciliationStatus): ReconciliationBadgeDescriptor;
```

From components/dashboard/status-badge.tsx (the variant→token mapping to stay consistent with):
```
ok            -> border-[color:var(--success)]/30  bg-[color:var(--success)]/10  text-[color:var(--success)]
needs_review  -> border-[color:var(--warning)]/30  bg-[color:var(--warning)]/10  text-[color:var(--warning)]
mismatch      -> border-destructive/30             bg-destructive/10             text-destructive
```

From components/ui/table.tsx:
```
TableRow renders a <tr> with cn("border-b transition-colors hover:bg-muted/50 ...", className)
cn is imported from "@/lib/utils"
```

Existing tokens in app/globals.css @theme (do NOT add new ones):
`--success`, `--warning`, `--error`; `--destructive` maps to `--error` in :root and is overridden in `.dark`.
`--success` / `--warning` are NOT overridden in `.dark` — this is why the row tint uses a very low opacity (layers acceptably over dark surfaces) and the badge text stays the authoritative signal.

Established working precedent for styling a TanStack <tr> in this codebase (gap rows, inventory table):
`<TableRow className="border-dashed bg-muted/30">` — confirms border + bg classes on the row `<tr>` render here.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add shared reconciliationStatusToRowClassName helper + unit test</name>
  <files>lib/dashboard/reconciliation-status.ts, lib/dashboard/reconciliation-status.test.ts</files>
  <behavior>
    - reconciliationStatusToRowClassName("ok") returns a string containing "border-l-4", "border-l-[color:var(--success)]", and "bg-[color:var(--success)]/5"
    - reconciliationStatusToRowClassName("needs_review") returns a string containing "border-l-4", "border-l-[color:var(--warning)]", and "bg-[color:var(--warning)]/5"
    - reconciliationStatusToRowClassName("mismatch") returns a string containing "border-l-4", "border-l-destructive", and "bg-destructive/5"
    - Function is exhaustive over the ReconciliationStatus union (switch with no default; TS enforces coverage)
  </behavior>
  <action>
    Add a new exported pure function `reconciliationStatusToRowClassName(status: ReconciliationStatus): string` to lib/dashboard/reconciliation-status.ts, placed directly after `reconciliationStatusToBadge`. Mirror that function's `switch` style (no `default` case so TS exhaustiveness holds). Return the Tailwind class strings specified in the behavior block: a `border-l-4` left accent plus a `/5` background tint, reusing the SAME brand tokens the StatusBadge uses for each variant (`--success`, `--warning`, `destructive`). Keep opacity low (`/5`) so the tint is subtle and layers acceptably in both light and dark themes; the StatusBadge text remains the primary, non-color signal (accessibility). Do NOT introduce any new color tokens. Keep the module pure — return a plain string, no DOM/React imports. Add a one-line doc comment noting this is UI class output consumed by the reconciliation table rows and mirrors the StatusBadge variant→token mapping. Create lib/dashboard/reconciliation-status.test.ts with a Vitest `describe`/`it` block covering all three statuses per the behavior block (assert `.toContain(...)` for each required class).
  </action>
  <verify>
    <automated>npx vitest run lib/dashboard/reconciliation-status.test.ts</automated>
  </verify>
  <done>Helper exported and pure; test file passes; all three statuses covered; no new tokens introduced.</done>
</task>

<task type="auto">
  <name>Task 2: Wire row styling into both reconciliation tables</name>
  <files>components/dashboard/reconciliation-billing-table.tsx, components/dashboard/reconciliation-inventory-table.tsx</files>
  <action>
    In both files, import `cn` from "@/lib/utils" and add `reconciliationStatusToRowClassName` to the existing import from "@/lib/dashboard/reconciliation-status". On the MAIN data-row `<TableRow>` inside `table.getRowModel().rows.map(...)` (the drillable rows), change the current `className="cursor-pointer"` to `className={cn("cursor-pointer", reconciliationStatusToRowClassName(row.original.status))}`. Apply this in BOTH the billing table and the inventory table — the inventory main rows carry `status: ReconciliationStatus` on `row.original`, same as billing. Do NOT touch the inventory gap rows (the `gapRows.map(...)` block with `border-dashed bg-muted/30`, D-07) and do NOT touch the always-amber APIGEE cross-check sub-table rows (D-10) — those keep their distinct existing treatment. Leave all drill onClick/onKeyDown/aria wiring unchanged. Do not alter column defs, the delta neutral-ink rule, or the StatusBadge cell (badge text must remain as the non-color signal).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx next lint --file components/dashboard/reconciliation-billing-table.tsx --file components/dashboard/reconciliation-inventory-table.tsx && grep -c reconciliationStatusToRowClassName components/dashboard/reconciliation-billing-table.tsx components/dashboard/reconciliation-inventory-table.tsx</automated>
  </verify>
  <done>Both main tables call `reconciliationStatusToRowClassName` via `cn` on their drillable rows; tsc and lint pass; gap rows and APIGEE sub-table are unchanged; StatusBadge cells intact.</done>
</task>

</tasks>

<verification>
- `npx vitest run lib/dashboard/reconciliation-status.test.ts` passes (helper contract).
- `npx tsc --noEmit` clean (exhaustive switch, correct imports/types).
- `npx next lint` clean on both edited table files.
- `grep -n "reconciliationStatusToRowClassName" components/dashboard/reconciliation-*-table.tsx` shows one import + one usage per file.
- Manual (optional): load `/reconciliation`, confirm OK rows show a green left accent + faint tint, Needs review amber, Mismatch red; badges still present; gap rows still dashed/muted; APIGEE sub-table still plain/amber; toggle dark theme and confirm tints remain subtle and badge text stays legible.
</verification>

<success_criteria>
- Billing and inventory main rows are visually distinguishable by status via a left border + subtle tint drawn only from existing brand tokens.
- Color is an enhancement, not the sole signal — every row keeps its StatusBadge text.
- A single shared helper drives both tables (DRY); the pure reconciliation-status module stays test-covered.
- D-07 gap rows and D-10 APIGEE sub-table treatments are untouched.
</success_criteria>

<output>
Create `.planning/quick/260823-l9c-add-status-based-row-styling-to-reconcil/260823-l9c-SUMMARY.md` when done
</output>
