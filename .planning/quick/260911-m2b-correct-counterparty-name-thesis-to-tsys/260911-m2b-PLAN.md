---
quick_id: 260911-m2b
slug: correct-counterparty-name-thesis-to-tsys
date: 2026-09-11
mode: quick
files_modified:
  - CLAUDE.md
  - lib/ingestion/parsers/apigee-stats.ts
---

# Correct counterparty name: Thesis → TSYS

## Objective

The card-processor counterparty is **TSYS**. "Thesis" is a voice-transcription error — the
two are near-identical spoken aloud — seeded into `CLAUDE.md` at project setup and propagated
from there. Confirmed by the user 2026-09-11.

Fix the two highest-value locations only: the project instructions every agent reads, and the
two error strings a user can actually see. Leave the remaining ~140 prose occurrences for a
later, deliberate sweep.

## Scope boundary (binding)

**Do not touch** `supabase/migrations/0018`–`0022` or `lib/dashboard/reconciliation-status.ts`.
Their git hashes are pinned as the `RECON_CHAIN_UNTOUCHED` gate in the phase-06 plan verifies
(06-06 Task 1); editing even a comment inside them fails that gate and trips the D-01
"never amend the Phase 4/5 chain" rule. Those migrations are also already applied live, so
their in-database comments would drift from the repo.

Also out of scope this task: the ~126 occurrences in `.planning/`, and the remaining comment
occurrences in `lib/ingestion/types.ts`, `lib/ingestion/handlers/apigee-stats.ts`,
`lib/ingestion/__tests__/apigee-stats.test.ts`, `supabase/migrations/0010`, `0028`, `0029`,
and `supabase/tests/reconciliation_no_source_data_test.sql`.

"Invex" is a separate, real entity — leave every occurrence unchanged.

## Tasks

### Task 1: Correct the four occurrences in CLAUDE.md

**Files:** `CLAUDE.md`

**Action:** Replace "Thesis" with "TSYS" at all four sites, preserving surrounding wording:
- line 6 — `(via Thesis / Invex)` → `(via TSYS / Invex)` (keep Invex)
- line 10 — `when Thesis flags a problem` → `when TSYS flags a problem`
- line 39 — `Thesis "Safecypher Stats" .xlsx` → `TSYS "Safecypher Stats" .xlsx`
- line 72 — `Thesis \`APIGEE Calls\`` → `TSYS \`APIGEE Calls\``

**Verify:** `grep -c Thesis CLAUDE.md` returns 0, and `grep -c Invex CLAUDE.md` is unchanged at 1.

**Done:** CLAUDE.md names the counterparty correctly, so every future agent reading the
project instructions inherits the right name rather than re-propagating the error.

### Task 2: Correct the two user-visible error strings

**Files:** `lib/ingestion/parsers/apigee-stats.ts`

**Action:** Replace "Thesis" with "TSYS" in the two thrown error messages, which surface to a
user on a failed upload:
- line 56 — `` throw new Error(`could not load Thesis workbook: ${reason}`) ``
- line 61 — `` throw new Error(`Thesis workbook is missing the "${APIGEE_SHEET_NAME}" sheet`) ``

Also correct the doc-comment on line 38 (`Load the Thesis "Safecypher Stats" workbook`) since
it sits in the same file and describes the same thing — leaving it would be internally
inconsistent.

Change no identifier, no constant, no test expectation beyond what these strings require.

**Verify:** `grep -c Thesis lib/ingestion/parsers/apigee-stats.ts` returns 0.

**Done:** A user who uploads a malformed workbook is told the correct counterparty name.

### Task 3: Confirm nothing else moved

**Files:** none

**Action:** Run the full regression gates and confirm the pinned-hash chain is intact.

**Verify:**
- `npx tsc --noEmit` exits 0
- `npm test` — 339/339 passing, no change from the phase-06 baseline
- the `RECON_CHAIN_UNTOUCHED` hash check still prints its success string
- `git status --porcelain` shows only the two intended files

**Done:** The rename is provably cosmetic — no behaviour, no type, no pinned artifact changed.

## Must-haves

- `CLAUDE.md` and `lib/ingestion/parsers/apigee-stats.ts` contain zero occurrences of "Thesis".
- `Invex` is untouched.
- No file outside those two is modified.
- `RECON_CHAIN_UNTOUCHED` still passes.
- `npx tsc --noEmit` and `npm test` (339) both pass.
