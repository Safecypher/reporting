---
phase: quick-260908-r3x
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/dashboard/card-inventory.ts
  - lib/dashboard/card-inventory.test.ts
  - components/dashboard/card-inventory-chart.tsx
  - components/dashboard/card-removals-chart.tsx
  - components/dashboard/card-inventory-table.tsx
  - components/dashboard/card-inventory-columns.tsx
  - components/dashboard/card-inventory-kpi-cards.tsx
  - app/(dashboard)/cards/page.tsx
  - components/app-shell/sidebar-nav.tsx
autonomous: true
requirements: [QUICK-260908-r3x]

estimate:
  tokens: 70000
  raw_tokens: 48000
  tasks: 3
  confidence: medium

must_haves:
  truths:
    - "Uploading the card-inventory and removed-cards reports produces a visible page — the literal complaint that opened this work"
    - "The enrolment chart never implies daily continuity: 9 snapshots over 27 days must read as 9 discrete observations with visible gaps"
    - "The 4,426-removal spike on 2026-09-04 is shown truthfully and explained, never truncated, smoothed or silently rescaled"
    - "The page works against the CURRENT live schema — no dependency on unapplied migration 0022 and no new migration of its own"
    - "DATA-06 (2026-08-13 floor) is honoured and the existing freshness/data-window header treatment is reused verbatim"
  artifacts:
    - path: "app/(dashboard)/cards/page.tsx"
      provides: "Server Component page: KPIs, enrolment chart, removals chart, card table"
      contains: "Suspense"
    - path: "lib/dashboard/card-inventory.ts"
      provides: "Pure shaping helpers + server fetchers for inventory and removals"
      contains: "export function"
  key_links:
    - "app/(dashboard)/verifications/page.tsx — the structural analogue this page mirrors"
    - "components/app-shell/sidebar-nav.tsx — NAV_ITEMS array"
    - "public/icons.svg — the `card` symbol already exists; use it"
---

# Quick 260908-r3x — Card inventory page

## Problem

Six report types ingest cleanly, but card-inventory and removed-cards data has
no page. Inventory appears only as a diff table inside Reconciliation, so
uploading the report produces no visible result. 912 inventory rows and 4,489
removal rows are effectively invisible.

## Design decisions (made, with reasoning)

**Route `/cards`, nav label "Cards", icon `card`.** The `card` symbol already
exists in `public/icons.svg` — do not invent one; a missing symbol renders as
an empty box with no error.

**Enrolment chart uses a TIME-SCALED x-axis, not a categorical one.** This is
the important call. A categorical axis spaces the 9 snapshots evenly, which
makes 13 Aug → 24 Aug (11 days apart) look identical to 1 Sep → 2 Sep (1 day
apart) — it hides exactly the sparseness the reader needs to see. A time-scaled
axis renders the gaps as whitespace. Use discrete bars or dots at real dates;
do NOT connect them with a line, which would imply we know the values in
between. Caption: "9 snapshots between 13 Aug and 8 Sep 2026 — this report is
not delivered daily."

**Removals chart shows the 4 Sep spike at full linear scale.** 4,426 against a
baseline of tens will flatten every other day to nothing. That is the honest
picture, and the flattening is itself the story. Do NOT use a log axis — this
dashboard's audience includes non-technical leadership, and a log scale makes
a 100× spike look like a modest bump, which is worse than an unreadable
baseline. Instead pair the chart with a short callout naming the incident:
Invex enabled the product to many customers via a mobile-app update on ~4 Sep
and reverted it; the 4,426 removals are that revert, not churn.

**No migration.** Shaping happens in TypeScript over plain table reads. Two
reasons: migration 0022 is authored but unapplied, so anything depending on it
would render a broken page in production; and adding 0023 would create a second
unapplied migration. At 912 inventory rows, fetching `(report_date,
external_card_reference)` and aggregating in TS costs nothing. **Record the
scale assumption in a comment** — revisit with a view if inventory reaches
five figures.

## Tasks

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure shaping helpers + server fetchers for card inventory and removals</name>
  <files>lib/dashboard/card-inventory.ts, lib/dashboard/card-inventory.test.ts</files>

  <behavior>
Write the Vitest specs first, then the implementation.

Pure helpers (no Supabase imports — keeps them unit-testable, matching
`lib/dashboard/bucketing.ts` convention):
- `snapshotSeries(rows)` → `{ day, cardCount }[]` ascending, one entry per
  distinct `report_date`.
- `firstSeenByCard(rows)` → `Map<reference, earliest report_date>`.
- `latestSnapshot(rows)` → `{ day, references[] }` for the max `report_date`.
- `removalSeries(rows)` → `{ day, removedCount }[]` bucketed by
  `removed_at at UTC`.
- `netChange(series)` → change vs the PREVIOUS snapshot, plus the day-gap
  between them, so the UI can say "+1 since 2 Sep (6 days earlier)" rather
  than implying a daily delta.

Server fetchers in the same file, mirroring `verification-drill.ts`'s shape:
read `card_inventory` and `removed_cards` via the session-scoped server client
(RLS applies), filtered to the 2026-08-13 floor.
  </behavior>

  <verify>npx vitest run lib/dashboard/card-inventory.test.ts</verify>
  <done>Helpers exported and specced, including the gap-aware netChange. Specs cover the sparse-snapshot case (a gap of more than one day between snapshots) explicitly.</done>
</task>

<task type="auto">
  <name>Task 2: Presentation components — charts, KPI cards, card table</name>
  <files>components/dashboard/card-inventory-chart.tsx, components/dashboard/card-removals-chart.tsx, components/dashboard/card-inventory-columns.tsx, components/dashboard/card-inventory-table.tsx, components/dashboard/card-inventory-kpi-cards.tsx</files>

  <behavior>
- Charts: Recharts, mirroring `verifications-chart.tsx` (client components,
  same theming). Time-scaled x-axis per the design decision above — discrete
  bars/dots at real dates, NOT a connected line.
- Removals chart: full linear scale including the 4 Sep spike, paired with a
  callout naming the Invex mobile-app incident. No log axis.
- KPI cards: reuse the `kpi-cards.tsx` pattern — cards enrolled, latest
  snapshot date, total removals in window.
- Table: TanStack Table v8 mirroring `verification-drill-columns.tsx` —
  reference, first seen, present-in-latest. Sortable.
  </behavior>

  <verify>npx tsc --noEmit</verify>
  <done>Components render from the Task 1 shapes; no new dependencies; charts carry the honesty captions.</done>
</task>

<task type="auto">
  <name>Task 3: Page route and sidebar nav entry</name>
  <files>app/(dashboard)/cards/page.tsx, components/app-shell/sidebar-nav.tsx</files>

  <behavior>
Mirror `app/(dashboard)/verifications/page.tsx` exactly: `Suspense` wrapper,
async body Server Component, `Promise.all` fetches, `PageHeader` with
`FreshnessBadge` and the `DATA_WINDOW_CAPTION`, plus `EmptyState` /
`ErrorState` / `LoadingState`. Reproduce the IN-03 behaviour — surface a
freshness-query error as `ErrorState`, never fold it into "no imports yet"
(which is indistinguishable from a legitimately empty table and hides a real
DB fault).

Add `{ href: "/cards", label: "Cards", icon: "card" }` to `NAV_ITEMS`. The
`card` symbol already exists in `public/icons.svg` — verify before committing;
a missing symbol renders as an empty box with no error.
  </behavior>

  <verify>npx tsc --noEmit &amp;&amp; npm run lint &amp;&amp; npx vitest run</verify>
  <done>/cards renders against live data; nav entry active-states correctly; all gates green.</done>
</task>

</tasks>

## Verification

- `npx tsc --noEmit` — clean
- `npm run lint` — 0 errors; baseline is 7 PRE-EXISTING warnings in unrelated
  files, no new ones
- `npx vitest run` — 172 existing tests stay green, plus the new specs

## Out of scope

- Gap-tolerant inventory diff (needs a grain change to inter-snapshot
  intervals — a phase, not a quick task)
- Anything touching the reconciliation views or page
- Applying any migration to the live database
