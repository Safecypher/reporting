---
phase: 02-complete-the-six-sources
plan: 03
type: tdd
wave: 2
depends_on: ["02-01"]
files_modified:
  - lib/ingestion/parsers/dcvv.ts
  - lib/ingestion/normalise-dcvv.ts
  - lib/ingestion/handlers/dcvv.ts
  - supabase/migrations/0007_dcvv.sql
  - lib/ingestion/__tests__/dcvv.test.ts
  - lib/ingestion/__tests__/dcvv.fixture.csv
autonomous: true
requirements: [DATA-01, DATA-05]
must_haves:
  truths:
    - "A user can upload the daily-dCVV report (CSV); it auto-classifies and ingests end-to-end"
    - "dCVV rows de-duplicate on a DB-generated whole-row hash over (raw_timestamp + duration_ms + external_reference) (D-04) — re-upload never double-counts, but genuinely distinct fetch events are always kept"
    - "The Z-suffixed timestamp is stored as UTC timestamptz with the raw string retained for lineage"
  artifacts:
    - path: "lib/ingestion/parsers/dcvv.ts"
      provides: "PapaParse dCVV parser + DcvvRowSchema"
      exports: ["parseDcvv", "validateDcvvRows", "DcvvRowSchema"]
    - path: "lib/ingestion/normalise-dcvv.ts"
      provides: "Z-UTC normaliser + cutoff accounting"
      exports: ["normaliseDcvv"]
    - path: "supabase/migrations/0007_dcvv.sql"
      provides: "dcvv_fetches table with GENERATED row_hash UNIQUE + RLS"
      contains: "generated always as"
  key_links:
    - from: "lib/ingestion/handlers/dcvv.ts"
      to: "deps.upsertRows"
      via: "upsert('dcvv_fetches', rows, { onConflict:'row_hash', ignoreDuplicates:true })"
      pattern: "row_hash"
---

<objective>
Vertical slice: the daily-dCVV report parses, normalises to UTC, de-duplicates on a whole-row hash, and ingests end-to-end through the registry.

Purpose: dCVV has no natural key; conservative whole-row-hash dedup (D-04) preserves every genuinely distinct fetch event while collapsing byte-identical re-ingested rows — the same "never lose a real event" precedent Phase 1 set for verification.
Output: `parsers/dcvv.ts`, `normalise-dcvv.ts`, `handlers/dcvv.ts` (overwriting the stub), migration `0007_dcvv.sql` (+RLS), TDD test against the real 19-row sample.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-complete-the-six-sources/02-CONTEXT.md
@.planning/phases/02-complete-the-six-sources/02-RESEARCH.md
@.planning/phases/02-complete-the-six-sources/02-PATTERNS.md
@lib/ingestion/parsers/verification.ts
@lib/ingestion/normalise.ts
@supabase/migrations/0002_verifications.sql
@supabase/migrations/0004_rls_and_storage.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1 (RED): Failing TDD test against the real 19-row dCVV sample</name>
  <read_first>
    - lib/ingestion/__tests__/ingestion.test.ts (test structure template incl. full-accounting + fake-deps generic upsertRows)
    - lib/ingestion/parsers/verification.ts (parser+Zod template)
    - /Users/markwright/Downloads/daily-dcvv-report_2026-08-13.csv (real sample: 19 rows; header timestamp,duration,ExternalReference; Z-UTC; BOM)
    - .planning/phases/02-complete-the-six-sources/02-PATTERNS.md (dcvv sections, whole-row-hash migration template)
  </read_first>
  <action>
    Copy `/Users/markwright/Downloads/daily-dcvv-report_2026-08-13.csv` to `lib/ingestion/__tests__/dcvv.fixture.csv`. Write lib/ingestion/__tests__/dcvv.test.ts mirroring ingestion.test.ts: classify (filename `daily-dcvv` / header `timestamp,duration,ExternalReference` / null); parseDcvv (19 rows, throws on missing column); validateDcvvRows (reject reasons: unparseable timestamp, invalid/negative duration, missing ExternalReference); normaliseDcvv (timestamp→UTC ISO, raw_timestamp retained, `normalised.length + excludedPreWindow === valid.length`); ingest end-to-end via a local makeFakeDeps generic `upsertRows` — full-accounting equation holds, and re-ingesting the identical rows through `upsertRows('dcvv_fetches', rows, {onConflict:'row_hash'})` yields 0 new inserts while two rows differing in any single column are both kept. Run — MUST fail (RED).
  </action>
  <verify>
    <automated>npm test -- dcvv 2>&1 | grep -Eiq "fail|cannot find|no test" && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>
    - dcvv.fixture.csv is the real 19-row sample
    - Test asserts whole-row-hash idempotency (identical rows collapse; one-column-different rows both kept) and full accounting
    - Test run fails (RED)
  </acceptance_criteria>
  <done>dCVV expected behaviour pinned before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): Implement dCVV parser, normaliser, handler, migration</name>
  <behavior>
    - parseDcvv returns 19 rows; throws when timestamp/duration/ExternalReference missing.
    - DcvvRowSchema rejects unparseable timestamp and negative/invalid duration with reasons.
    - normaliseDcvv stores timestamp as UTC ISO (Z-suffixed → `new Date(raw).toISOString()`, no naiveToUtcMs), retains raw_timestamp, per-type excludedPreWindow.
    - dcvvHandler.upsert → deps.upsertRows('dcvv_fetches', rows, { onConflict:'row_hash', ignoreDuplicates:true }); idempotent re-ingest.
  </behavior>
  <read_first>
    - lib/ingestion/parsers/verification.ts + lib/ingestion/normalise.ts (templates)
    - lib/ingestion/handlers/verification.ts + lib/ingestion/types.ts (ReportHandler wiring, upsertRows)
    - supabase/migrations/0002_verifications.sql (GENERATED row_hash gotcha: hash over the raw string column, never timestamptz::text — not IMMUTABLE)
    - .planning/phases/02-complete-the-six-sources/02-RESEARCH.md (dcvv migration template lines)
  </read_first>
  <action>
    Create lib/ingestion/parsers/dcvv.ts: EXPECTED_COLUMNS `["timestamp","duration","ExternalReference"]`; parseDcvv mirrors parseVerification; DcvvRowSchema with `timestamp` refined `Number.isFinite(Date.parse(v))` (Z-suffixed, no appended Z), `duration` `z.coerce.number().nonnegative()`, `ExternalReference` non-empty; validateDcvvRows copies the validate loop.
    Create lib/ingestion/normalise-dcvv.ts `normaliseDcvv(rows)`: `timestamp = new Date(row.timestamp).toISOString()`, raw_timestamp = row.timestamp, duration_ms = row.duration, external_reference = row.ExternalReference; local DATA_WINDOW_START cutoff into excludedPreWindow.
    Overwrite lib/ingestion/handlers/dcvv.ts with dcvvHandler: parse→parseDcvv, validate→validateDcvvRows, normalise→normaliseDcvv, upsert→`deps.upsertRows("dcvv_fetches", rows, { onConflict:"row_hash", ignoreDuplicates:true })`.
    Create supabase/migrations/0007_dcvv.sql: table `dcvv_fetches` (id bigint identity PK; timestamp timestamptz not null; raw_timestamp text not null; duration_ms numeric not null; external_reference text not null; source_file_id uuid references ingested_files(id); `row_hash text generated always as (md5(raw_timestamp || duration_ms::text || external_reference)) stored`; `constraint dcvv_fetches_row_hash_key unique (row_hash)`). Append RLS enable + select-authenticated policy (mirror 0004). Do NOT push (Wave 3).
    Run to GREEN.
  </action>
  <verify>
    <automated>npm test -- dcvv</automated>
  </verify>
  <acceptance_criteria>
    - parsers/dcvv.ts exports parseDcvv, validateDcvvRows, DcvvRowSchema
    - 0007_dcvv.sql hashes over `raw_timestamp` (the raw string), NOT `timestamp::text`
    - handlers/dcvv.ts calls upsertRows with onConflict:'row_hash' and no longer contains "not implemented yet"
    - `npm test -- dcvv` exits 0 (GREEN)
  </acceptance_criteria>
  <done>dCVV ingests end-to-end and idempotently; whole-row-hash dedup verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → ingest() | Untrusted dCVV CSV bytes |
| ingest() → Postgres | Server-side write with secret key |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-D1 | Integrity | whole-row-hash dedup drift | mitigate | Hash is a DB `GENERATED ALWAYS ... STORED` md5 over the raw string columns (never app-computed) — matches verifications.row_hash precedent; idempotency asserted in test |
| T-02-D2 | Information Disclosure | new dcvv_fetches table readable by anon | mitigate | RLS enable + select-only-authenticated policy in 0007 |
| T-02-D3 | Tampering | malformed/injection rows | mitigate | Zod validation rejects with reasons before insert; nothing silently dropped (CR-02) |
| T-02-SC | Tampering | npm installs | accept | No new packages |
</threat_model>

<verification>
- `npm test -- dcvv` green (idempotency + full-accounting).
- `npx tsc --noEmit` clean.
</verification>

<success_criteria>
dCVV auto-classifies, parses all 19 rows, stores Z-UTC timestamps with raw lineage, and de-duplicates on a DB-generated whole-row hash. Migration + RLS staged for Wave 3.
</success_criteria>

<output>
Create `.planning/phases/02-complete-the-six-sources/02-03-SUMMARY.md` when done.
</output>
