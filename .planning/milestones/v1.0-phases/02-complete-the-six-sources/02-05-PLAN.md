---
phase: 02-complete-the-six-sources
plan: 05
type: tdd
wave: 2
depends_on: ["02-01"]
files_modified:
  - lib/ingestion/parsers/removed-cards.ts
  - lib/ingestion/normalise-removed-cards.ts
  - lib/ingestion/handlers/removed-cards.ts
  - supabase/migrations/0009_removed_cards.sql
  - lib/ingestion/__tests__/removed-cards.test.ts
  - lib/ingestion/__tests__/removed-cards.fixture.csv
autonomous: true
requirements: [DATA-01, DATA-05]
must_haves:
  truths:
    - "A user can upload the removed-cards report (CSV event log); it auto-classifies and ingests end-to-end"
    - "removed-cards is an event log (not a snapshot) — de-dup on a DB-generated whole-row hash over (raw_removed_at + external_card_reference), no report_date (D-03)"
    - "The naive RemovedAt is stored as UTC with raw_removed_at retained (D-01/A1); re-upload never double-counts"
  artifacts:
    - path: "lib/ingestion/parsers/removed-cards.ts"
      provides: "PapaParse parser + RemovedCardsRowSchema"
      exports: ["parseRemovedCards", "validateRemovedCardsRows", "RemovedCardsRowSchema"]
    - path: "lib/ingestion/normalise-removed-cards.ts"
      provides: "naive→UTC normaliser + cutoff accounting"
      exports: ["normaliseRemovedCards"]
    - path: "supabase/migrations/0009_removed_cards.sql"
      provides: "removed_cards table with GENERATED row_hash UNIQUE + RLS"
      contains: "generated always as"
  key_links:
    - from: "lib/ingestion/handlers/removed-cards.ts"
      to: "deps.upsertRows"
      via: "upsert('removed_cards', rows, { onConflict:'row_hash', ignoreDuplicates:true })"
      pattern: "row_hash"
---

<objective>
Vertical slice: the removed-cards event-log report parses, normalises the naive RemovedAt to UTC, de-duplicates on a whole-row hash over (raw_removed_at + external_card_reference), and ingests end-to-end.

Purpose: A card is unenrolled once at a known RemovedAt — an event, not a daily snapshot (D-03). Whole-row-hash dedup keeps every distinct unenrolment while making re-upload idempotent, and needs no report_date.
Output: `parsers/removed-cards.ts`, `normalise-removed-cards.ts`, `handlers/removed-cards.ts` (overwriting the stub), migration `0009_removed_cards.sql` (+RLS), TDD test against the real 3-row sample.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-complete-the-six-sources/02-CONTEXT.md
@.planning/phases/02-complete-the-six-sources/02-RESEARCH.md
@.planning/phases/02-complete-the-six-sources/02-PATTERNS.md
@lib/ingestion/parsers/verification.ts
@lib/ingestion/normalise.ts
@supabase/migrations/0002_verifications.sql
@supabase/migrations/0004_rls_and_storage.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1 (RED): Failing TDD test against the real 3-row removed-cards sample</name>
  <read_first>
    - lib/ingestion/__tests__/ingestion.test.ts (test structure + fake-deps generic upsertRows)
    - lib/ingestion/parsers/verification.ts (parser+Zod template)
    - lib/ingestion/normalise.ts (naiveToUtcMs, cutoff accounting)
    - /Users/markwright/Downloads/removed-cards-report_2026-08-13.csv (real sample: 3 rows; header RemovedAt,ExternalCardReference; naive RemovedAt; BOM)
    - .planning/phases/02-complete-the-six-sources/02-PATTERNS.md (removed-cards / whole-row-hash sections)
  </read_first>
  <action>
    Copy `/Users/markwright/Downloads/removed-cards-report_2026-08-13.csv` to `lib/ingestion/__tests__/removed-cards.fixture.csv`. Write lib/ingestion/__tests__/removed-cards.test.ts: classify (filename `removed-cards` / header `RemovedAt,ExternalCardReference` / null); parseRemovedCards (3 rows, throws on missing column); validateRemovedCardsRows (reject reasons: unparseable RemovedAt, missing card reference); normaliseRemovedCards (removed_at naive→UTC ISO, raw_removed_at retained, per-type excludedPreWindow, NO report_date); ingest end-to-end via a local makeFakeDeps generic upsertRows keyed on `row_hash` — full accounting holds, re-ingest of identical rows yields 0 new inserts, two rows differing in either column both kept. Run — MUST fail (RED).
  </action>
  <verify>
    <automated>npm test -- removed-cards 2>&1 | grep -Eiq "fail|cannot find|no test" && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>
    - removed-cards.fixture.csv is the real 3-row sample
    - Test asserts whole-row-hash idempotency and full accounting, and that no report_date column is produced
    - Test run fails (RED)
  </acceptance_criteria>
  <done>Removed-cards behaviour pinned before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): Implement removed-cards parser, normaliser, handler, migration</name>
  <behavior>
    - parseRemovedCards returns 3 rows; throws when RemovedAt/ExternalCardReference missing.
    - RemovedCardsRowSchema rejects unparseable RemovedAt and missing card reference.
    - normaliseRemovedCards stores removed_at = naiveToUtcMs→ISO, raw_removed_at retained; per-type excludedPreWindow.
    - removedCardsHandler.upsert → deps.upsertRows('removed_cards', rows, { onConflict:'row_hash', ignoreDuplicates:true }); idempotent.
  </behavior>
  <read_first>
    - lib/ingestion/parsers/verification.ts + lib/ingestion/normalise.ts (templates, naiveToUtcMs)
    - lib/ingestion/handlers/verification.ts + lib/ingestion/types.ts
    - supabase/migrations/0002_verifications.sql (row_hash-over-raw-string gotcha) + 0004_rls_and_storage.sql
  </read_first>
  <action>
    Create lib/ingestion/parsers/removed-cards.ts: EXPECTED_COLUMNS `["RemovedAt","ExternalCardReference"]`; parseRemovedCards mirrors parseVerification; RemovedCardsRowSchema: RemovedAt refined `Number.isFinite(Date.parse(\`${v}Z\`))` (naive), ExternalCardReference non-empty; validateRemovedCardsRows copies the loop.
    Create lib/ingestion/normalise-removed-cards.ts `normaliseRemovedCards(rows)`: removed_at = `new Date(naiveToUtcMs(row.RemovedAt)).toISOString()`, raw_removed_at = row.RemovedAt, external_card_reference = row.ExternalCardReference; local DATA_WINDOW_START cutoff into excludedPreWindow.
    Overwrite lib/ingestion/handlers/removed-cards.ts: parse→parseRemovedCards, validate→validateRemovedCardsRows, normalise→normaliseRemovedCards, upsert→`deps.upsertRows("removed_cards", rows, { onConflict:"row_hash", ignoreDuplicates:true })`.
    Create supabase/migrations/0009_removed_cards.sql: table `removed_cards` (id bigint identity PK; removed_at timestamptz not null; raw_removed_at text not null; external_card_reference text not null; source_file_id uuid references ingested_files(id); `row_hash text generated always as (md5(raw_removed_at || external_card_reference)) stored`; `constraint removed_cards_row_hash_key unique (row_hash)`). Append RLS enable + select-authenticated policy. Do NOT push (Wave 3).
    Run to GREEN.
  </action>
  <verify>
    <automated>npm test -- removed-cards</automated>
  </verify>
  <acceptance_criteria>
    - parsers/removed-cards.ts exports parseRemovedCards, validateRemovedCardsRows, RemovedCardsRowSchema
    - 0009_removed_cards.sql hashes over `raw_removed_at` (raw string), has UNIQUE(row_hash), no report_date column, and a select-authenticated policy
    - handlers/removed-cards.ts uses onConflict:'row_hash' and no longer contains "not implemented yet"
    - `npm test -- removed-cards` exits 0 (GREEN)
  </acceptance_criteria>
  <done>Removed-cards ingests end-to-end and idempotently as an event log.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → ingest() | Untrusted removed-cards CSV bytes |
| ingest() → Postgres | Server-side write with secret key |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-R1 | Integrity | whole-row-hash dedup drift | mitigate | DB `GENERATED ALWAYS ... STORED` md5 over raw string columns (never app-computed); idempotency asserted in test |
| T-02-R2 | Information Disclosure | removed_cards readable by anon | mitigate | RLS enable + select-authenticated policy in 0009 |
| T-02-R3 | Tampering | malformed/injection rows | mitigate | Zod validation rejects with reasons before insert (CR-02) |
| T-02-SC | Tampering | npm installs | accept | No new packages |
</threat_model>

<verification>
- `npm test -- removed-cards` green (idempotency + full accounting).
- `npx tsc --noEmit` clean.
</verification>

<success_criteria>
Removed-cards auto-classifies, parses all rows, stores naive RemovedAt as UTC with raw lineage, and de-duplicates on a DB-generated whole-row hash with no report_date. Migration + RLS staged for Wave 3.
</success_criteria>

<output>
Create `.planning/phases/02-complete-the-six-sources/02-05-SUMMARY.md` when done.
</output>
