---
phase: 06-dual-source-alignment-tsys-vs-bit-addict
plan: 09
type: execute
wave: 2
depends_on: ["06-07", "06-08"]
gap_closure: true
files_modified:
  - types/db.ts
  - lib/dashboard/alignment.ts
  - lib/dashboard/alignment-drill.ts
  - app/(dashboard)/alignment/page.tsx
  - components/dashboard/alignment-drill-sheet.tsx
autonomous: false
requirements: [ALIGN-02, ALIGN-03, ALIGN-04]

estimate:
  tokens: 68000
  raw_tokens: 68000
  tasks: 3
  confidence: low

user_setup:
  - service: supabase
    why: "Migrations 0031, 0032 and 0033 must be applied to the linked project before any of this plan's TypeScript is meaningful. tsc, vitest and next build all pass WITHOUT the push — types are read from the generated types/db.ts, not the live database — so the gap closure would verify green while tsys_coverage_complete, counterpart_max_day, bit_addict_max_day and alignment_inventory_diff_rows do not exist. That is precisely the false-positive this phase exists to prevent."
    dashboard_config:
      - task: "No dashboard configuration is required. The established push path in this project is the Supabase MCP apply_migration tool, invoked by the ORCHESTRATOR — executor subagents in this project have no Supabase MCP access and must not attempt the push or report it done."
        location: "Supabase MCP server connection (or a SUPABASE_ACCESS_TOKEN-authenticated `supabase db push`)"

must_haves:
  truths:
    - "Migrations 0031, 0032 and 0033 are applied to the linked Supabase project in ascending order, and types/db.ts is regenerated FROM the live schema — so the presence of tsys_coverage_complete, counterpart_max_day, bit_addict_max_day and alignment_inventory_diff_rows in types/db.ts is itself evidence the push happened"
    - "All four SQL oracles run green against the live project after the push: alignment_truth_table_test.sql (six blocks), alignment_live_cards_test.sql (six blocks), alignment_inventory_diff_rows_test.sql (five blocks) and baseline_as_of_trigger_test.sql (three blocks)"
    - "The live-cards card's coverage statement reads the TSYS-only tsys_coverage_complete for the TSYS side, so when TSYS is fully covered and Bit Addict has no card_inventory snapshot the card states TSYS 1 of 1 and Bit Addict 0 of 1 — never TSYS 0 of 1 (closes CR-02, ALIGN-02)"
    - "A card can never display Aligned while its own coverage statement admits a gap — the badge and the coverage line are computed from the same two separable signals"
    - "fetchLiveCardsDayBreakdown's per-day settled is computed by computeAlignmentSettled from the two independent bound columns the view now exposes, so the TypeScript day-breakdown and the SQL RPC agree on settling under divergent per-side freshness (closes WR-01's TypeScript half, ALIGN-03)"
    - "addBusinessDaysLocal no longer exists in lib/dashboard/alignment-drill.ts — the business-day arithmetic has exactly one implementation in this codebase, the exported and unit-tested addBusinessDaysUtc"
    - "Drilling into the enrolled or unenrolled figure for a day shows the cards that actually entered or left the inventory on that day, and the row count of that sub-table equals the figure being explained (closes WR-04, ALIGN-04)"
    - "Each level-2 sub-table still carries one From {file_name} caption per distinct source file, and still states its own emptiness independently of the other side"
    - "A day whose prior-day snapshot is missing shows the level-2 Bit Addict empty state rather than an entire inventory snapshot presented as that day's new cards (ALIGN-04 empty edge)"
    - "The drill passes only the two literal direction values derived from the whitelisted alignment drill entity into the RPC — no search parameter reaches a query fragment (ALIGN-04 concurrency and injection edges)"
    - "npx vitest run, npx tsc --noEmit and npm run build are all green after the change, with the build still generating 17 routes"
  prohibitions:
    - "No plan in this gap-closure run edits supabase/migrations/0020_v_apigee_cross_check.sql, 0018_v_reconciliation_billing.sql, 0019_v_reconciliation_inventory.sql, 0021_fix_reconciliation_settling.sql or 0022_reconciliation_no_source_data.sql — the D-01 binding constraint"
    - "No plan in this gap-closure run modifies, replaces, supersedes or renumbers 06-01-PLAN.md through 06-06-PLAN.md, and no migration 0027 through 0033 is edited in place once authored"
    - "No executor subagent attempts the migration push or reports it applied — that task belongs to the orchestrator"
    - "The combined coverage_complete flag is never used again as the TSYS-side coverage figure"
    - "The level-2 enrolled or unenrolled sub-table is never presented as the day-over-day set difference while actually returning a full snapshot"
    - "No new npm, pip or cargo package is installed by this run"
  artifacts:
    - path: "types/db.ts"
      provides: "Types regenerated from the live schema after 0031, 0032 and 0033 — the evidence the push actually happened"
      contains: "alignment_inventory_diff_rows"
    - path: "lib/dashboard/alignment.ts"
      provides: "AlignmentLiveCardsRow carrying tsys_coverage_complete, and fetchAlignmentInventoryDiffRows"
      contains: "tsys_coverage_complete"
    - path: "lib/dashboard/alignment-drill.ts"
      provides: "Per-side settled in the live-cards day breakdown, and contributing rows sourced from the set-difference RPC for enrolled/unenrolled"
      contains: "computeAlignmentSettled"
    - path: "app/(dashboard)/alignment/page.tsx"
      provides: "The live-cards coverage statement reading the TSYS-only signal"
      contains: "tsys_coverage_complete"
    - path: "components/dashboard/alignment-drill-sheet.tsx"
      provides: "The per-metric basis caption stating what the Bit Addict sub-table's rows are"
      contains: "Bit Addict rows"
  key_links:
    - from: "alignment_live_cards_for_period.tsys_coverage_complete"
      to: "app/(dashboard)/alignment/page.tsx live-cards PairedMetricCard"
      via: "AlignmentLiveCardsRow.tsys_coverage_complete feeding tsysCoveredDays, replacing the combined coverage_complete"
      pattern: "tsysCoveredDays"
    - from: "alignment_inventory_diff_rows"
      to: "components/dashboard/alignment-drill-sheet.tsx Bit Addict sub-table"
      via: "fetchAlignmentInventoryDiffRows mapped into AlignmentBitAddictContributingRow, preserving the From {file_name} caption"
      pattern: "alignment_inventory_diff_rows"
    - from: "v_alignment_live_cards_daily.tsys_max_day and bit_addict_max_day"
      to: "fetchLiveCardsDayBreakdown"
      via: "computeAlignmentSettled, replacing the single combined maxDay comparison"
      pattern: "bit_addict_max_day"
---

<objective>
Apply the three fix migrations to the live database, then make the TypeScript and the UI consume
what they now expose: the TSYS-only coverage signal (**CR-02**), the two independent settling
bounds in the live-cards day breakdown (**WR-01**'s TypeScript half), and the real day-over-day
set difference in the level-2 drill (**WR-04**).

Purpose: the coverage statement's whole job (D-11/D-12, UI-SPEC E2) is to tell the team *which
side* to go chase. Misattributing the gap to TSYS when Bit Addict is the side missing data sends
whoever is triaging to look in the wrong place — a confidently wrong diagnostic statement, not
merely an unknown one. And SC4/D-19 promise a drill to *the contributing rows*; a user auditing
"Enrolled cards: 12" must see 12 rows, not a thousand.

Output: three applied migrations, regenerated types, and the consuming changes across four
TypeScript/TSX files.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-dual-source-alignment-tsys-vs-bit-addict/06-REVIEW.md
@.planning/phases/06-dual-source-alignment-tsys-vs-bit-addict/06-VERIFICATION.md
@.planning/phases/06-dual-source-alignment-tsys-vs-bit-addict/06-07-SUMMARY.md
@.planning/phases/06-dual-source-alignment-tsys-vs-bit-addict/06-08-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: [BLOCKING] Apply 0031, 0032 and 0033 to the live database, regenerate types, run all four oracles</name>

  <precondition>The orchestrator (not an executor subagent) has a working Supabase MCP connection to the linked project, or a `SUPABASE_ACCESS_TOKEN`-authenticated `supabase` CLI link. Executor subagents in this project have no Supabase MCP access; if this task is reached by an executor, halt and hand back to the orchestrator rather than reporting it done.</precondition>

  <read_first>
    - `supabase/migrations/0031_alignment_per_source_settling.sql`,
      `supabase/migrations/0032_alignment_inventory_diff_rows.sql` and
      `supabase/migrations/0033_baseline_as_of_on_offset_change.sql` — read all three before
      applying anything; 0031 contains a `drop function` that must succeed before its re-creation
      in the same file, so a partial apply leaves `alignment_live_cards_for_period` missing.
    - `.planning/phases/06-dual-source-alignment-tsys-vs-bit-addict/06-06-SUMMARY.md` — the
      record of how 0027 through 0030 were applied in this project, including the MCP path used.
    - `supabase/tests/alignment_truth_table_test.sql`,
      `supabase/tests/alignment_live_cards_test.sql`,
      `supabase/tests/alignment_inventory_diff_rows_test.sql` and
      `supabase/tests/baseline_as_of_trigger_test.sql` — all four are read-only and fixture-free;
      none may be edited to make a run pass.
  </read_first>

  <files>types/db.ts</files>

  <action>
    This is an ORCHESTRATOR / HUMAN step. Build and type checks pass without it, which is exactly
    why it is blocking: a green `tsc`, a green `vitest` and a clean `next build` would otherwise
    certify a gap closure against a schema that does not exist.

    Apply the three migrations to the linked Supabase project in ascending order — 0031, then
    0032, then 0033 — via the Supabase MCP `apply_migration` tool, or `supabase db push` if a
    `SUPABASE_ACCESS_TOKEN`-authenticated CLI link is available. Apply each file whole; 0031 in
    particular drops and re-creates `alignment_live_cards_for_period` and re-issues its grants, so
    a partial application leaves the RPC either missing or reachable by the wrong roles.

    If any statement errors, stop. Do not edit a migration to make it apply and do not skip
    ahead — report the exact error and the statement that produced it, and hand back.

    Regenerate the types from the live schema: `supabase gen types typescript --linked > types/db.ts`.
    This is the load-bearing step for verification, because the generated file is derived from the
    live database rather than from the migration text. If `tsys_coverage_complete`,
    `counterpart_max_day`, `bit_addict_max_day` and `alignment_inventory_diff_rows` appear in the
    regenerated `types/db.ts`, the push provably happened; if they do not, it provably did not.

    Then execute all four SQL oracles against the live project and record their NOTICE output
    verbatim in the plan SUMMARY — not a pass/fail summary, the actual values:
    - `supabase/tests/alignment_truth_table_test.sql` — six blocks. Block F's NOTICE prints the
      three per-source maxima; record them. If all three coincide, say so explicitly: it means
      today's dataset did not itself exercise divergence and the divergence evidence rests on
      Block D's literal-argument cases and on `alignment-settled.test.ts`.
    - `supabase/tests/alignment_live_cards_test.sql` — six blocks. Record the two per-side maxima
      from Block E and the three coverage values from Block F.
    - `supabase/tests/alignment_inventory_diff_rows_test.sql` — five blocks. Record the paired-day
      count from the summary NOTICE. A run reporting zero paired days is NOT evidence WR-04 is
      closed; record that fact plainly rather than treating the vacuous pass as a green gate.
    - `supabase/tests/baseline_as_of_trigger_test.sql` — three blocks, catalog-only.

    Finally run the Supabase security advisors and confirm no new RLS-bypass, missing-RLS or
    mutable-`search_path` warning is attributable to anything added by 0031, 0032 or 0033.
  </action>

  <instructions>
    Plans 06-07 and 06-08 have already authored and committed all three migrations and all four
    SQL oracles. Every automatable step is done. The ONE thing requiring an orchestrator or human
    is the push itself, because executor subagents in this project have no Supabase MCP access
    and there is no `SUPABASE_ACCESS_TOKEN` in the executor environment.

    1. Apply `supabase/migrations/0031_alignment_per_source_settling.sql`, then
       `0032_alignment_inventory_diff_rows.sql`, then
       `0033_baseline_as_of_on_offset_change.sql` — in that order, each file applied whole —
       via the Supabase MCP `apply_migration` tool, or `supabase db push` if an authenticated
       CLI link exists.
    2. Regenerate the types: `supabase gen types typescript --linked > types/db.ts`.
    3. Execute all four oracles against the live project and paste their NOTICE output into the
       plan SUMMARY verbatim: `alignment_truth_table_test.sql`,
       `alignment_live_cards_test.sql`, `alignment_inventory_diff_rows_test.sql`,
       `baseline_as_of_trigger_test.sql`.
    4. Run the Supabase security advisors and confirm no new warning is attributable to 0031,
       0032 or 0033.

    If any statement errors, stop and report the exact error and the statement that produced it.
    Do not edit a migration to make it apply, and do not skip ahead to a later file.
  </instructions>

  <verification>
    `types/db.ts` contains `tsys_coverage_complete`, `counterpart_max_day`, `bit_addict_max_day`
    and `alignment_inventory_diff_rows` — the generated file is derived from the live database,
    so their presence is proof the push happened and their absence is proof it did not.
  </verification>

  <resume-signal>Type "applied" once all three migrations are live, `types/db.ts` is regenerated, all four oracle runs are recorded in the SUMMARY, and the advisor report is clean — or paste the failing statement and its error.</resume-signal>

  <acceptance_criteria>
    - `types/db.ts` contains `tsys_coverage_complete`, `counterpart_max_day`, `bit_addict_max_day`
      and `alignment_inventory_diff_rows`.
    - All four oracles complete without raising an exception, and each one's final summary NOTICE
      is captured verbatim in the plan SUMMARY.
    - The observed per-source maxima from truth-table Block F and live-cards Block E are recorded
      as values in the SUMMARY, not as a "divergent / not divergent" judgement.
    - The paired-day count from the inventory-diff oracle is recorded, and if it is zero that is
      stated as an unproven case rather than a pass.
    - The Supabase security advisor report shows no new warning attributable to 0031, 0032 or
      0033.
    - Each oracle file still carries the block count plans 06-07 and 06-08 gave it — none was
      trimmed or weakened to make a run pass.
  </acceptance_criteria>

  <verify>
    <automated>grep -cE 'tsys_coverage_complete|counterpart_max_day|bit_addict_max_day|alignment_inventory_diff_rows' types/db.ts</automated>
    <fails_when>a count below 4, or a non-zero exit from grep — either means the regenerated types do not carry the new schema, which means the migrations were not applied</fails_when>
    <automated>grep -c '^do \$\$' supabase/tests/alignment_truth_table_test.sql supabase/tests/alignment_live_cards_test.sql supabase/tests/alignment_inventory_diff_rows_test.sql supabase/tests/baseline_as_of_trigger_test.sql</automated>
    <fails_when>the first two files reporting below 7, the third below 6, or the fourth below 4 — any lower count means an oracle was trimmed rather than executed as written</fails_when>
    <automated>npx tsc --noEmit</automated>
    <fails_when>non-zero exit, or any line of output at all</fails_when>
  </verify>

  <done>0031, 0032 and 0033 are live, `types/db.ts` is regenerated from the live schema and carries all four new symbols, all four oracles ran green with their observed values recorded, and the security advisors report nothing new.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Consume the separable signals — TSYS-only coverage on the live-cards card, per-side settled in its day breakdown</name>

  <precondition>Migrations 0031, 0032 and 0033 are applied and `types/db.ts` has been regenerated (Task 1 complete), so `tsys_coverage_complete`, `tsys_max_day` and `bit_addict_max_day` exist in the generated types.</precondition>

  <read_first>
    - `.planning/phases/06-dual-source-alignment-tsys-vs-bit-addict/06-REVIEW.md` — the **CR-02**
      section in full, including its note that `fetchLiveCardsDayBreakdown` already derives the
      two signals correctly and is the pattern to copy; and the **WR-01** section.
    - `lib/dashboard/alignment.ts` lines 149-211 — `AlignmentLiveCardsRow`, `EMPTY_LIVE_CARDS` and
      `fetchAlignmentLiveCards`, including the `as unknown` two-step cast convention the file
      documents at lines 105-110.
    - `app/(dashboard)/alignment/page.tsx` lines 426-459 — the live-cards `PairedMetricCard`
      block. Lines 442-443 are the defect.
    - `lib/dashboard/alignment-drill.ts` lines 101-124 (the private `addBusinessDaysLocal`),
      lines 157-282 (`fetchLiveCardsDayBreakdown`, whose `boundsResult` query and `settled`
      computation both change), and lines 253-260 where `tsysCovered`/`bitAddictCovered` are
      already derived correctly from the two separate signals.
    - `lib/dashboard/alignment-status.ts` — the `computeAlignmentSettled` and `addBusinessDaysUtc`
      exports added by plan 06-07, and `formatCoverageStatement`.
    - `lib/dashboard/__tests__/alignment-settled.test.ts` — the existing coverage, so this task's
      new cases extend rather than duplicate it.
  </read_first>

  <files>lib/dashboard/alignment.ts, app/(dashboard)/alignment/page.tsx, lib/dashboard/alignment-drill.ts</files>

  <behavior>
    - Given a live-cards RPC row with `tsys_coverage_complete: true`, `coverage_complete: false`
      and `bit_addict_snapshot_day: null`, the coverage figures passed to `PairedMetricCard` are
      `tsysCoveredDays: 1`, `bitAddictCoveredDays: 0`, `totalDays: 1` — the TSYS side is not
      reported as uncovered because the Bit Addict side is.
    - Given `tsys_coverage_complete: false` and a non-null `bit_addict_snapshot_day`, the figures
      are `tsysCoveredDays: 0`, `bitAddictCoveredDays: 1`.
    - Given both true, the figures are `1` and `1` and `coverage_complete` is true.
    - `fetchLiveCardsDayBreakdown` marks a day unsettled when the card_inventory maximum has not
      reached `add_business_days(day, 3)` even though the apigee maximum is far past it, and
      unsettled in the mirror-image case.
    - `fetchLiveCardsDayBreakdown` marks a day settled only when both maxima have reached
      `add_business_days(day, 3)`, inclusive of landing exactly on it.
  </behavior>

  <action>
    In `lib/dashboard/alignment.ts`, add `tsys_coverage_complete: boolean` as the final field of
    the `AlignmentLiveCardsRow` interface, and add `tsys_coverage_complete: false` to
    `EMPTY_LIVE_CARDS` — a period with no live-cards data at all has no TSYS coverage either, and
    the empty row must never imply otherwise. `fetchAlignmentLiveCards` itself needs no change;
    it already returns the RPC row through the documented `as unknown` two-step cast, so the new
    column arrives with it.

    In `app/(dashboard)/alignment/page.tsx`, change the live-cards `PairedMetricCard`'s
    `tsysCoveredDays` to read `liveCardsResult.data.tsys_coverage_complete` instead of
    `liveCardsResult.data.coverage_complete`. Leave `bitAddictCoveredDays` and `totalDays` exactly
    as they are — the Bit Addict side was already derived from its own signal
    (`bit_addict_snapshot_day !== null`), and `totalDays: 1` is the correct expression of a
    whole-window boolean guard as a one-of-one pair. Replace the existing explanatory comment
    above these three lines with one that states the new rule plainly: the TSYS-only running
    guard and the Bit Addict snapshot presence are two independent signals, and the combined
    `coverage_complete` returned by the RPC is the AND of them, so it can never serve as the
    figure for either side on its own. Name CR-02 in that comment so a future reader finds the
    reasoning rather than re-deriving it.

    In `lib/dashboard/alignment-drill.ts`:

    Delete the private `addBusinessDaysLocal` function entirely and remove the block comment above
    it that explains the local-mirror tradeoff. Import `computeAlignmentSettled` from
    `@/lib/dashboard/alignment-status` alongside the existing `computeAlignmentStatus` and
    `computeAlignmentShortSide` imports. The business-day arithmetic now has exactly one
    implementation in the codebase, and it is the unit-tested one.

    In `fetchLiveCardsDayBreakdown`, widen the bounds query: it currently selects only `day` from
    `v_alignment_live_cards_daily` ordered descending with a limit of one. Select `day`,
    `tsys_max_day` and `bit_addict_max_day` instead, typed by a small dedicated row interface, and
    keep the same ordering, limit and `maybeSingle()` shape. Both new columns are constant across
    every row of the view, so one row still answers the question. Replace the `settled`
    computation inside the `ascRows.map` callback — currently a single comparison of the combined
    `maxDay` against the locally-computed business-day target — with a call to
    `computeAlignmentSettled(row.day, <the tsys_max_day from the bounds row>, <the
    bit_addict_max_day from the bounds row>)`, passing `null` for either bound when the bounds
    query returned no row. Leave the D-07 gap-change derivation, `previousGap` threading,
    `coverageComplete`, `tsysCovered`, `bitAddictCovered`, `shortSide` and the `computeAlignmentStatus`
    arguments untouched — this change alters settling only. Update the function's JSDoc to say it
    now mirrors `alignment_live_cards_for_period`'s per-side settling as well as its gap-change
    derivation.

    Extend `lib/dashboard/__tests__/alignment-settled.test.ts` with a describe block asserting the
    three live-cards coverage-figure cases in this task's `<behavior>` list, written against a
    small exported pure helper if one is needed to keep them testable without a Supabase client —
    if a helper is added, export it from `lib/dashboard/alignment-status.ts` and name it for what
    it computes rather than for where it is used.
  </action>

  <acceptance_criteria>
    - `lib/dashboard/alignment.ts`'s `AlignmentLiveCardsRow` declares `tsys_coverage_complete` and
      `EMPTY_LIVE_CARDS` sets it to `false`.
    - In `app/(dashboard)/alignment/page.tsx`, the expression assigned to `tsysCoveredDays`
      references `tsys_coverage_complete`.
    - `lib/dashboard/alignment-drill.ts` contains no function named `addBusinessDaysLocal` and
      imports `computeAlignmentSettled`.
    - `fetchLiveCardsDayBreakdown`'s bounds query selects `tsys_max_day` and `bit_addict_max_day`.
    - Every case in this task's `<behavior>` block has a corresponding assertion in
      `lib/dashboard/__tests__/alignment-settled.test.ts`.
    - `npm run build` generates 17 routes.
  </acceptance_criteria>

  <verify>
    <automated>npx vitest run</automated>
    <fails_when>non-zero exit, or any failed count above zero, or a total passing count lower than the count recorded in 06-07-SUMMARY.md</fails_when>
    <automated>npx tsc --noEmit</automated>
    <fails_when>non-zero exit, or any line of output at all</fails_when>
    <automated>npx tsc --noEmit &amp;&amp; npm run build</automated>
    <fails_when>non-zero exit, or the route summary reporting fewer than 17 routes, or any "Failed to compile" line</fails_when>
    <automated>grep -c 'addBusinessDaysLocal' lib/dashboard/alignment-drill.ts</automated>
    <fails_when>any count above 0, or a zero exit status with a non-zero count — the duplicate business-day implementation must be gone</fails_when>
    <automated>grep -v '^[[:space:]]*//' lib/dashboard/alignment-drill.ts | grep -cE 'tsys_max_day|bit_addict_max_day|computeAlignmentSettled'</automated>
    <fails_when>a count below 4, or a non-zero exit from grep</fails_when>
  </verify>

  <done>The live-cards card states which side is actually uncovered, its day breakdown settles on two independent bounds in agreement with the SQL RPC, and the codebase has exactly one business-day implementation.</done>
</task>

<task type="auto">
  <name>Task 3: Level-2 contributing rows become the set difference the figure counts</name>

  <precondition>Migration 0032 is applied and `types/db.ts` has been regenerated (Task 1 complete), so `alignment_inventory_diff_rows` is callable through the RPC client.</precondition>

  <read_first>
    - `.planning/phases/06-dual-source-alignment-tsys-vs-bit-addict/06-REVIEW.md` — the **WR-04**
      section in full, including its explanation of why `removed_cards` is not interchangeable
      with the inventory-side drop.
    - `supabase/migrations/0032_alignment_inventory_diff_rows.sql` — the RPC's exact argument
      names (`p_day`, `p_direction`), its two accepted direction literals, and its four returned
      columns.
    - `lib/dashboard/alignment-drill.ts` lines 380-498 — `fetchAlignmentContributingRows` in full,
      its three raw-row interfaces, the `AlignmentBitAddictContributingRow` shape it maps into,
      the `Promise.all` two-sided fetch, the `DRILL_ROW_LIMIT` cap and the error-safe fallback
      that logs server-side and never throws.
    - `components/dashboard/alignment-drill-sheet.tsx` lines 213-350 — the level-2 body, the two
      `h3` sub-table headings, the `From {file_name}` caption rendering via `distinctFileNames`,
      and the per-side empty-state copy.
    - `06-UI-SPEC.md` `## UI Considerations` section E4 — the level-2 bullets, which bind the two
      sub-table headings to the exact strings `TSYS rows` and `Bit Addict rows` and bind the
      per-distinct-file caption behaviour.
  </read_first>

  <files>lib/dashboard/alignment.ts, lib/dashboard/alignment-drill.ts, components/dashboard/alignment-drill-sheet.tsx</files>

  <action>
    In `lib/dashboard/alignment.ts`, add `fetchAlignmentInventoryDiffRows(supabase, day, direction)`
    following the file's existing fetcher convention exactly: call
    `supabase.rpc("alignment_inventory_diff_rows", { p_day: day, p_direction: direction })`, apply
    `.limit(DRILL_ROW_LIMIT)` so the level-2 cap is preserved, cast the result through the
    documented `as unknown` two step into a row interface declaring `external_card_reference`,
    `report_date`, `created_at` and `file_name`, log the raw error server-side and return a
    discriminated result rather than throwing. Type `direction` as a union of the two literals
    `"enrolled"` and `"unenrolled"` so no other value is expressible at the call site — the
    function's own SQL guard is the second layer, not the first.

    In `lib/dashboard/alignment-drill.ts`, rewrite the Bit Addict branch of
    `fetchAlignmentContributingRows`. Today it is a three-way conditional selecting from
    `card_inventory` for enrolled and live-cards, `removed_cards` for unenrolled, and
    `verifications` for volume. It becomes:
    - `enrolled` and `unenrolled` call `fetchAlignmentInventoryDiffRows` with the matching
      direction literal, derived from the already-narrowed `metric` value — never from a raw
      search parameter.
    - `live-cards` keeps its existing `card_inventory` read for `report_date = day`. That metric's
      figure IS the whole snapshot's distinct card count (a stock metric, L-02), so the full
      snapshot is the correct contributing rowset for it; leave it alone and add a short comment
      saying why, so a future reader does not "fix" it into a set difference by analogy.
    - `volume` keeps its existing `verifications` read unchanged.
    Keep the two sides fetched in parallel via `Promise.all`, keep them explicitly separated in
    the returned result (Pitfall 5 — never merged into one flat list), and keep the error-safe
    fallback that logs and returns an empty result. Map the RPC rows into
    `AlignmentBitAddictContributingRow` with `eventTime` from `created_at`,
    `externalCardReference` from `external_card_reference` and `fileName` from `file_name`,
    matching the shape the existing columns already render. Delete the now-unused
    `RemovedCardContributingRawRow` interface and the `removed_at` discriminator branch in the
    mapping callback if nothing else references them. Update the function's JSDoc: it currently
    states that the Bit Addict rows come from "`card_inventory` for enrolled/live-cards,
    `removed_cards` for unenrolled" — that sentence is now wrong and must state the new sourcing
    per metric, and must say that enrolled/unenrolled return the day-over-day set difference the
    aggregate counts rather than the raw day's rows.

    In `components/dashboard/alignment-drill-sheet.tsx`, keep both `h3` headings exactly as they
    are — UI-SPEC E4 binds them to the literal strings `TSYS rows` and `Bit Addict rows`, and the
    per-side empty-state copy and the `From {file_name}` captions stay unchanged. Add one line of
    secondary caption directly beneath the Bit Addict heading, rendered only for the enrolled and
    unenrolled metrics, naming the basis of the rows shown: for enrolled, that these are the cards
    in this day's inventory snapshot that were not in the previous day's; for unenrolled, that
    these are the cards in the previous day's snapshot that are no longer in this day's. Style it
    with the same `text-xs font-light text-muted-foreground` treatment the existing file-name
    captions use, so it reads as provenance rather than as a new heading level. This caption is
    what makes the sub-table self-describing for the two metrics where the rowset is a derived set
    rather than a raw table read — the auditability SC4 asks for.
  </action>

  <acceptance_criteria>
    - `lib/dashboard/alignment.ts` exports `fetchAlignmentInventoryDiffRows` whose `direction`
      parameter is typed as a union of exactly two string literals.
    - `lib/dashboard/alignment-drill.ts` calls `fetchAlignmentInventoryDiffRows` for the
      `enrolled` and `unenrolled` metrics and no longer queries `removed_cards`.
    - The `live-cards` branch still reads `card_inventory` for the day, with a comment stating why
      the whole snapshot is correct for a stock metric.
    - `fetchAlignmentContributingRows`'s JSDoc no longer claims `removed_cards` supplies the
      unenrolled rows.
    - `components/dashboard/alignment-drill-sheet.tsx` still renders the exact heading strings
      `TSYS rows` and `Bit Addict rows`, still renders one `From {file_name}` caption per distinct
      file, and renders a metric-specific basis caption for enrolled and unenrolled only.
    - `npm run build` generates 17 routes.
  </acceptance_criteria>

  <verify>
    <automated>npx vitest run</automated>
    <fails_when>non-zero exit, or any failed count above zero</fails_when>
    <automated>npx tsc --noEmit</automated>
    <fails_when>non-zero exit, or any line of output at all</fails_when>
    <automated>npx tsc --noEmit &amp;&amp; npm run build</automated>
    <fails_when>non-zero exit, or the route summary reporting fewer than 17 routes, or any "Failed to compile" line</fails_when>
    <automated>grep -c 'removed_cards' lib/dashboard/alignment-drill.ts</automated>
    <fails_when>any count above 0 — the unenrolled drill must no longer source the independently-sourced removed-cards log</fails_when>
    <automated>grep -c 'alignment_inventory_diff_rows' lib/dashboard/alignment.ts</automated>
    <fails_when>a count below 1, or a non-zero exit from grep</fails_when>
    <automated>grep -c 'Bit Addict rows' components/dashboard/alignment-drill-sheet.tsx</automated>
    <fails_when>a count below 1, or a non-zero exit from grep — the UI-SPEC E4 heading string must survive the change</fails_when>
  </verify>

  <done>Drilling into an enrolled or unenrolled figure shows exactly the cards that entered or left the inventory that day, with their source file, under the spec's own headings and a caption stating the basis — and the drill for an unpaired day shows the honest empty state rather than a thousand unrelated rows.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration file → live database | The push is the only point where the fix becomes real; a skipped push produces a fully green verification against a schema that does not exist |
| URL drill parameter → RPC argument | The level-2 drill's metric and day originate in the URL; both must stay whitelisted and typed |
| authenticated session → `alignment_inventory_diff_rows` | Card references and file names cross here and must stay behind RLS |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06G-13 | Repudiation | the migration push | high | mitigate | The push is a blocking, non-autonomous orchestrator task, and its completion is verified by grepping the REGENERATED `types/db.ts` — which is derived from the live database, so the four new symbols cannot appear there unless the push actually happened. A claimed-but-unperformed push fails the gate. |
| T-06G-14 | Elevation of Privilege | 0031's dropped-and-recreated RPC | high | mitigate | Task 1 applies each migration whole and refuses to continue past an error, so the `drop function` and its following `grant execute ... to authenticated` land together. Confirmed by the security-advisor pass and by the live-cards oracle successfully calling the RPC as the session role. |
| T-06G-15 | Tampering | `p_direction` passed from the drill | high | mitigate | The TypeScript union type admits only `"enrolled"` and `"unenrolled"`, and both are derived from the already-narrowed `AlignmentMetric`, never from a raw search parameter; 0032's internal CASE whitelist is the second layer. |
| T-06G-16 | Information Disclosure | `alignment_inventory_diff_rows` results rendered in the Sheet | high | mitigate | Called through the session-scoped `lib/supabase/server.ts` client so RLS applies; the RPC is `security invoker` with EXECUTE granted to `authenticated` only. Never called with a secret-key client. |
| T-06G-17 | Information Disclosure | the live-cards coverage statement | medium | mitigate | CR-02's fix makes the statement name the correct side; a misattributed statement is a disclosure of a false fact about system state, which for a reconciliation tool is the harm that matters. Asserted by the behaviour cases in Task 2. |
| T-06G-18 | Denial of Service | level-2 drill over a large snapshot | low | accept | `DRILL_ROW_LIMIT` still caps the rendered rows, and the set difference returns strictly fewer rows than the previous whole-snapshot read for the two affected metrics. |
| T-06G-SC | Tampering | npm/pip/cargo installs | high | mitigate | This plan installs no package. If a dependency becomes necessary, stop and route through the researcher's Package Legitimacy Gate before any install; `[ASSUMED]`/`[SUS]` packages require a blocking human checkpoint and `[SLOP]` is forbidden. |
</threat_model>

<verification>
- `types/db.ts` carries all four new symbols, proving the push.
- All four SQL oracles ran green against the live project, with their observed values recorded
  verbatim in the SUMMARY rather than summarised as pass/fail.
- `npx vitest run`, `npx tsc --noEmit` and `npx tsc --noEmit && npm run build` are all green, with
  the build still generating 17 routes.
- `lib/dashboard/alignment-drill.ts` contains neither `addBusinessDaysLocal` nor `removed_cards`.
- The Supabase security advisors report no new warning attributable to 0031, 0032 or 0033.
</verification>

<success_criteria>
The three fix migrations are live and provably so; the live-cards card names the side that is
actually uncovered; the live-cards day breakdown and the live-cards RPC agree on settling under
divergent per-side freshness; and the level-2 drill for enrolled and unenrolled returns exactly
the rows the aggregate counts, captioned with their basis and their source file.
</success_criteria>

## Artifacts this plan produces

| Symbol | Kind | Location |
|---|---|---|
| `AlignmentLiveCardsRow.tsys_coverage_complete` | TS interface field | `lib/dashboard/alignment.ts` |
| `fetchAlignmentInventoryDiffRows` | exported TS function | `lib/dashboard/alignment.ts` |
| regenerated `types/db.ts` | generated types from the live schema | `types/` |
| level-2 Bit Addict basis caption (enrolled / unenrolled) | UI element | `components/dashboard/alignment-drill-sheet.tsx` |

Removed by this plan: `addBusinessDaysLocal` and `RemovedCardContributingRawRow` in
`lib/dashboard/alignment-drill.ts`.

## UI Considerations lift

Lifted from `06-UI-SPEC.md` `## UI Considerations` because this plan's gaps bear on them:

- E2: "Partial data in a paired KPI card is the D-12 case: incomplete coverage forces
  `needs_review` regardless of variance size, and the coverage line's clause and the badge must
  always agree — a card can never show `Aligned` while its own coverage statement admits a gap."
  → carried as a `must_haves` truth; this is exactly what CR-02 broke.
- E4: "The populated level 2 shows two labelled sub-tables (`TSYS rows` / `Bit Addict rows`), each
  captioned `From {file_name}` once per distinct source file, with the `← Back to day breakdown`
  affordance at the top of the body." → Task 3 binds the heading strings and the caption
  behaviour explicitly so the WR-04 rowset change does not disturb them.
- E4: "Level 2 shows `No {TSYS/Bit Addict} rows for this day.` per sub-table when that side has no
  contributing rows — each side's emptiness is stated independently." → the unpaired-day zero-row
  case must render this, not an error.
- E4: "A day whose rows span more than one uploaded file shows a `From {file_name}` caption per
  distinct file rather than naming only the first." → the RPC returns a per-row file name, so
  `distinctFileNames` keeps working unchanged.
- E1: "The `/alignment` page renders an error state naming the failed surface with a retry path."
  → unchanged by this plan; the new RPC's failure path returns the existing scoped error result
  rather than throwing.

Not re-lifted here: E3, E5, E6, E7 and E8 are untouched by this plan and remain covered by plans
06-01 through 06-06 and by `06-UAT.md`.

<output>
Create `.planning/phases/06-dual-source-alignment-tsys-vs-bit-addict/06-09-SUMMARY.md` when done.
</output>
