---
phase: 04-reconciliation-discrepancy-flagging
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - supabase/migrations/0018_v_reconciliation_billing.sql
  - lib/dashboard/reconciliation-drill.ts
  - components/dashboard/reconciliation-billing-table.tsx
  - components/dashboard/reconciliation-drill-sheet.tsx
  - app/(dashboard)/reconciliation/page.tsx
  - components/app-shell/sidebar-nav.tsx
autonomous: true
requirements: [RECON-01, RECON-03]
must_haves:
  truths:
    - "A user can open /reconciliation from the sidebar and see a Billing vs verifications section with one row per business day (D-08: single /reconciliation page with a new sidebar nav item; D-01: daily aggregate counts, not per-event)"
    - "Each billing row shows a status badge (OK/Needs review/Mismatch) plus billing count, verification count, and signed delta (D-09: badge + signed delta + which-side-is-short)"
    - "The most-recent day reads 'Needs review' (pending), not 'Mismatch', until its D+1 counterpart data has landed (D-03)"
    - "Clicking a flagged day opens the drill Sheet showing the contributing billing rows AND verification rows for that day, explicitly separated (D-01: drill lists the underlying rows for a flagged day)"
    - "The page implements the 4-state contract (loading / empty / populated / error) and never renders silently-empty on query error"
    - "Flags are bucketed and computed in UTC calendar days regardless of the display-timezone toggle (D-04: status computed in UTC, viewer tz only relabels)"
  artifacts:
    - path: "supabase/migrations/0018_v_reconciliation_billing.sql"
      provides: "v_billing_daily_counts + v_reconciliation_billing_daily (FULL OUTER JOIN + settling state machine)"
      contains: "full outer join"
    - path: "app/(dashboard)/reconciliation/page.tsx"
      provides: "4-state async Server Component rendering the Billing-vs-Verification section + drill dispatch"
      min_lines: 120
    - path: "components/dashboard/reconciliation-billing-table.tsx"
      provides: "'use client' TanStack table with function-bearing column defs, drillable rows"
    - path: "lib/dashboard/reconciliation-drill.ts"
      provides: "fetchReconciliationBillingDrillRows — whitelisted day-range fetch of billing + verification rows"
    - path: "components/dashboard/reconciliation-drill-sheet.tsx"
      provides: "Client wrapper owning drill column defs; billing drill shows both sources explicitly separated"
  key_links:
    - from: "app/(dashboard)/reconciliation/page.tsx"
      to: "v_reconciliation_billing_daily"
      via: "supabase.from('v_reconciliation_billing_daily').select"
      pattern: "v_reconciliation_billing_daily"
    - from: "components/dashboard/reconciliation-billing-table.tsx"
      to: "drill Sheet"
      via: "openDrill({ drill: 'recon-billing', date })"
      pattern: "recon-billing"
    - from: "supabase/migrations/0018_v_reconciliation_billing.sql"
      to: "v_revenue_daily_counts"
      via: "full outer join against the reused all-verifications count view (D-02)"
      pattern: "v_revenue_daily_counts"
---

<objective>
Deliver the billing-vs-verification reconciliation vertical slice end to end — the core-value focal point ("does billing equal verifications?"). SQL settling-window view -> server fetch -> new /reconciliation page (billing section) -> drill into contributing rows -> sidebar nav.

Purpose: RECON-01 + RECON-03 for the billing side. After this slice a user can navigate to /reconciliation, see per-day billing-vs-verification flags with status + signed delta, understand the calm "pending" state for the most recent day, and drill to the underlying billing and verification rows and their source files.
Output: migration 0018 (two views), the drill fetcher, the billing section table, the (billing-only) drill sheet, the new page, and the nav item. The card-inventory section is added by Wave 3 (04-03) which extends this same page/fetcher/drill-sheet.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-reconciliation-discrepancy-flagging/04-CONTEXT.md
@.planning/phases/04-reconciliation-discrepancy-flagging/04-RESEARCH.md
@.planning/phases/04-reconciliation-discrepancy-flagging/04-PATTERNS.md
@.planning/phases/04-reconciliation-discrepancy-flagging/04-UI-SPEC.md
@.planning/phases/04-reconciliation-discrepancy-flagging/04-01-SUMMARY.md

<interfaces>
From 04-01 (this plan depends on it):
```typescript
// lib/dashboard/reconciliation-status.ts
export type ReconciliationStatus = "ok" | "needs_review" | "mismatch";
export type ShortSide = "billing" | "verification" | null;
export function computeReconciliationStatus(billing: number, verification: number, settled: boolean): ReconciliationStatus;
export function computeShortSide(billing: number, verification: number): ShortSide;
export function reconciliationStatusToBadge(status: ReconciliationStatus): { label: string; variant: string };
// components/dashboard/status-badge.tsx
export function StatusBadge({ status }: { status: ReconciliationStatus }): JSX.Element;
// lib/dashboard/drill-params.ts — DrillEntity now includes "recon-billing" | "recon-inventory"
```

Existing verification drill fetcher to mirror (lib/dashboard/verification-drill.ts):
```typescript
export const DATA_WINDOW_START = "2026-08-13T00:00:00Z";
export const DRILL_ROW_LIMIT = 500;
export interface VerificationDrillRow { created_at: string; external_card_reference: string; duration_ms: number; authenticated: boolean; }
export async function fetchVerificationDrillRows(supabase, authenticated?: boolean): Promise<VerificationDrillFetchResult>;
```

Base table columns for SQL + drill queries:
- billing_transactions: event_time (timestamptz), transaction_id, authorised (boolean), verification_kind, region, source_file_id
- verifications: created_at (timestamptz), authenticated (boolean), external_card_reference, duration_ms
- REUSE v_revenue_daily_counts (0012): columns day_utc, verification_count — already all-verifications (D-02); do NOT create a second verification count view.

Reused view template (0013_v_sla_daily.sql shape): create view X with (security_invoker = on) as select date_trunc('day', <ts> at time zone 'UTC') as day_utc, ... from <table> where <ts> >= '2026-08-13T00:00:00Z' group by 1; + comment on view.

DrillSheet generic (components/dashboard/drill-sheet.tsx): props { filter, rows, columns, title, loading?, totalCount? }; open state derives from filter; closing calls useDrill().closeDrill(). useDrill() from components/dashboard/drillable-metric.tsx provides { openDrill, closeDrill }.

Page template to clone: app/(dashboard)/sla/page.tsx — Suspense fallback LoadingState; async Body does createClient(), await searchParams, parseDrillParams, Promise.all of view reads + freshness (ingested_files where status=done order uploaded_at desc limit 1 maybeSingle) + conditional drill fetch; error-first (any result.error -> PageHeader + ErrorState); then render sections + drill sheet. PageSearchParams type per that file. sla-breach-drill-sheet.tsx is the drill-sheet client wrapper template.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Billing reconciliation SQL views (0018)</name>
  <files>supabase/migrations/0018_v_reconciliation_billing.sql</files>
  <read_first>
    - supabase/migrations/0012_v_revenue.sql (view-chain header-comment convention; v_revenue_daily_counts to reuse)
    - supabase/migrations/0013_v_sla_daily.sql (simplest security_invoker + UTC-bucket + cutoff template)
    - supabase/migrations/0005_review_fixes_excluded_and_utc_view.sql (cutoff literal origin)
    - supabase/migrations/0006_billing.sql (billing_transactions columns; event_time is the canonical Z-UTC time)
    - .planning/phases/04-reconciliation-discrepancy-flagging/04-RESEARCH.md sections "Pattern 1" and "Pitfall 1/2/3/4"
    - .planning/phases/04-reconciliation-discrepancy-flagging/04-PATTERNS.md section 0018 (FULL OUTER JOIN target SQL)
    - lib/dashboard/reconciliation-status.ts (the status logic the case expression must mirror exactly)
  </read_first>
  <action>Create supabase/migrations/0018_v_reconciliation_billing.sql with a bottom-up view-chain header comment (mirror 0012 style). Define v_billing_daily_counts with (security_invoker = on): date_trunc('day', event_time at time zone 'UTC') as day_utc, count(*) as billing_count from billing_transactions where event_time >= '2026-08-13T00:00:00Z' group by 1 (count ALL rows incl. authorised=False per D-02). Then define v_reconciliation_billing_daily with (security_invoker = on) as the FULL OUTER JOIN state machine from RESEARCH Pattern 1: a joined CTE that full outer joins v_billing_daily_counts against the REUSED v_revenue_daily_counts using (day_utc) with coalesce(..,0) on both counts; a bounds CTE selecting max(day_utc) as max_day_utc from joined; then select day_utc, billing_count, verification_count, (billing_count - verification_count) as delta, a short_side case (null when equal, 'billing' when billing<verification, else 'verification'), a settled boolean (bounds.max_day_utc >= j.day_utc + interval '1 day'), and a status case producing exactly 'ok' / 'mismatch' / 'needs_review' matching computeReconciliationStatus. Derive settled from max(day_utc) of the joined data ONLY — never now()/current_date (Pitfall 1). Do NOT add a generate_series calendar spine here (Pitfall 2 — a day with zero activity on both sides correctly does not appear). Add a comment on view to each view noting security_invoker=on and the D-02/D-03 semantics.</action>
  <verify>
    <automated>grep -c "security_invoker = on" supabase/migrations/0018_v_reconciliation_billing.sql; grep -qi "full outer join" supabase/migrations/0018_v_reconciliation_billing.sql && grep -q "v_revenue_daily_counts" supabase/migrations/0018_v_reconciliation_billing.sql && ! grep -qiE "current_date|now\(\)|generate_series" supabase/migrations/0018_v_reconciliation_billing.sql && echo GATES_OK</automated>
  </verify>
  <acceptance_criteria>
    - Both v_billing_daily_counts and v_reconciliation_billing_daily carry with (security_invoker = on) (grep count >= 2).
    - The view FULL OUTER JOINs against v_revenue_daily_counts (reuse, not a new verification-count view).
    - settled uses max(day_utc); file contains no current_date/now()/generate_series (GATES_OK printed).
    - Both base views carry the >= '2026-08-13T00:00:00Z' cutoff.
    - The status case yields only 'ok'/'needs_review'/'mismatch' and matches lib/dashboard/reconciliation-status.ts.
  </acceptance_criteria>
  <done>Migration 0018 defines the billing daily-count and settling-window reconciliation views following the project view-chain convention, with no clock-based settling and no spurious calendar spine. (Push to live DB happens in 04-04.)</done>
</task>

<task type="auto">
  <name>Task 2: Billing drill fetcher + drillable billing section table</name>
  <files>lib/dashboard/reconciliation-drill.ts, components/dashboard/reconciliation-billing-table.tsx</files>
  <read_first>
    - lib/dashboard/verification-drill.ts (exact fetcher template: constants, Row/Result interfaces, whitelisted .gte()/.eq()/.limit(), returns<> pattern)
    - components/dashboard/sla-breach-table.tsx ('use client' TanStack section-table template with drillable rows + keyboard handler)
    - components/dashboard/drillable-metric.tsx (useDrill() openDrill signature)
    - .planning/phases/04-reconciliation-discrepancy-flagging/04-PATTERNS.md sections reconciliation-drill.ts and reconciliation-billing-table.tsx (day-range query shape, composition breakdown requirement, Pitfall 5)
    - .planning/phases/04-reconciliation-discrepancy-flagging/04-UI-SPEC.md sections Typography (mono/tabular-nums for counts/deltas; delta neutral ink) and Copywriting (delta direction phrase)
    - lib/dashboard/reconciliation-status.ts (import computeShortSide / types)
    - supabase/migrations/0006_billing.sql, supabase/migrations/0002_verifications.sql (column names for .select())
  </read_first>
  <action>Create lib/dashboard/reconciliation-drill.ts exporting fetchReconciliationBillingDrillRows(supabase, date) that, given a YYYY-MM-DD day, computes the UTC day-range (dayStart = `${date}T00:00:00Z`, dayEnd = +24h ISO) and runs TWO whitelisted queries — billing_transactions (select event_time, transaction_id, authorised, verification_kind, region, source_file_id; .gte("event_time", dayStart).lt("event_time", dayEnd)) and verifications (select created_at, external_card_reference, duration_ms, authenticated; day-range on created_at) — each ordered desc, .limit(DRILL_ROW_LIMIT), count: "exact". Return a ReconciliationBillingDrillFetchResult with SEPARATE billingRows and verificationRows arrays plus their totalCounts and derived breakdown counts (authorisedCount/declinedCount, authenticatedCount/failedCount) computed client-side from the fetched rows (PoC scale) to satisfy the D-02 drill composition sub-note. Export the row interfaces; reuse DATA_WINDOW_START/DRILL_ROW_LIMIT by importing from verification-drill.ts. Never string-interpolate searchParams — only builder methods (T-04 tampering mitigation). Then create components/dashboard/reconciliation-billing-table.tsx as a 'use client' TanStack table over rows of shape { day_utc, billing_count, verification_count, delta, short_side, status }: columns Date, Billing (mono/tabular-nums), Verifications (mono), Delta (signed, neutral --fg-1 ink — never colour-coded), Status (render StatusBadge with the row status). Rows keyboard-operable (tabIndex, role=button, Enter/Space) calling openDrill({ drill: "recon-billing", date: row.original.day_utc.slice(0,10) }). Column defs live IN this client module (never passed from the server page — Phase-3 crash lesson 260821-mgy). Section heading "Billing vs verifications" plus a one-line summary count ("N days need review") computed via rows.filter(...).length (never a second aggregate query).</action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "recon-billing" components/dashboard/reconciliation-billing-table.tsx && grep -q "billingRows" lib/dashboard/reconciliation-drill.ts && grep -qE "\.gte\(|\.lt\(" lib/dashboard/reconciliation-drill.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - fetchReconciliationBillingDrillRows returns separate billingRows and verificationRows arrays (Pitfall 5 — never a merged flat list).
    - The fetcher uses only .gte()/.lt()/.eq()/.order()/.limit() builder calls — no string interpolation of the date into a query.
    - reconciliation-billing-table.tsx is a 'use client' module whose column defs are defined locally; it calls openDrill with drill: "recon-billing".
    - Delta cell uses neutral ink; only StatusBadge carries colour (UI-SPEC Color binding rule).
    - npx tsc --noEmit passes.
  </acceptance_criteria>
  <done>The billing drill fetcher returns explicitly-separated billing and verification rows plus authorised/authenticated breakdowns; the client billing table renders status+delta rows that drill via URL params.</done>
</task>

<task type="auto">
  <name>Task 3: Billing drill sheet + /reconciliation page (billing section) + nav</name>
  <files>components/dashboard/reconciliation-drill-sheet.tsx, app/(dashboard)/reconciliation/page.tsx, components/app-shell/sidebar-nav.tsx</files>
  <read_first>
    - app/(dashboard)/sla/page.tsx (full file — the canonical 4-state async Server Component: FreshnessBadge, PageHeader, EmptyState, ErrorState, LoadingState, good-news micro-state, Promise.all body, drill dispatch, Suspense wrapper, PageSearchParams)
    - components/dashboard/sla-breach-drill-sheet.tsx (client drill-sheet wrapper: column defs live in the 'use client' module, thin DrillSheet render)
    - components/dashboard/drill-sheet.tsx (generic DrillSheet props/behaviour)
    - components/app-shell/sidebar-nav.tsx (NAV_ITEMS array to extend)
    - lib/dashboard/reconciliation-drill.ts (from Task 2 — fetchReconciliationBillingDrillRows + row types)
    - components/dashboard/reconciliation-billing-table.tsx (from Task 2 — the section table + its row type)
    - .planning/phases/04-reconciliation-discrepancy-flagging/04-PATTERNS.md sections reconciliation-drill-sheet.tsx and page.tsx and sidebar-nav.tsx
    - .planning/phases/04-reconciliation-discrepancy-flagging/04-UI-SPEC.md sections Copywriting (all page/section/state/drill copy) and Interaction & State Contract (4-state, pending-is-first-class, drill multi-source separation)
  </read_first>
  <action>Create components/dashboard/reconciliation-drill-sheet.tsx as a 'use client' module. For the billing entity it must show BOTH sources explicitly separated (Pitfall 5): compose two labelled Table blocks inside one Sheet body ("Billing rows" / "Verification rows"), each with locally-defined function-bearing TanStack column defs, plus the composition sub-note line from UI-SPEC ("Includes {authorised} authorised / {declined} declined billing rows and {authenticated} authenticated / {failed} failed verifications for this day."). Model the open/close/Copy-link behaviour on drill-sheet.tsx (open derived from filter, closeDrill on dismiss) — do NOT modify the shared drill-sheet.tsx; build a purpose-specific Sheet composition here since this entity has two source tables. Export a ReconciliationDrillSheet component taking { filter, billingResult } for now (inventory props added in 04-03). Create app/(dashboard)/reconciliation/page.tsx cloning sla/page.tsx's 4-state structure: page heading "Reconciliation" + sub-heading per UI-SPEC; a Body async Server Component that createClient(), awaits searchParams, parseDrillParams, and Promise.all-reads v_reconciliation_billing_daily (order day_utc asc) + the ingested_files freshness row + a conditional fetchReconciliationBillingDrillRows when drillFilter.drill === "recon-billing". Error-first: if the recon or freshness result has .error, render PageHeader + ErrorState (never silent-empty — trust rule). Empty state (UI-SPEC copy "No reconciliation data yet" + Upload link) when the billing view returns zero rows AND no data (this plan checks the billing source; 04-03 will widen the empty check to all four sources). Populated: render a Card-wrapped ReconciliationBillingTable section, the FreshnessBadge ("Data as of last import: {timestamp}") and data-window caption, a good-news micro-state ("No billing/verification mismatches in this period.") when rows exist but none are mismatch, and the ReconciliationDrillSheet. Wrap Body in Suspense with a skeleton LoadingState. Finally EDIT components/app-shell/sidebar-nav.tsx: add { href: "/reconciliation", label: "Reconciliation", icon: "rotate" } to NAV_ITEMS (icon glyph already exists in the sprite; no other change).</action>
  <verify>
    <automated>npx tsc --noEmit && npm run build 2>&1 | tail -5 && grep -q "/reconciliation" components/app-shell/sidebar-nav.tsx && grep -q "v_reconciliation_billing_daily" app/\(dashboard\)/reconciliation/page.tsx && grep -q "ErrorState" app/\(dashboard\)/reconciliation/page.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - npm run build succeeds (no RSC function-passing error — column defs stay inside 'use client' modules).
    - /reconciliation route renders the Billing-vs-Verification section reading v_reconciliation_billing_daily.
    - The page implements all four states; a query .error path returns ErrorState, never a silent-empty or silent-all-OK table.
    - The billing drill Sheet shows billing rows and verification rows as two explicitly-labelled tables (not one merged list) plus the authorised/authenticated composition sub-note.
    - Sidebar shows a "Reconciliation" item linking to /reconciliation with the #rotate glyph.
  </acceptance_criteria>
  <done>Navigating to /reconciliation shows the billing-vs-verification daily flags with status+delta, correct pending-vs-mismatch behaviour, and a two-source drill Sheet; the nav item is present and the app builds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser (authenticated user) -> Next Server Component -> Supabase | Session-scoped client; RLS must remain in force via security_invoker views |
| URL query string (?drill=&date=) -> parseDrillParams -> Supabase query builder | Untrusted params must not reach a query except as whitelisted, typed fields |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Information Disclosure | v_billing_daily_counts, v_reconciliation_billing_daily | mitigate | Both views declared with (security_invoker = on) so they run as the querying user and honour billing_transactions/verifications RLS select policies; verified by grep gate in Task 1. |
| T-04-04 | Tampering | reconciliation-drill.ts / page.tsx drill dispatch | mitigate | Drill date/entity only ever read from the parseDrillParams-validated DrillFilter; fetcher uses .gte()/.lt()/.eq() builders, never string interpolation of searchParams (T-03-19 rule extended). |
| T-04-05 | Denial of Service | drill fetch of contributing rows | mitigate | DRILL_ROW_LIMIT (500) caps the fetch; totalCount surfaces truncation rather than unbounded reads. |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No new packages this phase (RESEARCH Package Legitimacy Audit: not applicable). |
</threat_model>

<verification>
- npm run build green (proves no RSC serialization crash and the new page compiles).
- npx tsc --noEmit passes.
- Migration 0018 grep gates pass (security_invoker x2, FULL OUTER JOIN, no clock, no spine).
- Manual: /reconciliation shows a billing section; most-recent day badges "Needs review"; a settled unequal day badges "Mismatch"; drilling opens a two-table Sheet. (Live-data confirmation happens after the 04-04 schema push.)
</verification>

<success_criteria>
The billing-vs-verification reconciliation slice works end to end: a UTC-bucketed FULL OUTER JOIN settling-window view feeds a 4-state /reconciliation page whose per-day rows show status + signed delta and drill to explicitly-separated billing and verification source rows, reachable from a new sidebar item.
</success_criteria>

<output>
Create `.planning/phases/04-reconciliation-discrepancy-flagging/04-02-SUMMARY.md` when done.
</output>
