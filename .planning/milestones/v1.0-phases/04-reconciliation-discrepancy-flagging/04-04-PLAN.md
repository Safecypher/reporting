---
phase: 04-reconciliation-discrepancy-flagging
plan: 04
type: execute
wave: 4
depends_on: ["04-01", "04-02", "04-03"]
files_modified:
  - types/db.ts
autonomous: false
requirements: [RECON-01, RECON-02, RECON-03, DASH-02]
user_setup:
  - service: supabase
    why: "Push migrations 0018-0020 to the linked hosted project (build/type checks pass WITHOUT the push, creating a false-positive verification state — the new reconciliation views come from generated types, not the live DB)"
    env_vars:
      - name: SUPABASE_ACCESS_TOKEN
        source: "Supabase Dashboard -> Account -> Access Tokens (needed for non-interactive `supabase db push`; project already linked via supabase/config.toml). MCP apply_migration is the fallback path in this environment."
must_haves:
  truths:
    - "v_billing_daily_counts and v_reconciliation_billing_daily exist in the linked project with security_invoker=on"
    - "v_inventory_daily_diff, v_inventory_gap_days, v_inventory_live_count, v_reconciliation_inventory_daily, v_apigee_cross_check exist with security_invoker=on"
    - "Against live data, the most-recent business day reads 'needs_review' (pending), not 'mismatch' (D-03 settling proven on real data)"
    - "types/db.ts regenerated from the live schema includes all seven new views"
    - "A production next build succeeds and the full vitest suite is green"
    - "/reconciliation renders both sections against live data with drill Sheets opening from URL params"
  artifacts:
    - path: "types/db.ts"
      provides: "Regenerated Supabase types including the seven reconciliation views"
      contains: "v_reconciliation_billing_daily"
  key_links:
    - from: "supabase/migrations/0018..0020"
      to: "linked Supabase project"
      via: "supabase db push (or MCP apply_migration fallback)"
      pattern: "v_reconciliation_billing_daily"
    - from: "app/(dashboard)/reconciliation/page.tsx"
      to: "live reconciliation views"
      via: "Server Component reads returning real rows post-push"
      pattern: "v_reconciliation_inventory_daily"
---

<objective>
Land migrations 0018-0020 in the live database, regenerate DB types, and run the authoritative end-to-end verification: confirm the seven reconciliation views exist with security_invoker, that the settling state machine behaves correctly on real data (most-recent day pending, a settled unequal day mismatch), and that /reconciliation renders both sections and drills to source. This is the mandatory schema-push gate — without it, tsc/vitest/build still pass (types come from generated config, tests use pure fixtures), giving a false-positive "done" while no reconciliation view actually exists in the DB.

Purpose: make RECON-01, RECON-02, RECON-03, and DASH-02 real in the database and prove the two state machines and the escalation-proof APIGEE cross-check work on live infrastructure — the demo centrepiece and the close of the v1 milestone.
Output: 0018-0020 pushed, types/db.ts regenerated, security_invoker + settling behaviour verified on the live DB, production build + full test suite green, phase verification recorded.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-reconciliation-discrepancy-flagging/04-RESEARCH.md
@.planning/phases/03-revenue-sla-drill-down/03-07-PLAN.md
@.planning/phases/02-complete-the-six-sources/02-07-PLAN.md
@supabase/config.toml
@supabase/migrations/0018_v_reconciliation_billing.sql
@supabase/migrations/0019_v_reconciliation_inventory.sql
@supabase/migrations/0020_v_apigee_cross_check.sql
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full test suite + type-check + production build green (pre-push regression)</name>
  <files>—</files>
  <read_first>
    - package.json (the `test` script -> vitest run; the build script)
    - lib/dashboard/__tests__/reconciliation-status.test.ts, lib/dashboard/__tests__/drill-params.test.ts (this phase's + regression suites)
  </read_first>
  <action>Run the full unit suite (`npm test`) and confirm all prior-phase regression suites plus this phase's reconciliation-status and drill-params suites pass. Run `npx tsc --noEmit` clean. Run the production build (`npm run build`) and confirm it succeeds — this specifically re-catches any RSC function-passing crash from the drill tables/sheets (the 260821-mgy class of bug) which `next dev` hides. Do NOT weaken any assertion to make it pass — a real failure is a gap to fix in the owning slice (04-01/02/03), not here.</action>
  <verify>
    <automated>npm test && npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exits 0 with the reconciliation-status suite present and passing.
    - `npx tsc --noEmit` exits 0.
    - `npm run build` exits 0 (no missing-Suspense / RSC function-passing error from the reconciliation drill consumers).
  </acceptance_criteria>
  <done>All code is unit-proven and production-buildable before touching the live DB.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: [BLOCKING] Push migrations 0018-0020 to the linked Supabase project</name>
  <read_first>
    - supabase/config.toml (project already linked)
    - supabase/migrations/0018_v_reconciliation_billing.sql, 0019_v_reconciliation_inventory.sql, 0020_v_apigee_cross_check.sql (what is being pushed)
    - .planning/phases/03-revenue-sla-drill-down/03-07-PLAN.md and .planning/phases/02-complete-the-six-sources/02-07-PLAN.md (the established push precedent: SUPABASE_ACCESS_TOKEN + `supabase db push`, MCP apply_migration fallback)
    - .planning/phases/04-reconciliation-discrepancy-flagging/04-RESEARCH.md (Environment Availability / Pitfall 3 security_invoker)
  </read_first>
  <what-built>Three migrations defining seven read-only views: 0018 (v_billing_daily_counts, v_reconciliation_billing_daily — FULL OUTER JOIN settling state machine), 0019 (v_inventory_daily_diff, v_inventory_gap_days, v_inventory_live_count, v_reconciliation_inventory_daily), 0020 (v_apigee_cross_check — amber-only, no mismatch branch).</what-built>
  <action>Attempt the non-interactive CLI push first: ensure SUPABASE_ACCESS_TOKEN is set, then run `supabase db push`. If the CLI is not on PATH or the token is unavailable / prompts cannot be suppressed, fall back to the Supabase MCP apply_migration tool, applying 0018, 0019, 0020 in order against the linked project. This task is checkpoint:human-action because `supabase db push` commonly prompts for confirmation and may require the human to provide/confirm the access token; automate as much as possible, pausing only if authentication genuinely blocks.</action>
  <how-to-verify>After the push, list views (Supabase MCP list_tables/execute_sql against information_schema, or `supabase db diff`) and confirm all seven new views exist. Run a `select viewname, definition from pg_views where viewname like 'v_reconciliation%' or viewname like 'v_inventory%' or viewname = 'v_apigee_cross_check'` and confirm each carries security_invoker (or check `reloptions` in pg_class). Run Supabase advisors (get_advisors) and confirm no new SECURITY DEFINER-style RLS-bypass warning for the reconciliation views.</how-to-verify>
  <verify>
    <human-check>0018-0020 applied: all seven views (v_billing_daily_counts, v_reconciliation_billing_daily, v_inventory_daily_diff, v_inventory_gap_days, v_inventory_live_count, v_reconciliation_inventory_daily, v_apigee_cross_check) exist in the linked project, each with security_invoker=on; no RLS-bypass advisor warning.</human-check>
  </verify>
  <acceptance_criteria>
    - All seven views present in the linked project.
    - Each view reports security_invoker=on (Pitfall 3 — no view silently bypasses RLS).
    - No security-advisor warning for RLS bypass on the new views.
  </acceptance_criteria>
  <resume-signal>Type "pushed" once all seven views are confirmed present with security_invoker, or describe the blocker.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Regenerate types + authoritative live reconciliation verification</name>
  <files>types/db.ts</files>
  <read_first>
    - types/db.ts (current generated types — to be regenerated)
    - CLAUDE.md (`supabase gen types typescript --linked > types/db.ts` convention)
    - lib/dashboard/reconciliation-status.ts (the settling truth table the live views must reproduce)
    - .planning/phases/04-reconciliation-discrepancy-flagging/04-UI-SPEC.md Interaction & State Contract (the manual UAT checklist source)
  </read_first>
  <action>(a) Regenerate DB types: `supabase gen types typescript --linked > types/db.ts` (MCP type-gen path if CLI unavailable); re-run `npx tsc --noEmit` and fix any column-mapping type errors in the owning files (do not loosen types). (b) AUTHORITATIVE VERIFICATION against the live DB (via `supabase db`/psql or Supabase MCP execute_sql): query v_reconciliation_billing_daily ordered by day_utc and confirm the row for the max day_utc has status 'needs_review' (D-03 pending — settled is false for the most-recent day) while any earlier unequal day reads 'mismatch'; query v_apigee_cross_check and confirm no row has status 'mismatch' (only 'ok'/'needs_review', D-10); query v_inventory_gap_days and confirm any date in the data window with no card_inventory snapshot is present as a missing_day (D-07). Record the observed max-day status and a sample of each view's shape in the SUMMARY. (c) Manual UAT: open /reconciliation and confirm — both sections render (4-state), the most-recent billing day badges "Needs review" calmly (not alarmed), the inventory summary strip shows live/enrolled/unenrolled, a gap day (if any) shows the distinct muted/dashed row, the APIGEE sub-table is amber-only and subordinate, and clicking a flagged day reopens the drill Sheet from URL params showing two explicitly-separated source tables. If the live settling status does not match computeReconciliationStatus for known data, STOP — the SQL case in 0018/0019 has a bug; fix in the owning slice, re-push, re-verify.</action>
  <verify>
    <automated>bash -c "grep -q 'v_reconciliation_billing_daily' types/db.ts && grep -q 'v_reconciliation_inventory_daily' types/db.ts && grep -q 'v_apigee_cross_check' types/db.ts && grep -q 'v_inventory_gap_days' types/db.ts && echo OK"</automated>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - types/db.ts contains all seven new views (billing counts, billing recon, inventory diff, gap days, live count, inventory recon, apigee cross-check).
    - Live query output (recorded in SUMMARY): most-recent day = 'needs_review'; v_apigee_cross_check has zero 'mismatch' rows; gap days surfaced.
    - `npx tsc --noEmit` exits 0.
    - Manual UAT confirms both sections, calm pending state, gap-row treatment, amber APIGEE, and two-source drill Sheets.
  </acceptance_criteria>
  <done>Types match the live schema; the settling state machine, APIGEE non-escalation, and gap surfacing are proven on real infrastructure; /reconciliation is verifiably complete — the v1 milestone closes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local migrations -> live Postgres | Schema (view) changes applied to the hosted project |
| Browser (authenticated user) -> live reconciliation views | Views must run as the querying user (security_invoker) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-09 | Tampering | False-positive "done" without the push (types from config, not live DB) | mitigate | This [BLOCKING] gate applies 0018-0020 and runs the live settling/APIGEE/gap verification as the authoritative check. |
| T-04-10 | Information Disclosure | The seven new views on the live DB | mitigate | Post-push pg_views/reloptions + advisor check confirms every reconciliation view carries security_invoker=on and honours source-table RLS (Pitfall 3). |
| T-04-11 | Repudiation | Settling verdict wrong on live data (false confidence) | mitigate | Live query asserts most-recent day is 'needs_review' and settled unequal days are 'mismatch', matching computeReconciliationStatus — a trust tool must never look clean because the check misbehaved. |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No new packages this phase (RESEARCH Package Legitimacy Audit: not applicable). |
</threat_model>

<verification>
- `npm test`, `npx tsc --noEmit`, `npm run build` all green pre-push.
- Post-push: all seven views exist with security_invoker; no RLS-bypass advisor warning.
- Live: most-recent day 'needs_review'; v_apigee_cross_check has no 'mismatch' row; gap days surfaced.
- types/db.ts regenerated and type-checks; /reconciliation renders both sections; drill links reopen the Sheets.
</verification>

<success_criteria>
- Migrations 0018-0020 live; types regenerated; production build + full test suite green.
- The billing and inventory settling state machines and the escalation-proof APIGEE cross-check are proven correct on the live DB.
- All four phase requirements (RECON-01, RECON-02, RECON-03, DASH-02) verifiably realised end-to-end — the v1 milestone is complete.
</success_criteria>

<output>
Create `.planning/phases/04-reconciliation-discrepancy-flagging/04-04-SUMMARY.md` when done (record the live settling-status output and the UAT observations).
</output>
