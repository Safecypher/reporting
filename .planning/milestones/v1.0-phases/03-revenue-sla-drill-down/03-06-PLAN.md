---
phase: 03-revenue-sla-drill-down
plan: 06
type: execute
wave: 4
depends_on: ["03-05", "03-04"]
files_modified:
  - lib/dashboard/drill-params.ts
  - lib/dashboard/__tests__/drill-params.test.ts
  - components/dashboard/drillable-metric.tsx
  - components/dashboard/drill-sheet.tsx
  - app/(dashboard)/revenue/page.tsx
  - app/(dashboard)/sla/page.tsx
  - app/(dashboard)/verifications/page.tsx
autonomous: true
requirements: [DASH-03]
must_haves:
  truths:
    - "Clicking any summary metric (verification count, revenue KPI/per-tier, SLA breach row) opens a slide-over Sheet showing the filtered raw contributing rows (D-11: all summary metrics drillable, generic pattern for Phase 4 reuse)"
    - "The Sheet's open/closed state and filter are derived entirely from URL query params (?drill=...&date=...), not component state — a shared link or refresh reopens the same drilled view"
    - "Opening/closing uses router.replace (not push) so browser history is not polluted; useSearchParams consumers are wrapped in Suspense (D-10)"
    - "Server-side row fetch whitelists the drill entity + filter keys and uses parameterised .eq()/.gte() — never raw string interpolation"
  artifacts:
    - path: "lib/dashboard/drill-params.ts"
      provides: "pure parse/serialize of the drill URL contract (whitelisted entities/keys) — shared by Phase 4"
      exports: ["parseDrillParams", "serializeDrillParams", "DrillEntity", "DrillFilter"]
    - path: "components/dashboard/drill-sheet.tsx"
      provides: "generic Sheet + TanStack table over server-fetched filtered rows"
      contains: "Sheet"
    - path: "components/dashboard/drillable-metric.tsx"
      provides: "useDrill hook (router.replace URL sync)"
      contains: "useSearchParams"
  key_links:
    - from: "components/dashboard/drillable-metric.tsx"
      to: "URL query params"
      via: "router.replace with URLSearchParams (not push)"
      pattern: "router.replace"
    - from: "app/(dashboard)/revenue/page.tsx"
      to: "drill row fetch"
      via: "parseDrillParams(searchParams) → whitelisted supabase query → DrillSheet"
      pattern: "parseDrillParams"
---

<objective>
Build the generic, URL-synced drill-down Sheet once and wire it into every summary metric across Revenue, SLA, and Verifications. Filter state lives entirely in the URL (shareable/refreshable), open/close uses replace-state, and the server-side row fetch is strictly whitelisted and parameterised.

Purpose: DASH-03 — drill from any summary metric to the filtered contributing raw records. This is deliberately built as reusable infrastructure that Phase 4 will extend to reconciliation discrepancy flags without touching the Sheet component (D-11).
Output: drill-params + drillable-metric + drill-sheet + wiring on the three view pages.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-revenue-sla-drill-down/03-RESEARCH.md
@.planning/phases/03-revenue-sla-drill-down/03-PATTERNS.md
@.planning/phases/03-revenue-sla-drill-down/03-UI-SPEC.md
@app/(dashboard)/verifications/page.tsx
@app/(dashboard)/revenue/page.tsx
@app/(dashboard)/sla/page.tsx
@components/ui/sheet.tsx
@components/ui/table.tsx
@lib/dashboard/bucketing.ts
@lib/supabase/server.ts

<interfaces>
Drill URL contract this plan defines (whitelisted; Phase 4 adds "discrepancy"):
  export type DrillEntity = "verification" | "revenue-tier" | "sla-breach";
  export interface DrillFilter { drill: DrillEntity; date?: string; authenticated?: boolean; tierOrder?: number }
  export function parseDrillParams(params: Record<string, string | string[] | undefined>): DrillFilter | null
  export function serializeDrillParams(filter: DrillFilter): Record<string, string>

Next 16 page prop (must await): searchParams: Promise<{ [key: string]: string | string[] | undefined }>
useDrill() (client): { openDrill(filter: Record<string,string>): void; closeDrill(): void } — uses router.replace, scroll:false

TanStack Table v8 already installed (^8.21.3) — reuse the flexRender/getCoreRowModel pattern from the SLA breach table (03-04). sonner already installed for the "Copy link" toast.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: drill-params pure contract + tests (whitelist entities/keys)</name>
  <files>lib/dashboard/drill-params.ts, lib/dashboard/__tests__/drill-params.test.ts</files>
  <behavior>
    - parseDrillParams returns null when no `drill` key is present
    - parseDrillParams returns null when `drill` is an unrecognised entity (only verification|revenue-tier|sla-breach accepted)
    - parseDrillParams("drill=verification&date=2026-08-14&authenticated=false") → { drill:"verification", date:"2026-08-14", authenticated:false }
    - Unknown filter keys are ignored (not passed through) — only whitelisted keys survive
    - serializeDrillParams round-trips a DrillFilter back to string params
  </behavior>
  <read_first>
    - lib/dashboard/bucketing.ts (module shape to mirror: pure, no I/O, typed, unit-testable)
    - .planning/phases/03-revenue-sla-drill-down/03-PATTERNS.md (drill-params shape; security note — whitelist keys, never pass raw searchParams to a query builder)
    - .planning/phases/03-revenue-sla-drill-down/03-RESEARCH.md (Security Domain V4/Tampering row — whitelist entity + filter keys)
  </read_first>
  <action>
    Create lib/dashboard/drill-params.ts with DrillEntity + DrillFilter and pure parseDrillParams / serializeDrillParams. parseDrillParams validates `drill` against the exact allowed set, coerces `date` (keep as ISO string), `authenticated` ("true"/"false" → boolean), `tierOrder` (integer) — and DROPS any key not in the whitelist. Return null on missing/invalid entity. No DOM/network access. Then write lib/dashboard/__tests__/drill-params.test.ts covering every <behavior> case.
  </action>
  <verify>
    <automated>npx vitest run lib/dashboard/__tests__/drill-params.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test command: `npx vitest run lib/dashboard/__tests__/drill-params.test.ts` exits 0 with all <behavior> cases
    - Source assertion: only "verification", "revenue-tier", "sla-breach" are accepted entities; unknown keys are dropped
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>The shared, whitelisted drill URL contract is proven pure and safe against arbitrary-key tampering.</done>
</task>

<task type="auto">
  <name>Task 2: useDrill hook + generic DrillSheet (URL-driven, replace-state, Suspense)</name>
  <files>components/dashboard/drillable-metric.tsx, components/dashboard/drill-sheet.tsx</files>
  <read_first>
    - components/ui/sheet.tsx (Sheet/SheetContent/SheetHeader/SheetTitle exports to compose; side="right", --shadow-md intentional)
    - components/ui/table.tsx (shadcn primitives to wrap TanStack output)
    - components/dashboard/sla-breach-table.tsx (the first TanStack v8 usage to mirror: useReactTable + flexRender + getCoreRowModel/getSortedRowModel)
    - .planning/phases/03-revenue-sla-drill-down/03-RESEARCH.md (Pattern 1 useDrill verbatim; Pitfall 3 — useSearchParams needs Suspense or next build fails)
    - .planning/phases/03-revenue-sla-drill-down/03-UI-SPEC.md (Sheet header states the filter; Copy link → sonner "Link copied"; empty "No records match this selection."; Sheet title format "{metric label} — {date}, {filter}")
  </read_first>
  <action>
    (a) Create components/dashboard/drillable-metric.tsx ("use client") exporting useDrill(): uses useRouter/usePathname/useSearchParams; openDrill(filter) sets params via URLSearchParams and router.replace(pathname?params, {scroll:false}) — REPLACE not push (D-10); closeDrill() deletes all drill keys and router.replace back to the clean URL. Also export a small DrillableMetric wrapper (button/clickable with brand --focus-ring, keyboard-operable) that calls openDrill on click. Wrap any component that calls useSearchParams in Suspense at the usage site (Pitfall 3). (b) Create components/dashboard/drill-sheet.tsx ("use client"): a generic Sheet whose open state = !!filter (derived from props, no internal useState); renders SheetHeader with the filter description title, a "Copy link" button (copies window.location.href, sonner toast "Link copied"), and a TanStack Table v8 over the server-fetched `rows` prop (columns supplied per entity via a columns prop); empty renders "No records match this selection."; close via useDrill().closeDrill(). Rows are fetched server-side by the pages (Task 3) and passed as props — the Sheet does not query.
  </action>
  <verify>
    <automated>bash -c "grep -q 'router.replace' components/dashboard/drillable-metric.tsx && ! grep -q 'router.push' components/dashboard/drillable-metric.tsx && grep -q 'useSearchParams' components/dashboard/drillable-metric.tsx && grep -q 'Sheet' components/dashboard/drill-sheet.tsx && grep -q 'No records match this selection' components/dashboard/drill-sheet.tsx && grep -q 'useReactTable' components/dashboard/drill-sheet.tsx && echo OK"</automated>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: useDrill uses router.replace and NOT router.push
    - Source assertion: DrillSheet open state is prop/URL-derived (no internal useState open) and shows "No records match this selection." when empty
    - Source assertion: Copy link uses sonner; Sheet uses TanStack table via useReactTable
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>A single generic, URL-driven Sheet renders filtered rows with replace-state semantics and a shareable link.</done>
</task>

<task type="auto">
  <name>Task 3: Wire drill-down into Revenue, SLA, and Verifications pages (whitelisted server fetch)</name>
  <files>app/(dashboard)/revenue/page.tsx, app/(dashboard)/sla/page.tsx, app/(dashboard)/verifications/page.tsx</files>
  <read_first>
    - lib/dashboard/drill-params.ts (parseDrillParams — whitelist before any query)
    - components/dashboard/drill-sheet.tsx (DrillSheet props: filter, rows, columns)
    - components/dashboard/drillable-metric.tsx (DrillableMetric wrapper / useDrill for clickable metrics)
    - app/(dashboard)/verifications/page.tsx + revenue/page.tsx + sla/page.tsx (add the searchParams prop; make KPI/tier/breach rows clickable)
    - .planning/phases/03-revenue-sla-drill-down/03-RESEARCH.md (Server Component reads awaited searchParams; whitelisted parameterised .eq()/.gte() only; Pitfall 3 Suspense)
    - .planning/phases/03-revenue-sla-drill-down/03-UI-SPEC.md (every summary metric drillable; Sheet always labelled)
  </read_first>
  <action>
    Add `searchParams: Promise<...>` to each page's props and `const params = await searchParams`. On each page: parseDrillParams(params); if a valid DrillFilter is present, fetch the contributing raw rows server-side via the session createClient using ONLY whitelisted parameterised filters (verification → verifications filtered by .eq('authenticated', ...) and .gte/.lt on created_at for the date; revenue-tier → verifications/tier rows for that day/tier; sla-breach → v_sla_breaches filtered by date). Never interpolate raw searchParams into a query string — use .eq()/.gte()/.lt() builders. Render &lt;DrillSheet filter={...} rows={...} columns={...} /&gt; (wrapped in Suspense where a client useSearchParams consumer is involved). Make the metrics clickable: revenue Total KPI + each Revenue-by-tier row, SLA breach rows, and the verifications counts — wrap them with DrillableMetric/openDrill setting the appropriate drill params. Keep focus rings + keyboard operability. Do not change the existing 4-state logic — add the drill layer alongside it.
  </action>
  <verify>
    <automated>bash -c "for f in 'app/(dashboard)/revenue/page.tsx' 'app/(dashboard)/sla/page.tsx' 'app/(dashboard)/verifications/page.tsx'; do grep -q 'parseDrillParams' \"$f\" && grep -q 'searchParams' \"$f\" || { echo FAIL \"$f\"; exit 1; }; done; echo OK"</automated>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: all three pages await searchParams and call parseDrillParams before any drill fetch
    - Source assertion: drill fetches use parameterised .eq()/.gte()/.lt() (no raw string interpolation of searchParams into queries)
    - Source assertion: each page renders DrillSheet and exposes clickable metrics (revenue KPI + tier rows, SLA breaches, verification counts)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Every summary metric across the three views drills to its whitelisted, parameterised, filtered raw rows in a shareable Sheet.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL searchParams → server query | Untrusted query params drive server-side row filters |
| client click → URL | Drill state written to the shareable URL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-19 | Tampering | Arbitrary searchParams driving DB filters / SQL injection | mitigate | parseDrillParams whitelists entity + keys; queries use parameterised supabase-js .eq()/.gte()/.lt() only, never string concatenation |
| T-03-20 | Information Disclosure | Drill query returns rows outside the user's RLS scope | mitigate | Fetch uses the session-scoped createClient; RLS + security_invoker views apply to every drill query |
| T-03-21 | Tampering | Missing Suspense boundary breaks production build (hidden data-integrity/availability gap) | mitigate | Wrap useSearchParams consumers in Suspense (Pitfall 3); router.replace not push |
</threat_model>

<verification>
- `npx vitest run lib/dashboard/__tests__/drill-params.test.ts` green
- Grep gates confirm replace-not-push, whitelisted parse before fetch on all three pages, Sheet URL-derived open, sonner copy-link, TanStack table
- `npx tsc --noEmit` clean; a production `next build` (run in 03-07) confirms no missing-Suspense failure
</verification>

<success_criteria>
- A generic URL-synced Sheet drills every summary metric on Revenue, SLA, and Verifications to its filtered raw rows
- State is 100% URL-derived (shareable/refreshable), replace-state, Suspense-safe
- Server fetch is whitelisted + parameterised; RLS applies
</success_criteria>

<output>
Create `.planning/phases/03-revenue-sla-drill-down/03-06-SUMMARY.md` when done
</output>
