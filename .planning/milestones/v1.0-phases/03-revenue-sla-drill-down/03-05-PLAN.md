---
phase: 03-revenue-sla-drill-down
plan: 05
type: execute
wave: 3
depends_on: ["03-03", "03-04"]
files_modified:
  - lib/dashboard/revenue-bucketing.ts
  - lib/dashboard/__tests__/revenue-bucketing.test.ts
  - components/dashboard/revenue-chart.tsx
  - components/dashboard/revenue-kpi-cards.tsx
  - components/dashboard/revenue-tier-breakdown.tsx
  - components/dashboard/revenue-view-controls.tsx
  - app/(dashboard)/revenue/page.tsx
autonomous: true
requirements: [REV-01]
must_haves:
  truths:
    - "The revenue view shows a headline Total revenue KPI (USD — D-03, .metric treatment), a revenue-over-time chart, and a per-tier breakdown (D-07)"
    - "All money is formatted once at the display boundary with Intl.NumberFormat USD; the NUMERIC-as-string totals from the views are never re-summed as floats in JS"
    - "The revenue view reuses the granularity + timezone toggle UX (D-09) and the freshness/4-state contract"
    - "No pricing tiers configured for the period renders an ERROR state, never a silent $0"
  artifacts:
    - path: "app/(dashboard)/revenue/page.tsx"
      provides: "revenue Server Component (4-state; no-tiers = error)"
      contains: "v_revenue_daily"
    - path: "components/dashboard/revenue-kpi-cards.tsx"
      provides: "Total revenue .metric card with USD formatter"
      contains: "Intl.NumberFormat"
    - path: "components/dashboard/revenue-tier-breakdown.tsx"
      provides: "per-tier revenue breakdown (D-07)"
  key_links:
    - from: "app/(dashboard)/revenue/page.tsx"
      to: "v_revenue_daily / v_revenue_by_tier"
      via: "session-scoped supabase.from().select()"
      pattern: "v_revenue_by_tier"
    - from: "components/dashboard/revenue-kpi-cards.tsx"
      to: "USD display"
      via: "Intl.NumberFormat currency, rounded once at display"
      pattern: "Intl.NumberFormat"
---

<objective>
Deliver the revenue view page: reuse the granularity/timezone controls, render a headline Total revenue KPI (USD), a revenue-over-time chart, and a per-tier breakdown — all formatting money exactly once at the display boundary over the exact-NUMERIC totals produced by the 03-03 views, with a hard rule that a missing tier configuration is an error, not a silent zero.

Purpose: REV-01 — view tiered revenue over the billing period. Consumes the marginal-bracket views from 03-03 and the pricing config from 03-01/03-02.
Output: /revenue route. (Live data appears after the 03-07 push; page verifiable via type-check + structural gates now.)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-revenue-sla-drill-down/03-RESEARCH.md
@.planning/phases/03-revenue-sla-drill-down/03-PATTERNS.md
@.planning/phases/03-revenue-sla-drill-down/03-UI-SPEC.md
@app/(dashboard)/verifications/page.tsx
@components/dashboard/verifications-chart.tsx
@components/dashboard/view-controls.tsx
@components/dashboard/kpi-cards.tsx
@lib/dashboard/bucketing.ts

<interfaces>
From lib/dashboard/bucketing.ts (bucketKeyAndLabel + DATA_WINDOW_START were exported by plan 03-04 — reuse, do NOT duplicate):
  bucketKeyAndLabel(dayUtc, granularity, timeZone); DATA_WINDOW_START; type Granularity, BucketTimeZone

Revenue view rows (from 03-03 views; supabase-js returns numeric as STRING):
  v_revenue_daily: { day_utc: string; revenue: string }
  v_revenue_by_tier: { day_utc: string; tier_order: number; tier_revenue: string; tier_set_id: string }

New shapes this plan defines:
  export type RevenueDailyRow = { day_utc: string; revenue: string }
  export interface RevenueBucketPoint { bucketKey: string; label: string; revenue: number }

Currency rule (binding, UI-SPEC): Intl.NumberFormat('en-US', { style: 'currency', currency: 'USD' }); format ONCE at display; never re-sum formatted strings. Prefer the grand total already summed by the SQL view (Pitfall 2).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Revenue re-bucketing + revenue chart + Total revenue KPI</name>
  <files>lib/dashboard/revenue-bucketing.ts, lib/dashboard/__tests__/revenue-bucketing.test.ts, components/dashboard/revenue-chart.tsx, components/dashboard/revenue-kpi-cards.tsx</files>
  <read_first>
    - lib/dashboard/bucketing.ts (import the exported bucketKeyAndLabel + DATA_WINDOW_START; do NOT re-implement)
    - components/dashboard/verifications-chart.tsx (ChartContainer/chartConfig wiring to mirror; single-series delta)
    - components/dashboard/kpi-cards.tsx (the .metric MetricCard treatment; swap formatCount for a currency formatter on the Total revenue card only)
    - .planning/phases/03-revenue-sla-drill-down/03-RESEARCH.md (Pitfall 2 — numeric-as-string; sum in SQL, format once; §Currency display rule)
    - .planning/phases/03-revenue-sla-drill-down/03-UI-SPEC.md (Color: single Cypher-Blue series; Typography: KPI keeps 48px Poppins-700, money in tables is mono/tabular)
  </read_first>
  <action>
    (a) Create lib/dashboard/revenue-bucketing.ts: RevenueDailyRow/RevenueBucketPoint types and rebucketRevenue(rows, granularity, timeZone) reusing the exported bucketKeyAndLabel; convert the revenue string to a number only at the final aggregation step and document that the authoritative grand total should come from the SQL view (this client re-bucket is for the chart series only, tiny volumes). Drop rows before DATA_WINDOW_START. (b) Write lib/dashboard/__tests__/revenue-bucketing.test.ts (daily passthrough, weekly/monthly grouping, revenue summation). (c) Create components/dashboard/revenue-chart.tsx ("use client"): mirror verifications-chart but a single series keyed revenue, Cypher-Blue, tooltip formatter using Intl.NumberFormat USD. (d) Create components/dashboard/revenue-kpi-cards.tsx ("use client" or presentational): the Total revenue .metric card reusing the MetricCard shape but with a formatCurrency helper (Intl.NumberFormat en-US currency USD) — takes the pre-summed total as a prop (do not re-sum floats).
  </action>
  <verify>
    <automated>bash -c "grep -q 'rebucketRevenue' lib/dashboard/revenue-bucketing.ts && grep -q 'bucketKeyAndLabel' lib/dashboard/revenue-bucketing.ts && grep -q 'Intl.NumberFormat' components/dashboard/revenue-chart.tsx && grep -q 'Intl.NumberFormat' components/dashboard/revenue-kpi-cards.tsx && echo OK"</automated>
    <automated>npx vitest run lib/dashboard/__tests__/revenue-bucketing.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: revenue-bucketing imports (not duplicates) bucketKeyAndLabel
    - Source assertion: both revenue chart and KPI use Intl.NumberFormat currency USD
    - Test command: `npx vitest run lib/dashboard/__tests__/revenue-bucketing.test.ts` exits 0
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Revenue re-buckets for the chart and the Total revenue KPI renders USD via a single display-boundary formatter.</done>
</task>

<task type="auto">
  <name>Task 2: Tier breakdown + revenue view-controls wrapper</name>
  <files>components/dashboard/revenue-tier-breakdown.tsx, components/dashboard/revenue-view-controls.tsx</files>
  <read_first>
    - components/dashboard/view-controls.tsx (toggle scaffold + session-only useState to mirror; D-09)
    - components/dashboard/revenue-chart.tsx (rendered by the controls wrapper)
    - components/dashboard/revenue-kpi-cards.tsx (rendered by the controls wrapper)
    - .planning/phases/03-revenue-sla-drill-down/03-UI-SPEC.md (D-07 tier breakdown: table or stacked bar; single-hue Cypher-Blue tint ramp for tier order — tiers are ordinal; money mono/tabular; heading "Revenue by tier")
    - .planning/phases/03-revenue-sla-drill-down/03-PATTERNS.md (tier-breakdown is separate from the chart, not a modification)
  </read_first>
  <action>
    (a) Create components/dashboard/revenue-tier-breakdown.tsx ("use client"): render the per-tier revenue (from v_revenue_by_tier aggregated across the period) as a table (heading "Revenue by tier"), tier amounts mono/tabular, formatted with Intl.NumberFormat USD; if a stacked bar is used instead, colour tiers with a single-hue Cypher-Blue tint ramp (blue-tint lightest → --cypher-blue darkest), never arbitrary hues. (b) Create components/dashboard/revenue-view-controls.tsx ("use client") mirroring ViewControls (granularity + timezone ToggleGroups, session-only useState, UTC default) but calling rebucketRevenue and rendering RevenueChart + RevenueKpiCards (Total revenue) + RevenueTierBreakdown. Pass the pre-summed grand total (from the view) to the KPI, not a client float re-sum.
  </action>
  <verify>
    <automated>bash -c "grep -q 'Revenue by tier' components/dashboard/revenue-tier-breakdown.tsx && grep -q 'rebucketRevenue' components/dashboard/revenue-view-controls.tsx && grep -q 'RevenueChart\\|revenue-chart' components/dashboard/revenue-view-controls.tsx && echo OK"</automated>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: tier-breakdown contains heading "Revenue by tier" and formats money with Intl.NumberFormat
    - Source assertion: revenue-view-controls uses rebucketRevenue and composes chart + KPI + breakdown with reused toggles
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>The per-tier breakdown and the composed revenue controls (chart + KPI + breakdown + toggles) render together.</done>
</task>

<task type="auto">
  <name>Task 3: Revenue page (4-state; no-tiers = error, never silent $0)</name>
  <files>app/(dashboard)/revenue/page.tsx</files>
  <read_first>
    - app/(dashboard)/verifications/page.tsx (4-state shell to copy: PageHeader/FreshnessBadge/EmptyState/ErrorState/LoadingState + Suspense body + Promise.all read)
    - components/dashboard/revenue-view-controls.tsx (rendered when data present)
    - .planning/phases/03-revenue-sla-drill-down/03-UI-SPEC.md (Copywriting: revenue headings/KPI label "Total revenue"/empty/error copy; the binding rule that "no pricing tiers configured" = ErrorState not $0)
    - .planning/phases/03-revenue-sla-drill-down/03-PATTERNS.md (revenue page pattern; revenue-specific error branch)
  </read_first>
  <action>
    Create app/(dashboard)/revenue/page.tsx as a Server Component mirroring verifications/page.tsx (heading "Revenue", DATA_WINDOW_CAPTION reused, copy from UI-SPEC). Async body reads v_revenue_daily (order day_utc asc), v_revenue_by_tier, the grand-total revenue (sum from the view or a dedicated total query — do NOT re-sum in JS), and the ingested_files freshness row via one Promise.all on the session createClient. Branching: any query error → ErrorState. If there is verification data but NO pricing tier set covers the period (query pricing_tier_sets / detect empty revenue attributable to missing config) → ErrorState with the UI-SPEC revenue error copy ("...check the pricing tiers are configured correctly."), NEVER a silent $0. If there is no verification data at all → EmptyState (upload link). Otherwise render RevenueViewControls with the daily rows, tier breakdown rows, and pre-summed total.
  </action>
  <verify>
    <automated>bash -c "grep -q 'v_revenue_daily' 'app/(dashboard)/revenue/page.tsx' && grep -q 'v_revenue_by_tier' 'app/(dashboard)/revenue/page.tsx' && grep -q 'pricing_tier_sets' 'app/(dashboard)/revenue/page.tsx' && grep -qi 'Total revenue' 'app/(dashboard)/revenue/page.tsx' && echo OK"</automated>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: page reads v_revenue_daily, v_revenue_by_tier, and checks pricing_tier_sets for the no-tiers error branch
    - Source assertion: "no pricing tiers configured" path renders ErrorState (revenue error copy), not $0
    - Source assertion: grand total is not re-summed in JS (uses SQL total)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>The revenue page renders KPI + chart + tier breakdown with the reused controls and correctly treats missing pricing config as an error rather than a misleading zero.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| session client → revenue views | Reads inherit verifications + pricing RLS via security_invoker |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-16 | Tampering | Float re-sum in JS reintroduces rounding error | mitigate | Grand total summed in SQL; JS formats a single already-summed value once (Pitfall 2; grep gate) |
| T-03-17 | Information Disclosure | Silent $0 presented as a real answer when tiers unconfigured | mitigate | No-tiers path is an explicit ErrorState with actionable copy (UI-SPEC binding rule) |
| T-03-18 | Information Disclosure | Revenue view leaks rows past RLS | mitigate | security_invoker views (03-03) + session-scoped read client |
</threat_model>

<verification>
- Grep gates confirm views read, no-tiers error branch (pricing_tier_sets), Intl.NumberFormat used in chart + KPI + breakdown, rebucketRevenue reuses shared helper
- `npx vitest run lib/dashboard/__tests__/revenue-bucketing.test.ts` green
- `npx tsc --noEmit` clean
- Live revenue numbers validated in 03-07 (boundary test + spot check)
</verification>

<success_criteria>
- /revenue shows Total revenue KPI (USD), revenue-over-time chart, and per-tier breakdown, reusing the granularity/timezone toggle + freshness + 4-state
- Money formatted once at display over exact-NUMERIC totals; no float re-sum
- Missing pricing config surfaces as an error, never a silent zero
</success_criteria>

<output>
Create `.planning/phases/03-revenue-sla-drill-down/03-05-SUMMARY.md` when done
</output>
