---
phase: 03-revenue-sla-drill-down
plan: 07
type: execute
wave: 5
depends_on: ["03-01", "03-02", "03-03", "03-04", "03-05", "03-06"]
files_modified:
  - types/db.ts
autonomous: false
requirements: [REV-01, REV-02, ADMIN-01, DATA-03, SLA-01, DASH-03]
user_setup:
  - service: supabase
    why: "Push migrations 0011-0013 to the linked hosted project (build/type checks pass WITHOUT the push, creating a false-positive verification state — views/tables come from generated types, not the live DB)"
    env_vars:
      - name: SUPABASE_ACCESS_TOKEN
        source: "Supabase Dashboard -> Account -> Access Tokens (needed for non-interactive `supabase db push`; project already linked via supabase/config.toml, project_id gditxlxfdwlvnyhhxybf)"
must_haves:
  truths:
    - "pricing_tier_sets, pricing_tiers, pricing_tier_audit exist in the linked project with the first authenticated-INSERT RLS policy, the audit table client-immutable, and the audit trigger attributing auth.uid()"
    - "v_revenue_daily_counts, v_revenue_tier_set_by_day, v_revenue_daily, v_revenue_by_tier, v_sla_daily, v_sla_breaches exist with security_invoker=on"
    - "The boundary hand-calc test runs against the live DB and returns EXACTLY 215.0000 (day total) / 80.0000 / 135.0000 (per tier)"
    - "types/db.ts regenerated from the live schema includes the three new tables and the new views"
    - "A production next build succeeds (no missing-Suspense failure from the drill-down useSearchParams consumers)"
  artifacts:
    - path: "types/db.ts"
      provides: "Regenerated Supabase types incl. pricing tables + revenue/SLA views"
      contains: "pricing_tier_sets"
  key_links:
    - from: "supabase/migrations/0011..0013"
      to: "linked Supabase project"
      via: "supabase db push (or MCP apply_migration fallback)"
      pattern: "pricing_tier_sets"
    - from: "supabase/tests/revenue_boundary_test.sql"
      to: "live v_revenue_daily"
      via: "executed against the pushed DB, asserting 215.0000"
      pattern: "215"
---

<objective>
Land migrations 0011-0013 in the live database, regenerate DB types, and run the authoritative correctness gate: execute the revenue boundary hand-calc test against the real Postgres and confirm it returns exactly $215.00 (80.00 + 135.00). This is the mandatory schema-push gate — without it, tsc/vitest still pass (types come from generated config, tests use fixtures), giving a false-positive "done" while no new table or view actually exists.

Purpose: make the revenue (REV-01/DATA-03), pricing admin (ADMIN-01/REV-02), SLA (SLA-01), and drill-down (DASH-03) features real in the database, and prove the money math is correct to the cent on live infrastructure.
Output: 0011-0013 pushed, types/db.ts regenerated, boundary test green on the live DB, production build green, phase verification recorded.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-revenue-sla-drill-down/03-RESEARCH.md
@.planning/phases/02-complete-the-six-sources/02-07-PLAN.md
@supabase/config.toml
@supabase/migrations/0011_pricing_tiers.sql
@supabase/migrations/0012_v_revenue.sql
@supabase/migrations/0013_v_sla_daily.sql
@supabase/tests/revenue_boundary_test.sql
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full test suite + type-check + production build green (pre-push regression)</name>
  <read_first>
    - package.json (the `test` script → vitest run; the build script)
    - lib/pricing/__tests__/schema.test.ts, lib/dashboard/__tests__/sla-bucketing.test.ts, lib/dashboard/__tests__/revenue-bucketing.test.ts, lib/dashboard/__tests__/drill-params.test.ts (this phase's unit suites must all pass)
  </read_first>
  <files>—</files>
  <action>
    Run the full unit suite (`npm test`) and confirm all Phase 1/2 regression suites plus this phase's new suites (pricing schema, sla-bucketing, revenue-bucketing, drill-params) pass. Run `npx tsc --noEmit` clean. Run the production build (`npm run build`) and confirm it succeeds — this specifically catches the Pitfall 3 "Missing Suspense boundary with useSearchParams" failure from the drill-down consumers (03-06), which `next dev` hides. Do NOT weaken any assertion to make it pass — a real failure is a gap to fix in the owning slice.
  </action>
  <verify>
    <automated>npm test && npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exits 0 with the four new suites present and passing
    - `npx tsc --noEmit` exits 0
    - `npm run build` exits 0 (no missing-Suspense error from drill-down)
  </acceptance_criteria>
  <done>All code is unit-proven and production-buildable before touching the live DB.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: [BLOCKING] Push migrations 0011-0013 to the linked Supabase project</name>
  <read_first>
    - supabase/config.toml (project already linked; project_id gditxlxfdwlvnyhhxybf)
    - supabase/migrations/0011_pricing_tiers.sql, 0012_v_revenue.sql, 0013_v_sla_daily.sql (what is being pushed)
    - .planning/phases/02-complete-the-six-sources/02-07-PLAN.md (the established push precedent: SUPABASE_ACCESS_TOKEN + `supabase db push`, MCP apply_migration fallback)
    - .planning/phases/03-revenue-sla-drill-down/03-RESEARCH.md (Environment Availability — CLI/token may be absent; MCP fallback)
  </read_first>
  <what-built>Three migrations: 0011 (pricing_tier_sets/pricing_tiers/pricing_tier_audit + first authenticated-INSERT RLS + SECURITY DEFINER audit trigger), 0012 (revenue view chain with marginal-bracket math), 0013 (SLA views).</what-built>
  <action>
    Attempt the non-interactive CLI push first: ensure SUPABASE_ACCESS_TOKEN is set, then run `supabase db push`. If the CLI is not on PATH or the token is unavailable/prompts cannot be suppressed, fall back to the Supabase MCP apply_migration tool, applying 0011, 0012, 0013 in order against the linked project. This task is checkpoint:human-action because `supabase db push` commonly prompts for confirmation and may require the human to provide/confirm the access token; automate as much as possible, pausing only if authentication genuinely blocks.
  </action>
  <how-to-verify>
    After the push, list tables (Supabase MCP list_tables or `supabase db diff`) and confirm the three new tables + six new views exist. Run Supabase advisors (get_advisors) and confirm: no new table is flagged as missing RLS; specifically confirm pricing_tier_sets/pricing_tiers have BOTH select and insert policies for authenticated, and pricing_tier_audit has select-only (no client insert/update/delete).
  </how-to-verify>
  <verify>
    <human-check>0011-0013 applied: three pricing tables (with the first authenticated-INSERT RLS + SECURITY DEFINER audit trigger + audit table client-immutable) and all six views (security_invoker) exist in the linked project; no RLS advisor warnings.</human-check>
  </verify>
  <acceptance_criteria>
    - pricing_tier_sets, pricing_tiers, pricing_tier_audit present with correct RLS (insert-authenticated on the two tier tables; audit select-only)
    - v_revenue_daily_counts, v_revenue_tier_set_by_day, v_revenue_daily, v_revenue_by_tier, v_sla_daily, v_sla_breaches present with security_invoker
    - No security-advisor warning for missing RLS on the new tables
  </acceptance_criteria>
  <resume-signal>Type "pushed" once all tables, views, RLS, and the audit trigger are confirmed in the project, or describe the blocker.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Regenerate types + authoritative live boundary correctness test + phase spot-checks</name>
  <read_first>
    - types/db.ts (current generated types — to be regenerated)
    - CLAUDE.md (`supabase gen types typescript --linked > types/db.ts` convention)
    - supabase/tests/revenue_boundary_test.sql (the boundary assertion to run against the live DB)
  </read_first>
  <files>types/db.ts</files>
  <action>
    (a) Regenerate DB types: `supabase gen types typescript --linked > types/db.ts` (MCP type-gen path if CLI unavailable); re-run `npx tsc --noEmit` and fix any column-mapping type errors in the owning files (do not loosen types). (b) AUTHORITATIVE CORRECTNESS GATE: execute supabase/tests/revenue_boundary_test.sql against the live DB (via `supabase db` / psql, or Supabase MCP execute_sql running the same seed+assert inside a rolled-back transaction). Confirm v_revenue_daily returns EXACTLY 215.0000 for the boundary-crossing day and v_revenue_by_tier returns 80.0000 (tier 0) and 135.0000 (tier 1). If it does not match to the cent, STOP — the marginal-bracket SQL in 03-03 has a bug; fix there, re-push, re-run. (c) Spot-check: query v_sla_daily/v_sla_breaches return rows/shape as expected; open /revenue, /sla, /settings/pricing and confirm they render (4-state); confirm a drill link (e.g. ?drill=sla-breach&date=...) reopens the Sheet.
  </action>
  <verify>
    <automated>bash -c "grep -q 'pricing_tier_sets' types/db.ts && grep -q 'v_revenue_by_tier' types/db.ts && grep -q 'v_sla_daily' types/db.ts && echo OK"</automated>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - types/db.ts contains pricing_tier_sets, pricing_tiers, pricing_tier_audit, and the new views (v_revenue_by_tier, v_sla_daily)
    - CLI/SQL output: the live boundary test returns exactly 215.0000 / 80.0000 / 135.0000 (recorded in the SUMMARY)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Types match the live schema, and the revenue math is proven correct to the cent at a tier boundary on real infrastructure — the phase is verifiably complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local migrations → live Postgres | Schema changes applied to the hosted project |
| package installs (this phase) | react-hook-form / @hookform/resolvers / @tanstack/react-table already in package.json |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-22 | Tampering | False-positive "done" without the push (types from config, not live DB) | mitigate | This [BLOCKING] gate applies migrations and runs the live boundary test as the authoritative correctness check |
| T-03-23 | Repudiation | Audit trigger not actually enforcing auth.uid() on the live DB | mitigate | Post-push advisor + policy check confirms audit table is client-immutable and the SECURITY DEFINER trigger exists |
| T-03-SC | Tampering | Supply-chain: package-manager installs pulling a wrong/malicious version | mitigate | RESEARCH Package Legitimacy Audit — all three new packages [OK] (slopcheck, no postinstall). Do NOT run a bare `npm install @tanstack/react-table` (resolves to v9); pins are already fixed in package.json (^8.21.3). No install task in this phase, so no legitimacy checkpoint required. |
</threat_model>

<verification>
- `npm test`, `npx tsc --noEmit`, `npm run build` all green pre-push
- Post-push: three tables + six views exist with correct RLS/security_invoker; no RLS advisor warnings
- Live revenue_boundary_test.sql returns 215.0000 / 80.0000 / 135.0000 exactly
- types/db.ts regenerated and type-checks; /revenue, /sla, /settings/pricing render; a drill link reopens the Sheet
</verification>

<success_criteria>
- Migrations 0011-0013 live; types regenerated; production build green
- Marginal-bracket revenue proven correct to the cent at a tier boundary on the live DB
- All six phase requirements (REV-01, REV-02, ADMIN-01, DATA-03, SLA-01, DASH-03) verifiably realised end-to-end
</success_criteria>

<output>
Create `.planning/phases/03-revenue-sla-drill-down/03-07-SUMMARY.md` when done (record the live boundary-test output values)
</output>
