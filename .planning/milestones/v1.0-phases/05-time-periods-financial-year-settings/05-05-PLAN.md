---
phase: 05-time-periods-financial-year-settings
plan: 05
type: execute
wave: 3
depends_on: ["05-01", "05-02", "05-03", "05-04"]
files_modified:
  - types/db.ts
autonomous: false
requirements: [PERIOD-01, PERIOD-02, PERIOD-03, FY-01, TSYS-01, TSYS-02]
user_setup:
  - service: supabase
    why: "Push migrations 0023-0026 to the linked hosted project. tsc, vitest and next build all pass WITHOUT the push because types come from the generated types/db.ts rather than the live database — so the phase would verify green while app_settings, revenue_total_for_period, the loosened RPCs and the TSYS tier set do not exist."
    env_vars:
      - name: SUPABASE_ACCESS_TOKEN
        source: "Supabase Dashboard -> Account -> Access Tokens (needed for a non-interactive `supabase db push`; the project is already linked via supabase/config.toml). The Supabase MCP apply_migration tool is the established fallback path in this environment — 02-07, 03-07 and 04-04 all used one of these two routes."

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "app_settings and app_settings_audit exist in the linked project with RLS enabled, exactly one row in app_settings, and no delete policy on either table"
    - "revenue_total_for_period, the four-argument save_pricing_tier_set and delete_pricing_tier_set exist in the linked project; the three-argument save function and the latest-only delete function no longer exist, so no PostgREST overload ambiguity remains"
    - "The TSYS MSA tier set exists live: one pricing_tier_sets row with effective_from 2026-08-13 and reset_window 'monthly', six pricing_tiers rows at 0.0405 / 0.0279 / 0.0225 / 0.0205 / 0.0189 / 0.0174 with the sixth open-ended, and no placeholder set remains (ROADMAP SC4, D-14, D-15, D-16)"
    - "Executed against the live database, the MSA worked example returns exactly 45450.0000 for 1,500,000 monthly transactions, and every one of the six band-boundary assertions passes (ROADMAP SC4)"
    - "Executed against the live database, the D-06 invariant assertion passes: a multi-month figure equals the sum of per-month tiered figures and differs from the ladder run over aggregate volume (ROADMAP SC5, PERIOD-03)"
    - "types/db.ts regenerated from the live schema contains app_settings, app_settings_audit and the three new or changed function signatures, and tsc passes against it with every narrow-cast RPC workaround removed"
    - "Every one of the five metric page files resolves its window through lib/dashboard/period.ts — the promoted-noun invariant, enforced as a gate rather than assumed (ROADMAP SC2)"
    - "An admin can change the financial-year start at /settings/general against the live database, the audit row appears, and the FY boundary shown in the scope badge on a metric view moves accordingly (ROADMAP SC1)"
    - "Navigating to a previous month and a previous year on each of the five views shows that period's figures (ROADMAP SC3)"
    - "The Supabase security advisors report no new RLS-bypass or mutable-search_path warning for anything added in 0023-0026"
    - "The full unit suite, a clean type-check and a production build are all green before the push and again after type regeneration"
  artifacts:
    - path: "types/db.ts"
      provides: "Regenerated Supabase types covering app_settings, app_settings_audit and the new/changed RPC signatures"
      contains: "app_settings"
  key_links:
    - from: "supabase/migrations/0023..0026"
      to: "the linked Supabase project"
      via: "supabase db push, or Supabase MCP apply_migration applied in ascending order"
      pattern: "app_settings"
    - from: "supabase/tests/tsys_msa_tier_test.sql"
      to: "the live pricing_tiers rows"
      via: "executed against the pushed schema — the authoritative 45450.0000 run"
      pattern: "45450"
    - from: "app/(dashboard)/settings/pricing/actions.ts"
      to: "types/db.ts"
      via: "narrow-cast RPC workarounds removed once the real signatures exist"
      pattern: "save_pricing_tier_set"
---

<objective>
Land migrations 0023-0026 in the live database, regenerate the DB types, and run the authoritative
verification the rest of the phase has only been able to encode: that the TSYS MSA rates produce
$45,450 for the MSA's own worked example, that a multi-month figure is the sum of per-month tiered
figures and not the ladder over aggregate volume, that the financial-year setting is editable end
to end, and that all five views answer the same period question against real data.

Purpose: this is the mandatory schema gate. `npx tsc --noEmit`, `npm test` and `npm run build` all
pass without the push — types come from the generated `types/db.ts` and the unit tests are pure —
so skipping it would produce a confidently green phase in which `app_settings` does not exist, the
pricing RPCs still carry their Phase-3 guards, and revenue is still priced off placeholder rates.
Output: 0023-0026 live, `types/db.ts` regenerated, every narrow cast removed, the SQL assertions
executed for real, and the five ROADMAP success criteria demonstrated against live infrastructure.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-time-periods-financial-year-settings/05-CONTEXT.md
@.planning/phases/05-time-periods-financial-year-settings/05-RESEARCH.md
@.planning/phases/05-time-periods-financial-year-settings/05-UI-SPEC.md
@.planning/phases/04-reconciliation-discrepancy-flagging/04-04-PLAN.md
@supabase/config.toml
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
The complete Phase 5 artifact list, verified live by this plan:

| Kind | Name |
|---|---|
| Tables | `app_settings`, `app_settings_audit` |
| Trigger + function | `trg_app_settings_audit`, `fn_app_settings_audit()` |
| SQL functions | `revenue_total_for_period(date, date)`, `save_pricing_tier_set(date, text, jsonb, uuid)`, `delete_pricing_tier_set(uuid)` |
| Removed SQL functions | the three-argument `save_pricing_tier_set`, the latest-only delete function |
| Data | The TSYS MSA tier set (one `pricing_tier_sets` row + six `pricing_tiers` rows) |
| Migrations | `0023_app_settings.sql`, `0024_revenue_total_for_period.sql`, `0025_pricing_tier_edit_in_place.sql`, `0026_tsys_msa_tier_seed.sql` |
| SQL tests | `supabase/tests/tsys_msa_tier_test.sql` |
| TS modules | `lib/dashboard/period.ts`, `lib/settings/fy-settings.ts`, `lib/settings/schema.ts` |
| TS functions | `resolvePeriod`, `resolveFinancialYearBounds`, `monthOptions`, `yearOptions`, `serializePeriodParams`, `fetchFinancialYearStart`, `rowsWithin`, `fetchCardInventoryRowsUpTo`, `saveFinancialYearSettings`, `savePricingTierSet`, `deletePricingTierSet`, `countRestatedDays` |
| Components | `ScopeBadge`, `PeriodControls`, `PeriodEmptyState`, `FySettingsForm`, `TierSetSelector`, `DeleteTierSet`, `components/ui/select.tsx` |
| Routes | `/settings/general` (new); `/verifications`, `/revenue`, `/sla`, `/cards`, `/reconciliation`, `/settings/pricing` (extended) |
| Nav | `{ href: "/settings/general", label: "General", icon: "tools" }` |
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Full test suite, type-check and production build green before touching the live database</name>
  <files>—</files>
  <read_first>
    - package.json — the `test` script (`vitest run`) and the `build` script
    - lib/dashboard/__tests__/period.test.ts, lib/settings/__tests__/schema.test.ts, lib/dashboard/card-inventory.test.ts — this phase's suites
    - lib/dashboard/__tests__/drill-params.test.ts, lib/dashboard/__tests__/reconciliation-status.test.ts, lib/pricing/__tests__/schema.test.ts — the prior-phase regression suites that must still pass
    - .planning/phases/04-reconciliation-discrepancy-flagging/04-04-PLAN.md Task 1 — the identical pre-push regression gate this task repeats
  </read_first>
  <action>Run the full unit suite with `npm test` and confirm the three Phase-5 suites are present and passing alongside every prior-phase suite. Run `npx tsc --noEmit` clean. Run `npm run build` and confirm it succeeds — this is what re-catches the RSC function-passing class of crash (the 260821-mgy incident) that `next dev` hides, and Phase 5 adds several new client components consumed by Server Components, so it is a live risk. Do not weaken or skip any assertion to make this pass: a real failure belongs to the plan that owns the file (05-01 through 05-04), not here. Record the test count and the build output tail in the SUMMARY.</action>
  <verify>
    <automated>npm test && npx tsc --noEmit && npm run build</automated>
    <fails_when>non-zero exit from any of the three, any "error TS" line, any failed-test line in the vitest summary, or a "Failed to compile" line in the build output</fails_when>
    <automated>bash -c "for f in 'app/(dashboard)/verifications/page.tsx' 'app/(dashboard)/revenue/page.tsx' 'app/(dashboard)/sla/page.tsx' 'app/(dashboard)/cards/page.tsx' 'app/(dashboard)/reconciliation/page.tsx'; do grep -q 'resolvePeriod' \"\$f\" || { echo \"UNSCOPED: \$f\"; exit 1; }; done; echo ALL_FIVE_SCOPED"</automated>
    <fails_when>the string ALL_FIVE_SCOPED is absent from stdout, or an "UNSCOPED:" line names a page — this is the promoted-noun invariant from the 05-01 assumption-delta decision: every metric view resolves its window through the one resolver</fails_when>
  </verify>
  <acceptance_criteria>
    - `npm test` exits 0 with `period.test.ts`, `lib/settings/__tests__/schema.test.ts` and `card-inventory.test.ts` all present in the run.
    - `npx tsc --noEmit` exits 0.
    - `npm run build` exits 0.
    - All five metric page files reference `resolvePeriod`.
  </acceptance_criteria>
  <done>Every line of Phase 5 code is unit-proven and production-buildable before the live database is touched.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: [BLOCKING] Push migrations 0023-0026 to the linked Supabase project</name>
  <read_first>
    - supabase/config.toml — the project is already linked
    - supabase/migrations/0023_app_settings.sql, 0024_revenue_total_for_period.sql, 0025_pricing_tier_edit_in_place.sql, 0026_tsys_msa_tier_seed.sql — exactly what is being applied, in this order
    - .planning/phases/04-reconciliation-discrepancy-flagging/04-04-PLAN.md Task 2 — the established push procedure and its MCP fallback
    - .planning/phases/05-time-periods-financial-year-settings/05-RESEARCH.md — Runtime State Inventory (the placeholder tier set is deleted by 0026; its audit rows survive via ON DELETE SET NULL, and no materialized data needs invalidating because the revenue chain is all views)
  </read_first>
  <what-built>Four migrations. 0023 creates `app_settings` and `app_settings_audit` with RLS and a `SECURITY DEFINER` audit trigger. 0024 creates `revenue_total_for_period(date, date)`. 0025 replaces `save_pricing_tier_set` with a four-argument in-place-capable form, replaces the latest-only delete with `delete_pricing_tier_set(uuid)`, and adds the data-window coverage guard. 0026 deletes the placeholder pricing tier set (recording the deletion in `pricing_tier_audit`) and seeds the signed TSYS MSA six-tier set effective 2026-08-13 with a monthly reset window.</what-built>
  <action>Attempt the non-interactive CLI route first: confirm `SUPABASE_ACCESS_TOKEN` is set, then run `supabase db push`. If the CLI is unavailable or the token cannot be supplied without an interactive prompt, fall back to the Supabase MCP `apply_migration` tool, applying 0023, 0024, 0025 and 0026 in that exact ascending order — 0025 must precede 0026 so the loosened guards are in place before the seed runs, and 0026's placeholder delete must precede its insert so the `effective_from` UNIQUE constraint cannot collide. This is a `checkpoint:human-action` because `supabase db push` commonly prompts for confirmation and may require the human to provide or confirm the access token; automate everything automatable and pause only if authentication genuinely blocks.</action>
  <instructions>
    Claude has already written and unit-proven all four migrations and will attempt the push itself
    via `supabase db push` (or the Supabase MCP `apply_migration` fallback). The one thing Claude
    may not be able to do is authenticate.

    If Claude reports an authentication block:
    1. Create or copy a personal access token from the Supabase Dashboard -> Account -> Access Tokens.
    2. Paste the token here, or export `SUPABASE_ACCESS_TOKEN` in the shell Claude is using.
    Claude then retries the push and runs every post-push check itself.

    Do not run the migrations by hand in the SQL editor unless Claude asks you to — applying them
    out of order (0026 before 0025) would hit the retired backdating guard on the seed.
  </instructions>
  <verification>After the push, query the live schema (Supabase MCP `execute_sql`, `supabase db diff`, or psql): confirm `app_settings` holds exactly one row and both new tables report `relrowsecurity`; confirm `pg_proc` lists `revenue_total_for_period`, `delete_pricing_tier_set` and a `save_pricing_tier_set` with four arguments, and lists neither a three-argument `save_pricing_tier_set` nor the retired latest-only delete function; confirm `pricing_tier_sets` holds exactly one row with `effective_from = 2026-08-13` and `reset_window = 'monthly'`, and its six `pricing_tiers` rows carry the MSA rates with a NULL `upper_bound` on `tier_order` 5. Then run Supabase `get_advisors` and confirm no new RLS-bypass or mutable-search_path warning appears for anything added in 0023-0026.</verification>
  <verify>
    <human-check>0023-0026 applied in order. app_settings has exactly one row and RLS on; app_settings_audit has RLS on and no client write policy. The four-argument save function, delete_pricing_tier_set and revenue_total_for_period all exist; the three-argument save function and the latest-only delete function are gone. Exactly one pricing_tier_sets row remains, effective 2026-08-13, monthly, with the six MSA rates and an open-ended top tier. Security advisors report nothing new.</human-check>
  </verify>
  <acceptance_criteria>
    - All four migrations applied, in ascending order.
    - `app_settings` row count is exactly 1; RLS enabled on `app_settings` and `app_settings_audit`.
    - `pg_proc` contains `revenue_total_for_period`, `delete_pricing_tier_set` and a four-argument `save_pricing_tier_set`, and contains neither the three-argument save form nor the retired latest-only delete function.
    - `pricing_tier_sets` contains exactly one row: `effective_from = 2026-08-13`, `reset_window = 'monthly'`.
    - Its `pricing_tiers` rows are exactly six, rates 0.0405 / 0.0279 / 0.0225 / 0.0205 / 0.0189 / 0.0174, `upper_bound` NULL only on `tier_order` 5.
    - No new security-advisor warning for the objects added in 0023-0026.
  </acceptance_criteria>
  <resume-signal>Type "pushed" once all four migrations are applied and every item above is confirmed, or describe the blocker.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Regenerate types, drop the narrow casts, and run the authoritative live verification</name>
  <files>types/db.ts, app/(dashboard)/settings/pricing/actions.ts, app/(dashboard)/revenue/page.tsx</files>
  <read_first>
    - types/db.ts — the current generated types, about to be regenerated
    - CLAUDE.md — the `supabase gen types typescript --linked > types/db.ts` convention
    - app/(dashboard)/settings/pricing/actions.ts — the narrow-cast RPC wrappers to remove once the real signatures exist
    - app/(dashboard)/revenue/page.tsx — the `revenue_total_for_period` narrow cast to remove
    - supabase/tests/tsys_msa_tier_test.sql — the three assertion blocks to execute authoritatively
    - supabase/tests/revenue_boundary_test.sql — the prior-phase fixture that must still pass after the tier data changed, since it installs its own isolated tier set inside a rolled-back transaction
    - .planning/phases/05-time-periods-financial-year-settings/05-UI-SPEC.md — the Interaction and State Contract, the source of the manual checklist below
  </read_first>
  <action>
(a) Regenerate types: `supabase gen types typescript --linked > types/db.ts` (or the Supabase MCP type-generation path if the CLI is unavailable). Re-run `npx tsc --noEmit` and fix any column- or argument-mapping errors in the owning files — never by loosening a type.

(b) Remove every narrow-cast RPC workaround now that the real signatures exist: the `supabase.rpc as unknown as (...)` wrappers in `app/(dashboard)/settings/pricing/actions.ts` (save and delete) and in `app/(dashboard)/revenue/page.tsx` (`revenue_total_for_period`). Call the typed `supabase.rpc` directly and let the compiler check the argument names against the regenerated types. This is the point of the type regeneration: leaving the casts in place would keep the phase's three most important write and money paths untyped.

(c) AUTHORITATIVE VERIFICATION against the live database, via `supabase db`, psql, or Supabase MCP `execute_sql`. Execute `supabase/tests/tsys_msa_tier_test.sql`. Every block rolls back, so this is safe to run against live data, but read the isolation deletes in each block before running and confirm for yourself they are inside the transaction. Confirm: Block A reports the MSA worked example at exactly `45450.0000` and passes all six band-boundary assertions; Block B passes the D-06 invariant, with the summed-per-month figure differing from the aggregate-ladder figure — record both numbers in the SUMMARY, because the difference is the evidence for ROADMAP SC5; Block C reports `20251.3950` through the real `v_revenue_daily` chain. Also re-run `supabase/tests/revenue_boundary_test.sql` and confirm it still passes, since 0026 changed the live tier data underneath it. If any assertion fails, STOP: the defect is in the owning plan's SQL (05-04 for the seed and the ladder, 05-02 for the RPC), so fix it there, re-push, and re-verify — do not adjust the expected figure to match the output.

(d) Live period verification. Query `revenue_total_for_period` for a single month, for the calendar year containing it, and for the open-ended all-time range, and confirm the year figure equals the sum of the per-month figures across the data window. Confirm `/verifications`, `/revenue`, `/sla`, `/cards` and `/reconciliation` each return different figures for a previous month than for the current one. Record the observed numbers.

(e) Manual UAT against the running app, covering the UI-SPEC Interaction and State Contract: every one of the five views defaults to the current UTC month and shows the scope badge beside the freshness badge; the Month / Year / All time toggle changes the URL and the figures; selecting Year reveals the Calendar / Financial toggle and the other two scopes remove it from the DOM; an unrecognised `?period=` value renders the default and the badge states the default; a period with no data shows the calendar-icon period-empty state with a working `View current month` link and no upload call to action; on `/settings/general` the FY start saves, toasts, appears at the top of Change history, and the new boundary dates immediately show inside the scope badge when a metric view is switched to Year then Financial; day 30 with February selected is rejected inline; on `/settings/pricing` the TSYS set loads into the editor, a rate edit opens the warning-styled restate dialog with a plausible day count, confirming it writes an old-to-new audit entry, and attempting to delete the only tier set is refused with the data-window message as a toast rather than a silent no-op; `/revenue` now shows figures priced at the real MSA rates. Record each observation.
  </action>
  <verify>
    <automated>bash -c "grep -q 'app_settings' types/db.ts && grep -q 'app_settings_audit' types/db.ts && grep -q 'revenue_total_for_period' types/db.ts && grep -q 'delete_pricing_tier_set' types/db.ts && echo TYPES_OK"</automated>
    <fails_when>the string TYPES_OK is absent from stdout, or the command exits non-zero — a missing name means the types were regenerated against a project the migrations never reached</fails_when>
    <automated>bash -c "! grep -q 'supabase.rpc as unknown' 'app/(dashboard)/settings/pricing/actions.ts' && ! grep -q 'supabase.rpc as unknown' 'app/(dashboard)/revenue/page.tsx' && echo CASTS_REMOVED"</automated>
    <fails_when>the string CASTS_REMOVED is absent from stdout, or the command exits non-zero — a surviving cast means a money or write path is still untyped after regeneration</fails_when>
    <automated>npx tsc --noEmit && npm test && npm run build</automated>
    <fails_when>non-zero exit from any of the three, any "error TS" line, any failed-test line in the vitest summary, or a "Failed to compile" line in the build output</fails_when>
    <human-check>Live SQL run recorded: the MSA worked example returns exactly 45450.0000; all six band-boundary assertions pass; the D-06 invariant block passes with the summed-per-month figure and the aggregate-ladder figure both recorded and different; Block C returns 20251.3950; revenue_boundary_test.sql still returns 215.0000. Manual UAT: all five views default to the current UTC month with a scope badge, previous periods show their own figures, the FY setting saves and moves the boundary shown in the badge, and the pricing editor's restate dialog and blocked-delete refusal both behave as specified.</human-check>
  </verify>
  <acceptance_criteria>
    - `types/db.ts` contains `app_settings`, `app_settings_audit`, `revenue_total_for_period` and `delete_pricing_tier_set`.
    - No `supabase.rpc as unknown` cast remains in the pricing actions or the revenue page.
    - `npx tsc --noEmit`, `npm test` and `npm run build` all exit 0 after regeneration.
    - Live SQL output recorded in the SUMMARY: `45450.0000`, the six boundary figures, both sides of the D-06 comparison, `20251.3950`, and `215.0000` from the retained Phase-3 fixture.
    - Live check recorded: `revenue_total_for_period` for a calendar year equals the sum of its per-month values.
    - Manual UAT observations recorded for all five views plus both settings pages.
  </acceptance_criteria>
  <done>The period lens, the financial-year setting and the signed TSYS rates are all proven on real infrastructure, and no money path is left untyped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local migration files -> live Postgres | Schema, function and data changes applied to the hosted project |
| Authenticated browser -> the new tables and functions | Everything added in 0023-0026 must honour RLS as the querying user |
| Generated types -> compiler guarantees | Until regeneration, three write and money paths are cast past the type system |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-25 | Tampering | A false-positive "done" with no push — types come from generated config, tests are pure | critical | mitigate | This [BLOCKING] gate applies 0023-0026 and runs the live SQL assertions as the authoritative check, exactly as 02-07, 03-07 and 04-04 did before it. |
| T-05-26 | Information Disclosure | The two new tables readable without a session, or the new functions bypassing RLS | high | mitigate | Post-push confirmation of `relrowsecurity` on both tables, `security invoker` on `revenue_total_for_period`, execute revoked from `anon` on all three functions, plus a Supabase advisor run checking for new RLS-bypass and mutable-search_path warnings. |
| T-05-27 | Denial of correctness | Revenue priced off wrong rates, or a year figure computed by running the ladder over aggregate volume | critical | mitigate | The live run asserts 45450.0000 against the actually-seeded rows, all six band boundaries, and the per-month-versus-aggregate difference. A mismatch stops the plan rather than being reconciled by editing the expectation. |
| T-05-28 | Tampering | PostgREST resolving the wrong `save_pricing_tier_set` overload | high | mitigate | Post-push check asserts the three-argument form no longer exists, so only one candidate can match; the regenerated types then bind the four-argument signature at compile time. |
| T-05-29 | Repudiation | The placeholder tier set disappearing with no record | medium | mitigate | 0026 writes a `pricing_tier_audit` row naming each removed set before deleting it, and the existing ON DELETE SET NULL foreign key preserves the summary text afterwards; confirmed by reading the audit log on `/settings/pricing` during UAT. |
| T-05-30 | Tampering | Untyped RPC calls surviving type regeneration | medium | mitigate | An explicit gate fails the task while any `supabase.rpc as unknown` cast remains in the pricing actions or the revenue page. |
| T-05-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs in this plan (05-RESEARCH Package Legitimacy Audit: not applicable). |
</threat_model>

<verification>
- Pre-push: `npm test`, `npx tsc --noEmit`, `npm run build` all green; all five metric pages reference `resolvePeriod`.
- Post-push: both new tables exist with RLS and the singleton invariant; the three new functions exist and both retired functions are gone; exactly one tier set, effective 2026-08-13, monthly, six MSA rates; no new security-advisor warning.
- Live SQL: 45450.0000, six band boundaries, the D-06 per-month-versus-aggregate difference, 20251.3950, and the retained 215.0000 fixture.
- Post-regeneration: `types/db.ts` carries the new names, every narrow cast is gone, and the three commands are green again.
- Manual UAT recorded for all five metric views, `/settings/general` and `/settings/pricing`.

</verification>

<success_criteria>
- ROADMAP SC1: the FY start is settable in the settings area with no redeploy and the FY/CY toggle derives its boundaries from it — demonstrated live.
- ROADMAP SC2: all five metric views accept the same period scope and show which is active.
- ROADMAP SC3: any previous month or year shows that period's figures.
- ROADMAP SC4: the TSYS MSA tier table exists as a `pricing_tier_sets` row with `reset_window = 'monthly'` and the worked example matches to the cent.
- ROADMAP SC5: period scoping never changes the tier maths, proven by an executable assertion that the aggregate-ladder alternative gives a different, lower number.
</success_criteria>

<output>
Create `.planning/phases/05-time-periods-financial-year-settings/05-05-SUMMARY.md` when done. Record the live SQL figures verbatim (45450.0000, the six band boundaries, both sides of the D-06 comparison, 20251.3950, 215.0000), the observed period figures for one month versus its containing year, and every manual UAT observation.
</output>
