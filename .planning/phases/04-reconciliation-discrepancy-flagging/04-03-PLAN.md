---
phase: 04-reconciliation-discrepancy-flagging
plan: 03
type: execute
wave: 3
depends_on: [04-01, 04-02]
files_modified:
  - supabase/migrations/0019_v_reconciliation_inventory.sql
  - supabase/migrations/0020_v_apigee_cross_check.sql
  - lib/dashboard/reconciliation-drill.ts
  - components/dashboard/reconciliation-inventory-table.tsx
  - components/dashboard/reconciliation-drill-sheet.tsx
  - app/(dashboard)/reconciliation/page.tsx
autonomous: true
requirements: [RECON-02, RECON-03, DASH-02]
must_haves:
  truths:
    - "The /reconciliation page shows a Card inventory section below the billing section with a live-count / enrolled / unenrolled summary strip"
    - "Each inventory day row shows status + signed delta (snapshot day-over-day drop vs removed-cards tally), flagging when they disagree (D-06)"
    - "Days with no card_inventory snapshot appear as distinct 'missing report day' rows, never silently skipped and never diffed across (D-07)"
    - "An APIGEE cross-check sub-table shows the four endpoint mappings as needs-review (amber) only — never escalated to mismatch (D-10)"
    - "Clicking an inventory day drills to the contributing card_inventory and removed_cards rows for that day"
  artifacts:
    - path: "supabase/migrations/0019_v_reconciliation_inventory.sql"
      provides: "v_inventory_daily_diff, v_inventory_gap_days, v_inventory_live_count, v_reconciliation_inventory_daily"
      contains: "generate_series"
    - path: "supabase/migrations/0020_v_apigee_cross_check.sql"
      provides: "v_apigee_cross_check — always ok/needs_review, structurally no mismatch branch (D-10)"
      contains: "needs_review"
    - path: "components/dashboard/reconciliation-inventory-table.tsx"
      provides: "'use client' inventory section table + APIGEE sub-table + gap-row treatment"
    - path: "app/(dashboard)/reconciliation/page.tsx"
      provides: "Extended to render the inventory section, gap rows, APIGEE sub-table, and inventory drill"
  key_links:
    - from: "supabase/migrations/0019_v_reconciliation_inventory.sql"
      to: "removed_cards + card_inventory"
      via: "day-over-day self-join diff compared to daily removed-cards tally (D-06)"
      pattern: "removed_cards"
    - from: "supabase/migrations/0020_v_apigee_cross_check.sql"
      to: "status enum"
      via: "case with only 'ok'/'needs_review', no mismatch branch (D-10)"
      pattern: "needs_review"
    - from: "app/(dashboard)/reconciliation/page.tsx"
      to: "v_reconciliation_inventory_daily / v_apigee_cross_check / v_inventory_gap_days"
      via: "Promise.all reads extended to the inventory + apigee + gap views"
      pattern: "v_reconciliation_inventory_daily"
---

<objective>
Deliver the card-inventory reconciliation vertical slice end to end, completing the /reconciliation page's second section. SQL self-join diff + gap detection + APIGEE cross-check views -> server fetch -> inventory section (summary strip + per-day flags + gap rows + APIGEE sub-table) -> inventory drill.

Purpose: RECON-02 + DASH-02 + RECON-03 (inventory side). After this slice a user sees live card count, daily enrolled/unenrolled, the inventory-drop-vs-removed-cards flag, missing-snapshot-day gaps surfaced explicitly, and the corroborating amber-only APIGEE cross-check — all drillable to source.
Output: migrations 0019 + 0020 (five views), the inventory drill fetcher (extends 04-02's file), the inventory section table + APIGEE sub-table, the drill-sheet inventory variant, and the page edit adding the section. Pushed to the live DB and verified in 04-04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-reconciliation-discrepancy-flagging/04-CONTEXT.md
@.planning/phases/04-reconciliation-discrepancy-flagging/04-RESEARCH.md
@.planning/phases/04-reconciliation-discrepancy-flagging/04-PATTERNS.md
@.planning/phases/04-reconciliation-discrepancy-flagging/04-UI-SPEC.md
@.planning/phases/04-reconciliation-discrepancy-flagging/04-02-SUMMARY.md

<interfaces>
Base table columns:
- card_inventory: report_date (date), external_card_reference, created_at (timestamptz enrolment), source_file_id. Cutoff literal is '2026-08-13'::date (plain date column — NOT the timestamptz string).
- removed_cards: removed_at (timestamptz), external_card_reference, source_file_id.
- apigee_calls: event_time (timestamptz), endpoint_category (text nullable), external_card_reference (nullable), response_code (integer), source_file_id.
- verifications: created_at, ...; dcvv_fetches: (dCVV table, see 0007).

APIGEE endpoint_category values (derived at ingest, lib/ingestion/normalise-apigee.ts):
  'verify' -> verifications, 'cvv-fetch' -> dcvv_fetches, 'enrol' -> card_inventory enrolments (day-over-day enrolled), 'unenrol' -> removed_cards. NULL endpoint_category = no-match (exclude from mapping). D-10: surface response_code = 500 counts.

From 04-01: reconciliation-status.ts (ReconciliationStatus type, StatusBadge). Inventory status reuses computeReconciliationStatus semantics (delta = inventory drop - removed tally; settled/gap handling per D-06/D-07).

From 04-02 (extend, do not rewrite): lib/dashboard/reconciliation-drill.ts (add fetchReconciliationInventoryDrillRows), components/dashboard/reconciliation-drill-sheet.tsx (add inventory variant to the same ReconciliationDrillSheet), app/(dashboard)/reconciliation/page.tsx (add inventory section + widen empty check + inventory drill dispatch).

SQL templates: 0012_v_revenue.sql (view-chain comments), 0013_v_sla_daily.sql (grouped single-status view; count(*) filter (where ...) idiom for the 500-count), 0017_v_revenue_total.sql (single-row total idiom for v_inventory_live_count). RESEARCH.md Patterns 2 (self-join set difference) and 3 (generate_series gap spine) give the exact starting SQL — flagged A2/MEDIUM confidence, validate against seed data.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inventory reconciliation SQL views (0019)</name>
  <files>supabase/migrations/0019_v_reconciliation_inventory.sql</files>
  <read_first>
    - supabase/migrations/0012_v_revenue.sql (multi-view chain + header-comment convention)
    - supabase/migrations/0017_v_revenue_total.sql (single-row total idiom for the live-count view; PGRST123 rationale)
    - supabase/migrations/0008_card_inventory.sql (report_date is a plain date; unique (report_date, external_card_reference))
    - supabase/migrations/0009_removed_cards.sql (removed_at timestamptz event log — no report_date; day tally = group by date_trunc)
    - .planning/phases/04-reconciliation-discrepancy-flagging/04-RESEARCH.md sections "Pattern 2", "Pattern 3", "Pitfall 3", "Pitfall 4", Assumptions A2
    - .planning/phases/04-reconciliation-discrepancy-flagging/04-PATTERNS.md section 0019
    - lib/dashboard/reconciliation-status.ts (status logic the inventory case must mirror)
    - .planning/phases/04-reconciliation-discrepancy-flagging/04-CONTEXT.md D-05/D-06/D-07
  </read_first>
  <action>Create supabase/migrations/0019_v_reconciliation_inventory.sql with a bottom-up view-chain header comment. Define four views, each with (security_invoker = on) and a comment on view trailer: (1) v_inventory_daily_diff — the day-over-day snapshot set-difference from RESEARCH Pattern 2: self-join card_inventory today vs a snapshot shifted report_date - interval '1 day', using NULL-checks to count enrolled_count (present today, absent yesterday) and unenrolled_count (present yesterday, absent today), grouped by day; restrict to day-pairs where BOTH days have a snapshot so a gap is never diffed across (D-07). Apply the '2026-08-13'::date cutoff (plain date, Pitfall 4). (2) v_inventory_gap_days — RESEARCH Pattern 3: generate_series('2026-08-13'::date, (select coalesce(max(report_date),'2026-08-13'::date) from card_inventory), interval '1 day') LEFT JOIN card_inventory, where the join is null, yielding missing_day rows. (3) v_inventory_live_count — single-row view mirroring 0017: count(distinct external_card_reference) as live_count from card_inventory where report_date = (select max(report_date) from card_inventory). (4) v_reconciliation_inventory_daily — join v_inventory_daily_diff's unenrolled_count against the daily removed_cards tally (count(*) grouped by date_trunc('day', removed_at at time zone 'UTC')::date), compute delta = unenrolled_count - removed_count and short_side, a settled boolean from max day of the joined data (mirror 0018's max(day_utc) approach, never now()/current_date, Pitfall 1), and a status case yielding only 'ok'/'needs_review'/'mismatch' matching computeReconciliationStatus. Expose day, enrolled_count, unenrolled_count, removed_count, delta, short_side, status.</action>
  <verify>
    <automated>grep -c "security_invoker = on" supabase/migrations/0019_v_reconciliation_inventory.sql; grep -qi "generate_series" supabase/migrations/0019_v_reconciliation_inventory.sql && grep -q "removed_cards" supabase/migrations/0019_v_reconciliation_inventory.sql && grep -q "'2026-08-13'::date" supabase/migrations/0019_v_reconciliation_inventory.sql && ! grep -qiE "current_date|now\(\)" supabase/migrations/0019_v_reconciliation_inventory.sql && echo GATES_OK</automated>
  </verify>
  <acceptance_criteria>
    - Four views defined, all with (security_invoker = on) (grep count >= 4).
    - v_inventory_gap_days uses generate_series and surfaces days with no snapshot (D-07); the diff view never diffs across a gap.
    - Cutoff uses '2026-08-13'::date (plain date column, Pitfall 4); no current_date/now() (GATES_OK printed).
    - v_reconciliation_inventory_daily compares unenrolled_count vs removed_cards daily tally (D-06) and yields only ok/needs_review/mismatch.
    - v_inventory_live_count is a single-row distinct-count view (avoids the PGRST123 client-aggregate block).
  </acceptance_criteria>
  <done>Migration 0019 defines the inventory diff, gap-detection, live-count, and inventory reconciliation views following the view-chain convention, gap-safe and clock-free. (Push in 04-04.)</done>
</task>

<task type="auto">
  <name>Task 2: APIGEE cross-check view (0020) + inventory drill fetcher + inventory table</name>
  <files>supabase/migrations/0020_v_apigee_cross_check.sql, lib/dashboard/reconciliation-drill.ts, components/dashboard/reconciliation-inventory-table.tsx</files>
  <read_first>
    - supabase/migrations/0013_v_sla_daily.sql (grouped view + count(*) filter (where ...) idiom for the 500-count column)
    - supabase/migrations/0010_apigee_stats.sql (apigee_calls columns; endpoint_category values)
    - supabase/migrations/0007_dcvv.sql (dcvv_fetches columns for the cvv-fetch mapping)
    - lib/dashboard/reconciliation-drill.ts (from 04-02 — extend with the inventory fetcher, mirroring the billing fetcher's day-range whitelisted shape)
    - components/dashboard/sla-breach-table.tsx + components/dashboard/reconciliation-billing-table.tsx (section-table + APIGEE-subtable + gap-row patterns to mirror)
    - .planning/phases/04-reconciliation-discrepancy-flagging/04-RESEARCH.md Anti-Patterns ("Escalating APIGEE divergence"), Assumptions A3
    - .planning/phases/04-reconciliation-discrepancy-flagging/04-PATTERNS.md sections 0020 and reconciliation-inventory-table.tsx
    - .planning/phases/04-reconciliation-discrepancy-flagging/04-UI-SPEC.md Color (APIGEE always warning), Interaction contract (gap-row distinct treatment, APIGEE subordinate), Copywriting (APIGEE + gap copy)
  </read_first>
  <action>Create supabase/migrations/0020_v_apigee_cross_check.sql with (security_invoker = on): group apigee_calls by day_utc (date_trunc('day', event_time at time zone 'UTC')) and endpoint_category (exclude NULL categories), with the '2026-08-13T00:00:00Z' cutoff; for each of the four mappings compute apigee_count and the mapped_count from the counterpart source (verify->verifications, cvv-fetch->dcvv_fetches, enrol->v_inventory_daily_diff.enrolled_count, unenrol->removed_cards daily tally), a error_500_count = count(*) filter (where response_code = 500), and a status case that is STRUCTURALLY ONLY 'ok' (counts equal) or 'needs_review' — NO mismatch branch at all (D-10 binding; A3 recommends a single spanning view with a mapped_metric discriminator column). Add a comment on view stating status is never 'mismatch' by design. Then EXTEND lib/dashboard/reconciliation-drill.ts with fetchReconciliationInventoryDrillRows(supabase, date): whitelisted day-range queries returning SEPARATE card_inventory rows (report_date = date) and removed_cards rows (day-range on removed_at) as a ReconciliationInventoryDrillFetchResult (never a merged list, Pitfall 5), each capped at DRILL_ROW_LIMIT with count: "exact". Then create components/dashboard/reconciliation-inventory-table.tsx as a 'use client' module: a summary strip of three secondary numerics (Live cards / Enrolled today / Unenrolled today per UI-SPEC labels), a TanStack per-day table over { day, enrolled_count, unenrolled_count, removed_count, delta, status } rendering StatusBadge + neutral-ink delta, rows drilling via openDrill({ drill: "recon-inventory", date }); gap-day rows (from v_inventory_gap_days) rendered with a distinct muted/dashed treatment and the copy "No inventory snapshot for {date} — cannot compute a diff for this day." with a warning badge (D-07); and a subordinate/nested APIGEE sub-table (rows from v_apigee_cross_check) styled always-amber (needs_review), never using the escalating status logic (D-10), with the APIGEE divergence + 500-surfacing copy. All column defs local to this 'use client' module.</action>
  <verify>
    <automated>grep -c "security_invoker = on" supabase/migrations/0020_v_apigee_cross_check.sql; ! grep -q "'mismatch'" supabase/migrations/0020_v_apigee_cross_check.sql && grep -q "response_code = 500" supabase/migrations/0020_v_apigee_cross_check.sql && grep -q "recon-inventory" components/dashboard/reconciliation-inventory-table.tsx && grep -q "fetchReconciliationInventoryDrillRows" lib/dashboard/reconciliation-drill.ts && npx tsc --noEmit && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - v_apigee_cross_check contains NO 'mismatch' literal — only 'ok'/'needs_review' (D-10 structurally enforced).
    - The view surfaces a response_code = 500 count column and carries the cutoff + security_invoker.
    - fetchReconciliationInventoryDrillRows returns separate card_inventory and removed_cards row arrays via whitelisted builders.
    - The inventory table renders the live/enrolled/unenrolled summary strip, per-day status+delta rows drilling with "recon-inventory", distinct gap rows, and an always-amber APIGEE sub-table.
    - npx tsc --noEmit passes.
  </acceptance_criteria>
  <done>The APIGEE cross-check view cannot escalate to mismatch by construction; the inventory drill fetcher returns separated source rows; the inventory section table renders the summary strip, per-day flags, gap rows, and amber APIGEE sub-table.</done>
</task>

<task type="auto">
  <name>Task 3: Wire inventory section + drill into the /reconciliation page</name>
  <files>app/(dashboard)/reconciliation/page.tsx, components/dashboard/reconciliation-drill-sheet.tsx</files>
  <read_first>
    - app/(dashboard)/reconciliation/page.tsx (from 04-02 — the billing-section page to extend)
    - components/dashboard/reconciliation-drill-sheet.tsx (from 04-02 — add the inventory variant to the same wrapper)
    - components/dashboard/reconciliation-inventory-table.tsx (from Task 2 — the section to mount)
    - lib/dashboard/reconciliation-drill.ts (from Task 2 — fetchReconciliationInventoryDrillRows)
    - app/(dashboard)/sla/page.tsx (Card + Separator section composition reference)
    - .planning/phases/04-reconciliation-discrepancy-flagging/04-PATTERNS.md section page.tsx (five-read Promise.all, empty-check across four sources)
    - .planning/phases/04-reconciliation-discrepancy-flagging/04-UI-SPEC.md Visual Hierarchy (two sections, separator, APIGEE subordinate) and Interaction contract (empty only when none of the four sources have data)
  </read_first>
  <action>EDIT app/(dashboard)/reconciliation/page.tsx: extend the Body's Promise.all to also read v_reconciliation_inventory_daily (order day_utc/day asc), v_apigee_cross_check (order day_utc asc), v_inventory_gap_days (order missing_day asc), and v_inventory_live_count (maybeSingle), plus a conditional fetchReconciliationInventoryDrillRows when drillFilter.drill === "recon-inventory". Add these results to the error-first check (any .error -> ErrorState). WIDEN the empty-state condition so EmptyState shows only when NONE of the four contributing sources (billing recon, inventory recon, gap days, apigee) have any data — not just the billing source (UI-SPEC binding). Render the Card inventory section below a Separator: the ReconciliationInventoryTable (summary strip fed by v_inventory_live_count + aggregated enrolled/unenrolled, per-day rows, gap rows, APIGEE sub-table) with its own good-news micro-state ("No inventory discrepancies in this period."). EDIT components/dashboard/reconciliation-drill-sheet.tsx: extend ReconciliationDrillSheet to also accept an inventory drill result and render, for the recon-inventory entity, two explicitly-labelled tables ("Card inventory rows" / "Removed-cards rows") mirroring the billing variant's two-source separation. Keep all column defs inside the 'use client' module (no RSC function passing). Sheet titles per UI-SPEC ("Card inventory — {date}, {status}").</action>
  <verify>
    <automated>npx tsc --noEmit && npm run build 2>&1 | tail -5 && grep -q "v_reconciliation_inventory_daily" app/\(dashboard\)/reconciliation/page.tsx && grep -q "v_apigee_cross_check" app/\(dashboard\)/reconciliation/page.tsx && grep -q "v_inventory_gap_days" app/\(dashboard\)/reconciliation/page.tsx && grep -q "recon-inventory" components/dashboard/reconciliation-drill-sheet.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - npm run build succeeds (no RSC function-passing crash).
    - The page reads all five recon/gap/apigee/live-count sources plus freshness in one Promise.all and error-firsts on any failure.
    - EmptyState triggers only when all four contributing sources are empty (widened from 04-02's billing-only check).
    - The inventory drill Sheet shows card_inventory and removed_cards rows as two explicitly-labelled tables.
    - Both sections render, separated by a Separator, APIGEE visually subordinate and amber-only.
  </acceptance_criteria>
  <done>The /reconciliation page now renders both reconciliation sections with the inventory summary strip, per-day flags, gap rows, subordinate amber APIGEE sub-table, and a two-source inventory drill; the app builds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser (authenticated user) -> Next Server Component -> Supabase | Session-scoped client; the four new views must honour source-table RLS |
| URL query string (?drill=recon-inventory&date=) -> parseDrillParams -> query builder | Untrusted params reach queries only as whitelisted typed fields |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06 | Information Disclosure | v_inventory_daily_diff, v_inventory_gap_days, v_inventory_live_count, v_reconciliation_inventory_daily, v_apigee_cross_check | mitigate | All five views declared with (security_invoker = on) so they run as the querying user and honour card_inventory/removed_cards/apigee_calls/dcvv_fetches/verifications RLS; verified by grep gates in Tasks 1 and 2. |
| T-04-07 | Tampering | reconciliation-drill.ts inventory fetcher / page drill dispatch | mitigate | recon-inventory date/entity only from parseDrillParams-validated DrillFilter; .gte()/.lt()/.eq() builders only, no string interpolation. |
| T-04-08 | Repudiation / Elevation | v_apigee_cross_check status | mitigate | Status case has no 'mismatch' branch — a false hard-fail (which could erode trust in the real flags or mask a real problem as merely amber-vs-red) is structurally impossible (D-10). Verified by grep gate (no 'mismatch' literal). |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No new packages this phase (RESEARCH Package Legitimacy Audit: not applicable). |
</threat_model>

<verification>
- npm run build green.
- Migration 0019 gates: security_invoker x4, generate_series present, removed_cards join, '2026-08-13'::date cutoff, no clock.
- Migration 0020 gates: security_invoker present, NO 'mismatch' literal, 500-count column.
- Manual (post-04-04 push): inventory section shows summary strip, day flags, a gap row for a missing snapshot day, an amber APIGEE sub-table; inventory drill opens two labelled tables.
</verification>

<success_criteria>
The card-inventory reconciliation slice works end to end: gap-safe self-join diff, generate_series gap surfacing, single-row live count, and an escalation-proof amber APIGEE cross-check feed the second /reconciliation section with a summary strip, per-day status+delta flags, distinct gap rows, and a two-source inventory drill.
</success_criteria>

<output>
Create `.planning/phases/04-reconciliation-discrepancy-flagging/04-03-SUMMARY.md` when done.
</output>
