---
phase: 04-reconciliation-discrepancy-flagging
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - lib/dashboard/reconciliation-status.ts
  - lib/dashboard/__tests__/reconciliation-status.test.ts
  - lib/dashboard/drill-params.ts
  - components/dashboard/status-badge.tsx
autonomous: true
requirements: [RECON-01, RECON-03]
must_haves:
  truths:
    - "computeReconciliationStatus returns 'ok' when counts are equal, 'needs_review' when unequal and unsettled, 'mismatch' when unequal and settled"
    - "A shared StatusBadge renders exactly three visual states (OK / Needs review / Mismatch) mapped to brand --success/--warning/--error"
    - "parseDrillParams accepts 'recon-billing' and 'recon-inventory' as valid drill entities and still rejects unknown entities"
  artifacts:
    - path: "lib/dashboard/reconciliation-status.ts"
      provides: "Pure computeReconciliationStatus() + status→badge mapping helpers, unit-tested; the single source of truth the SQL views must match"
      min_lines: 30
    - path: "lib/dashboard/__tests__/reconciliation-status.test.ts"
      provides: "Boundary tests for the settling state machine (D-03) covering equal/unequal × settled/unsettled"
    - path: "components/dashboard/status-badge.tsx"
      provides: "Shared 3-state Badge component used by both reconciliation section tables"
      contains: "needs_review"
    - path: "lib/dashboard/drill-params.ts"
      provides: "Extended DrillEntity whitelist with recon-billing + recon-inventory"
      contains: "recon-billing"
  key_links:
    - from: "lib/dashboard/drill-params.ts"
      to: "DRILL_ENTITIES array"
      via: "recon-billing/recon-inventory added to both the union type and the runtime whitelist array"
      pattern: "recon-billing"
    - from: "components/dashboard/status-badge.tsx"
      to: "brand semantic tokens"
      via: "className references var(--success)/var(--warning)/var(--error)"
      pattern: "var\\(--warning\\)"
---

<objective>
Establish the tested correctness core and shared UI contract that both reconciliation vertical slices (billing-vs-verification, card-inventory) build on: (1) a pure, unit-tested `computeReconciliationStatus()` state machine that the SQL views must mirror exactly, (2) a shared 3-state `StatusBadge`, and (3) the extended drill-param whitelist.

Purpose: The settling state machine (D-03) is the single most error-prone piece of this phase (off-by-one on the D+1 boundary, and Pitfall 1 — settling from wall-clock instead of data). Extracting it as a test-first pure TS helper catches boundary bugs before they are hand-translated into SQL, and gives the phase its one mechanically-testable artifact. The badge + whitelist are the shared primitives both later slices consume.
Output: `reconciliation-status.ts` (+test), `status-badge.tsx`, extended `drill-params.ts`. No new views or pages yet — this is the foundation both Wave 2/3 slices depend on.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-reconciliation-discrepancy-flagging/04-CONTEXT.md
@.planning/phases/04-reconciliation-discrepancy-flagging/04-RESEARCH.md
@.planning/phases/04-reconciliation-discrepancy-flagging/04-PATTERNS.md
@.planning/phases/04-reconciliation-discrepancy-flagging/04-UI-SPEC.md

<interfaces>
Current lib/dashboard/drill-params.ts contract (extend, do not rewrite):
```typescript
export type DrillEntity = "verification" | "revenue-tier" | "sla-breach";
const DRILL_ENTITIES: readonly DrillEntity[] = ["verification", "revenue-tier", "sla-breach"];
```
`parseDrillParams`/`serializeDrillParams` operate generically on DrillEntity — no other change is needed once the two new values are added to both the union and the array.

Brand semantic tokens (app/globals.css lines 55-64): --success #0a7a4b / --success-bg / --success-border; --warning #8a6d00 / --warning-bg / --warning-border; --error #c1121f / --error-bg / --error-border. `--destructive` aliases `--error`.

Existing StatusBadge precedent to mirror: components/upload/uploads-history-table.tsx (inline private StatusBadge). RESEARCH.md §Code Examples gives the exact 3-state extended version to copy verbatim.

Existing test conventions: co-located under lib/dashboard/__tests__/ (see drill-params.test.ts, bucketing.test.ts). No vitest.config file — `npm test` (vitest run) resolves by default. Run one file with `npx vitest run <path>`.
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Pure reconciliation status state machine (RED→GREEN)</name>
  <files>lib/dashboard/reconciliation-status.ts, lib/dashboard/__tests__/reconciliation-status.test.ts</files>
  <read_first>
    - lib/dashboard/__tests__/drill-params.test.ts (test file structure, describe/it/expect style, vitest import)
    - lib/dashboard/bucketing.ts (pure-helper module style to mirror — small, no I/O, named exports)
    - .planning/phases/04-reconciliation-discrepancy-flagging/04-RESEARCH.md §"Pattern 1" and §"Pitfall 1" and §"Validation Architecture → Wave 0 Gaps" (the exact case logic and the settled-from-data rule)
    - .planning/phases/04-reconciliation-discrepancy-flagging/04-CONTEXT.md D-02/D-03/D-04
  </read_first>
  <behavior>
    - `computeReconciliationStatus(billingCount, verificationCount, settled)` returns `'ok'` when billingCount === verificationCount (regardless of settled).
    - Returns `'needs_review'` when counts differ AND settled === false (D-03 pending — the most-recent unsettled day).
    - Returns `'mismatch'` when counts differ AND settled === true (settled, unresolved).
    - A `computeShortSide(billingCount, verificationCount)` returns `null` when equal, `'billing'` when billing < verification, `'verification'` when verification < billing.
    - A `reconciliationStatusToBadge(status)` maps `'ok'|'needs_review'|'mismatch'` to a `{ label, variant }` object where labels are exactly `OK` / `Needs review` / `Mismatch` (UI-SPEC copywriting) and variant is a stable enum consumed by StatusBadge.
    - Edge: equal counts of 0 → 'ok'. Boundary: counts differ by exactly 1 → non-ok per settled flag.
  </behavior>
  <action>Write the test file FIRST (RED) covering every row of the truth table above (equal/unequal × settled/unsettled, plus the zero-equal and delta-of-1 boundaries and all three short_side branches), run it and confirm it fails, then implement `lib/dashboard/reconciliation-status.ts` as pure named exports (`computeReconciliationStatus`, `computeShortSide`, `reconciliationStatusToBadge`, plus a `ReconciliationStatus = 'ok' | 'needs_review' | 'mismatch'` exported type and a `ShortSide = 'billing' | 'verification' | null` type) with no imports of Supabase/DOM/network. Document in a header comment that the SQL `case` expressions in migrations 0018/0019/0020 MUST mirror this logic exactly (per RESEARCH Wave-0 rationale: write test first, hand-translate to SQL). Do NOT compute `settled` here from any clock — `settled` is an input the SQL view supplies from max(day_utc); this helper is clock-free by construction (Pitfall 1).</action>
  <verify>
    <automated>npx vitest run lib/dashboard/__tests__/reconciliation-status.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run lib/dashboard/__tests__/reconciliation-status.test.ts` passes with tests covering all three status branches, the short_side branches, and the delta-of-1 + zero-equal boundaries.
    - `grep -n "current_date\|Date.now\|new Date()" lib/dashboard/reconciliation-status.ts` returns no matches (clock-free, Pitfall 1).
    - Module exports `computeReconciliationStatus`, `computeShortSide`, `reconciliationStatusToBadge`, `ReconciliationStatus`, `ShortSide`.
  </acceptance_criteria>
  <done>Pure status helper exists, is clock-free, and its unit tests pass covering the full settling truth table.</done>
</task>

<task type="auto">
  <name>Task 2: Shared StatusBadge + drill whitelist extension</name>
  <files>components/dashboard/status-badge.tsx, lib/dashboard/drill-params.ts</files>
  <read_first>
    - components/upload/uploads-history-table.tsx (existing inline StatusBadge — the pattern to extract/extend to a 3rd state)
    - components/dashboard/sla-breach-table.tsx (a 'use client' dashboard component's import style for @/components/ui/badge)
    - lib/dashboard/drill-params.ts (full file — extend only the union + DRILL_ENTITIES array + header comment)
    - lib/dashboard/__tests__/drill-params.test.ts (confirm existing tests still describe accepted/rejected entities)
    - .planning/phases/04-reconciliation-discrepancy-flagging/04-UI-SPEC.md §Color (binding: badge colour is a pure function of the status enum, never inferred from delta sign)
    - .planning/phases/04-reconciliation-discrepancy-flagging/04-RESEARCH.md §"Status badge pattern" and §"Extending the drill whitelist"
  </read_first>
  <action>Create `components/dashboard/status-badge.tsx` as a `'use client'` module exporting `StatusBadge({ status }: { status: ReconciliationStatus })` (import the `ReconciliationStatus` type from `@/lib/dashboard/reconciliation-status`). Render three outline `Badge` variants using the exact brand-token classNames from RESEARCH §"Status badge pattern": OK → border/bg/text `var(--success)`; needs_review → `var(--warning)`; mismatch → `border-destructive/30 bg-destructive/10 text-destructive`. Labels exactly `OK` / `Needs review` / `Mismatch`. Colour is switched only on the `status` enum — never on delta sign/magnitude (UI-SPEC binding rule). Then EDIT `lib/dashboard/drill-params.ts`: add `"recon-billing"` and `"recon-inventory"` to both the `DrillEntity` union type and the `DRILL_ENTITIES` runtime array (per RESEARCH exact diff), and update the file header comment's forward-reference (currently mentions a single "discrepancy" entity) to name the two actual entities. Change nothing else — the generic `date` field and `parseDrillParams` whitelist logic already handle the new entities.</action>
  <verify>
    <automated>npx vitest run lib/dashboard/__tests__/drill-params.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "recon-billing\|recon-inventory" lib/dashboard/drill-params.ts` returns 4 (each value appears in both the union and the array).
    - `grep -v '^\s*//' components/dashboard/status-badge.tsx | grep -c "var(--success)\|var(--warning)\|destructive"` is ≥ 3.
    - `npx tsc --noEmit` passes (StatusBadge imports the ReconciliationStatus type cleanly).
    - Existing `drill-params.test.ts` still passes; no unknown-entity is newly accepted.
  </acceptance_criteria>
  <done>StatusBadge renders three brand-token states from the status enum only; drill whitelist accepts the two new recon entities; typecheck and existing drill-param tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL query string → Server Component → Supabase query | Untrusted `?drill=&date=` params cross into DB queries; parseDrillParams is the choke point |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | drill-params.ts whitelist | mitigate | New entities added to the SAME strict `DRILL_ENTITIES` whitelist; parseDrillParams still returns null for any unknown `drill` and drops malformed `date` (existing logic unchanged, only two values added). Verified by drill-params.test.ts. |
| T-04-02 | Information Disclosure | reconciliation-status.ts | accept | Pure clock-free logic module, no data access, no secrets. No exposure surface. |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No new packages this phase (RESEARCH §Package Legitimacy Audit: not applicable). No install task; nothing to vet. |
</threat_model>

<verification>
- `npm test` (full vitest suite) green including the new reconciliation-status tests and the existing drill-params tests.
- `npx tsc --noEmit` passes.
- No wall-clock references in the status helper (grep gate above).
</verification>

<success_criteria>
The settling state machine is expressed once, as tested pure TS, ready to be hand-translated into the SQL views (Wave 2/3); the shared 3-state badge and extended drill whitelist exist and typecheck, unblocking both reconciliation vertical slices.
</success_criteria>

<output>
Create `.planning/phases/04-reconciliation-discrepancy-flagging/04-01-SUMMARY.md` when done.
</output>
