---
phase: 03-revenue-sla-drill-down
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0011_pricing_tiers.sql
  - lib/pricing/schema.ts
  - lib/pricing/__tests__/schema.test.ts
autonomous: true
requirements: [ADMIN-01, DATA-03]
must_haves:
  truths:
    - "A migration creates pricing_tier_sets, pricing_tiers, pricing_tier_audit with exact-NUMERIC rate columns (no float/real anywhere)"
    - "pricing_tier_sets and pricing_tiers have BOTH a select-to-authenticated AND an insert-to-authenticated RLS policy (the codebase's first authenticated-insert policy)"
    - "pricing_tier_audit is append-only to clients: select-to-authenticated only, no insert/update/delete policy; a SECURITY DEFINER trigger writes its rows attributing auth.uid()"
    - "The Zod tier-set schema rejects empty tiers, non-ascending/overlapping upper bounds, and a non-final NULL upper bound"
  artifacts:
    - path: "supabase/migrations/0011_pricing_tiers.sql"
      provides: "pricing tier schema + RLS + audit trigger"
      contains: "create table pricing_tier_sets"
    - path: "lib/pricing/schema.ts"
      provides: "pricingTierSetSchema + PricingTierSetInput type (single source of truth for client + server validation)"
      exports: ["pricingTierSetSchema", "PricingTierSetInput"]
    - path: "lib/pricing/__tests__/schema.test.ts"
      provides: "contiguity/ordering/empty validation proof"
  key_links:
    - from: "supabase/migrations/0011_pricing_tiers.sql (trigger)"
      to: "pricing_tier_audit"
      via: "AFTER INSERT trigger writing changed_by = auth.uid()"
      pattern: "auth.uid"
    - from: "lib/pricing/schema.ts"
      to: "tier contiguity rule"
      via: "superRefine"
      pattern: "superRefine"
---

<objective>
Lay the pricing-tier data foundation: three tables (versioned date-effective tier sets, ordered tier rows, append-only audit), the codebase's first authenticated-INSERT RLS policy, an auth.uid()-attributing audit trigger, and the single Zod schema that both the client form and the server action will validate against.

Purpose: This is the exact-NUMERIC, auditable storage layer that ADMIN-01 (in-app configurable pricing) and REV-01 (revenue from tiered pricing) both stand on. Getting the money columns as NUMERIC and the validation contract exact here prevents float error and mis-priced gap/overlap regions downstream.
Output: `0011_pricing_tiers.sql` migration + `lib/pricing/schema.ts` + passing schema unit tests. (Migration is pushed to the live DB in plan 03-07, the [BLOCKING] gate.)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-revenue-sla-drill-down/03-CONTEXT.md
@.planning/phases/03-revenue-sla-drill-down/03-RESEARCH.md
@.planning/phases/03-revenue-sla-drill-down/03-PATTERNS.md
@supabase/migrations/0002_verifications.sql
@supabase/migrations/0004_rls_and_storage.sql
@.planning/PROJECT.md

<interfaces>
Reset-window versioning decision (resolves RESEARCH Assumption A4 / D-01): `reset_window`
is versioned TOGETHER with the tier set on `pricing_tier_sets` (one column on that table),
NOT a separate always-current global setting. Rationale: D-04 treats every pricing change as
date-effective auditable history; a reset-cadence change is a rate-change-shaped event and must
be date-effective the same way. State this rationale in the migration's leading comment block.

Zod schema public surface the pricing form (03-02) and server action (03-02) both import:
  export const pricingTierSetSchema: ZodType   // validated on BOTH client and server
  export type PricingTierSetInput = z.infer<typeof pricingTierSetSchema>
  // shape: { effectiveFrom: string (ISO date), resetWindow: 'monthly'|'quarterly'|'none',
  //          tiers: { upperBound: number|null, rate: number }[] }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 0011 — pricing tables, first authenticated-INSERT RLS, append-only audit trigger</name>
  <files>supabase/migrations/0011_pricing_tiers.sql</files>
  <read_first>
    - supabase/migrations/0002_verifications.sql (leading-comment-block convention, `generated always as identity`, inline check constraints)
    - supabase/migrations/0004_rls_and_storage.sql (the existing `for select to authenticated using (true)` RLS shape — and its comment that there is deliberately NO authenticated insert policy; you are adding the FIRST one)
    - .planning/phases/03-revenue-sla-drill-down/03-RESEARCH.md (§Tiered Revenue SQL Pattern schema; §Pattern 4 write-path RLS note; Security Domain Repudiation row)
    - .planning/phases/03-revenue-sla-drill-down/03-PATTERNS.md (0011 pattern assignment — the exact table bodies and the RLS divergence to flag)
  </read_first>
  <action>
    Create `supabase/migrations/0011_pricing_tiers.sql` with a leading comment block (mirroring 0002's style) that states: this is the first authenticated-INSERT RLS in the codebase (D-06 no-RBAC: any logged-in user may edit pricing), reset_window is versioned on pricing_tier_sets per D-01/A4, and all rate math stays NUMERIC per L-01/DATA-03.

    Tables (exact names/columns): `pricing_tier_sets` (id uuid primary key default gen_random_uuid(), effective_from date not null, reset_window text not null check (reset_window in ('monthly','quarterly','none')), created_by uuid references auth.users(id) default auth.uid(), created_at timestamptz not null default now(), constraint pricing_tier_sets_effective_from_key unique (effective_from)). `pricing_tiers` (id bigint generated always as identity primary key, tier_set_id uuid not null references pricing_tier_sets(id) on delete cascade, tier_order int not null, upper_bound bigint, rate numeric(12,4) not null check (rate >= 0), constraint pricing_tiers_unique_order unique (tier_set_id, tier_order)). `pricing_tier_audit` (id bigint generated always as identity primary key, tier_set_id uuid not null references pricing_tier_sets(id), changed_by uuid references auth.users(id), changed_at timestamptz not null default now(), summary text not null). Do NOT use float/real/double precision anywhere.

    RLS: enable row level security on all three. On pricing_tier_sets and pricing_tiers add `..._select_authenticated` (for select to authenticated using (true)) AND `..._insert_authenticated` (for insert to authenticated with check (true)) — the new shape. On pricing_tier_audit add ONLY the select-to-authenticated policy (no insert/update/delete policy — clients cannot write or tamper).

    Audit trigger: a `security definer` function `fn_pricing_tier_sets_audit()` that on AFTER INSERT on pricing_tier_sets inserts into pricing_tier_audit (tier_set_id = NEW.id, changed_by = auth.uid(), summary = 'New pricing tier set effective ' || NEW.effective_from::text || ', reset window ' || NEW.reset_window). Attach it as `trg_pricing_tier_sets_audit`. Because pricing_tier_audit has no client insert policy, the trigger MUST be security definer so it writes as the table owner (this is what makes the audit unforgeable/undeletable by clients — D-06 + Repudiation mitigation). Note in a comment: rich per-tier-rate diff copy (UI-SPEC example sentence) is deferred to a later enhancement per RESEARCH Open Question 3; the audit here is tier-set-granular (who / when / which effective set), which satisfies D-06's attributed-and-timestamped bar.
  </action>
  <verify>
    <automated>bash -c "f=supabase/migrations/0011_pricing_tiers.sql; b=\$(grep -v '^--' \$f); echo \"\$b\" | grep -q 'create table pricing_tier_sets' && echo \"\$b\" | grep -q 'create table pricing_tiers' && echo \"\$b\" | grep -q 'create table pricing_tier_audit' && echo \"\$b\" | grep -q 'rate *numeric(12,4)' && echo \"\$b\" | grep -q 'for insert to authenticated' && echo \"\$b\" | grep -q 'security definer' && echo \"\$b\" | grep -q 'auth.uid' && ! echo \"\$b\" | grep -Eiq '(float|real|double precision)' && echo OK"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: file contains `create table pricing_tier_sets`, `create table pricing_tiers`, `create table pricing_tier_audit`
    - Source assertion: `rate numeric(12,4)`; no `float`/`real`/`double precision` token appears in non-comment SQL
    - Source assertion: at least one `for insert to authenticated ... with check (true)` policy exists (first in codebase)
    - Source assertion: pricing_tier_audit has a select policy but NO insert/update/delete policy for authenticated
    - Source assertion: a `security definer` trigger function references `auth.uid()` and inserts into pricing_tier_audit
  </acceptance_criteria>
  <done>The pricing schema, RLS, and unforgeable audit trigger are authored and structurally verified, ready for the plan 03-07 push.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Zod tier-set schema with contiguity validation + unit tests</name>
  <files>lib/pricing/schema.ts, lib/pricing/__tests__/schema.test.ts</files>
  <behavior>
    - Rejects an empty tiers array with message "Add at least one tier before saving."
    - Rejects tiers whose upper_bound values are not strictly ascending (overlap/gap) with message "Tiers must be contiguous and in ascending order — check the thresholds and try again."
    - Rejects a NULL upper_bound on any tier that is not the last tier
    - Accepts a valid 2-tier set: [{upperBound: 500000, rate: 0.08}, {upperBound: null, rate: 0.09}], resetWindow 'monthly', effectiveFrom '2026-08-13'
    - Rejects a negative rate
  </behavior>
  <read_first>
    - .planning/phases/03-revenue-sla-drill-down/03-PATTERNS.md (lib/pricing/schema.ts required shape; the two exact validation-error copy strings)
    - .planning/phases/03-revenue-sla-drill-down/03-RESEARCH.md (Pitfall 5 — the contiguity gap the SQL cannot self-detect; both client and server must enforce)
    - .planning/phases/03-revenue-sla-drill-down/03-UI-SPEC.md (Copywriting Contract — exact validation error copy)
  </read_first>
  <action>
    Create `lib/pricing/schema.ts` exporting `pricingTierSetSchema` and inferred type `PricingTierSetInput`. Object shape: effectiveFrom (z.string() ISO date), resetWindow (z.enum(["monthly","quarterly","none"])), tiers (z.array of { upperBound: z.number().int().positive().nullable(), rate: z.number().nonnegative() }).min(1, "Add at least one tier before saving."). Add a `.superRefine` that enforces: only the LAST tier may have upperBound === null; all non-null upperBound values are strictly increasing in array order (no equal, no descending → catches overlap and gap); on violation `ctx.addIssue` with exactly "Tiers must be contiguous and in ascending order — check the thresholds and try again." Write these copy strings verbatim from the UI-SPEC. Then write `lib/pricing/__tests__/schema.test.ts` covering every case in <behavior> using vitest (assert `safeParse().success` and the exact error messages).
  </action>
  <verify>
    <automated>npx vitest run lib/pricing/__tests__/schema.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test command: `npx vitest run lib/pricing/__tests__/schema.test.ts` exits 0 with all cases from <behavior> present and passing
    - Source assertion: schema.ts contains the exact strings "Add at least one tier before saving." and "Tiers must be contiguous and in ascending order — check the thresholds and try again."
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>The single validation contract exists and provably rejects the gap/overlap/empty cases the marginal-bracket SQL cannot self-detect.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → pricing tables | Any authenticated user may now INSERT pricing config (first write-enabled table set) |
| trigger → audit table | Only the SECURITY DEFINER trigger may write audit rows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | pricing_tiers.rate stored as float | mitigate | `rate numeric(12,4)`; automated grep forbids float/real/double precision in the migration (DATA-03/L-01) |
| T-03-02 | Repudiation | User edits/deletes their own audit entries to hide a change | mitigate | pricing_tier_audit has NO client insert/update/delete policy; only the SECURITY DEFINER trigger writes it, stamping auth.uid() (D-06) |
| T-03-03 | Elevation of Privilege | Unauthenticated/anon write to pricing tables | mitigate | RLS insert policy scoped `to authenticated with check (true)` — anon role has no policy, so denied |
| T-03-04 | Tampering | Overlapping/gapped tiers silently mis-price the gap region | mitigate | Zod superRefine strict-ascending contiguity rule, enforced client + server (03-02); Pitfall 5 |
</threat_model>

<verification>
- `npx vitest run lib/pricing/__tests__/schema.test.ts` green
- Structural grep of 0011 confirms tables, NUMERIC rate, authenticated-insert policy, audit-select-only, security-definer trigger
- `npx tsc --noEmit` clean
- (Live table existence + RLS advisor check happens in plan 03-07 after the push)
</verification>

<success_criteria>
- pricing_tier_sets / pricing_tiers / pricing_tier_audit defined with NUMERIC rate and no float anywhere
- First authenticated-INSERT RLS policy present; audit table client-immutable with a SECURITY DEFINER auth.uid() trigger
- Zod schema rejects empty/overlap/gap/misplaced-null-bound and is the shared client+server contract
</success_criteria>

<output>
Create `.planning/phases/03-revenue-sla-drill-down/03-01-SUMMARY.md` when done
</output>
