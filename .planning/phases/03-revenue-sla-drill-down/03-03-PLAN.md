---
phase: 03-revenue-sla-drill-down
plan: 03
type: tdd
wave: 2
depends_on: ["03-01"]
files_modified:
  - supabase/migrations/0012_v_revenue.sql
  - supabase/tests/revenue_boundary_test.sql
autonomous: true
requirements: [REV-01, DATA-03, REV-02]
must_haves:
  truths:
    - "Daily revenue is computed with marginal-bracket math (GREATEST/LEAST overlap clamps) in exact NUMERIC — never a single rate applied to the whole volume"
    - "Each day uses the tier set effective on that day (D-04); the reset-window boundary is fixed by the reset_window value at the start of each window instance"
    - "ALL verifications count toward revenue and tier counters regardless of authenticated (D-02) — the base count view has no authenticated filter"
    - "A boundary-crossing hand-calc (500,000 boundary, 2,500-count day: $80.00 + $135.00 = $215.00) is encoded as an executable SQL assertion"
    - "Every view carries security_invoker = on and the created_at >= 2026-08-13 cutoff"
  artifacts:
    - path: "supabase/migrations/0012_v_revenue.sql"
      provides: "v_revenue_daily_counts, v_revenue_tier_set_by_day, v_revenue_daily, v_revenue_by_tier"
      contains: "security_invoker = on"
    - path: "supabase/tests/revenue_boundary_test.sql"
      provides: "executable boundary hand-calc assertion (215.0000 / 80.0000 / 135.0000)"
      contains: "215"
  key_links:
    - from: "v_revenue_daily"
      to: "pricing_tiers"
      via: "LATERAL join applying GREATEST/LEAST marginal overlap"
      pattern: "GREATEST"
    - from: "v_revenue_tier_set_by_day"
      to: "pricing_tier_sets"
      via: "cross join lateral latest effective_from <= day"
      pattern: "effective_from"
---

<objective>
Author the revenue computation as a chain of exact-NUMERIC Postgres views over `verifications` and the date-effective pricing tier sets, implementing marginal-bracket math (the single hardest correctness bar of the phase), plus an executable SQL boundary test that proves the math matches a hand calculation exactly where brackets change.

Purpose: REV-01 (tiered revenue) + DATA-03 (exact NUMERIC) + REV-02 (recompute = re-evaluate against versioned tier sets, no re-ingestion). This is the trustworthy-revenue centrepiece — the number must be reproducible and correct to the cent at tier boundaries.
Output: `0012_v_revenue.sql` + `revenue_boundary_test.sql`. (Views are applied to the live DB and the boundary test executed authoritatively in plan 03-07.)

TDD note: the correctness spec is the boundary hand-calc. Write `revenue_boundary_test.sql` FIRST as the failing assertion, then build the view chain until it would pass. Because a local Supabase stack may not be available, the authoritative RED→GREEN execution against a real Postgres happens in plan 03-07 (seed + assert); this plan proves the SQL is structurally correct and the test fixture encodes the exact expected values.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-revenue-sla-drill-down/03-RESEARCH.md
@.planning/phases/03-revenue-sla-drill-down/03-PATTERNS.md
@.planning/phases/03-revenue-sla-drill-down/03-CONTEXT.md
@supabase/migrations/0002_verifications.sql
@supabase/migrations/0005_review_fixes_excluded_and_utc_view.sql
@supabase/migrations/0011_pricing_tiers.sql

<interfaces>
From 0011 (built in 03-01):
  pricing_tier_sets(id uuid, effective_from date, reset_window text)   -- reset_window versioned WITH the set (A4/D-01)
  pricing_tiers(tier_set_id uuid, tier_order int, upper_bound bigint NULL=open, rate numeric(12,4))

From 0002:
  verifications(created_at timestamptz, duration_ms numeric, authenticated boolean, external_card_reference text, cvi2_value int, ...)

View row shapes this plan produces (consumed by revenue page 03-04, supabase-js returns numeric as STRING):
  v_revenue_daily(day_utc timestamp, revenue numeric)             -- grand daily total
  v_revenue_by_tier(day_utc timestamp, tier_order int, tier_revenue numeric, tier_set_id uuid)  -- D-07 breakdown
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 (RED): Boundary hand-calc test fixture + base/tier-resolution views</name>
  <files>supabase/tests/revenue_boundary_test.sql, supabase/migrations/0012_v_revenue.sql</files>
  <read_first>
    - .planning/phases/03-revenue-sla-drill-down/03-RESEARCH.md (§Tiered Revenue SQL Pattern — the verified worked example: 2 tiers first 500,000 @ 0.0800 / next @ 0.0900, a 2,500-count day crossing 499,000→501,500 → tier1 1000*0.08=80.00, tier2 1500*0.09=135.00, day total 215.00)
    - supabase/migrations/0005_review_fixes_excluded_and_utc_view.sql (the security_invoker + UTC-bucket + cutoff template to copy verbatim)
    - .planning/phases/03-revenue-sla-drill-down/03-PATTERNS.md (0012 pattern — use 0005 as the view template; NUMERIC end-to-end)
  </read_first>
  <action>
    First write supabase/tests/revenue_boundary_test.sql: a self-contained script that (in a transaction it rolls back) inserts a pricing_tier_sets row (effective_from 2026-08-13, reset_window 'monthly') with two pricing_tiers (tier_order 0 upper_bound 500000 rate 0.0800; tier_order 1 upper_bound NULL rate 0.0900), inserts verifications so cumulative count crosses 499,000→501,500 within one UTC day inside August 2026, then asserts (via a DO block raising an exception on mismatch) that v_revenue_daily.revenue for that day = 215.0000 and v_revenue_by_tier yields 80.0000 (tier_order 0) and 135.0000 (tier_order 1). Use exact NUMERIC literals.
    Then begin 0012_v_revenue.sql (leading comment block per project convention). Create the base views: v_revenue_daily_counts (with security_invoker = on) = date_trunc('day', created_at at time zone 'UTC') as day_utc, count(*) as verification_count from verifications where created_at >= '2026-08-13T00:00:00Z' group by 1 — CRITICALLY no authenticated filter (D-02: all verifications count). Create v_revenue_tier_set_by_day (security_invoker = on): for each day, cross join lateral the latest pricing_tier_sets where effective_from <= day_utc::date order by effective_from desc limit 1, exposing tier_set_id + reset_window (D-04).
  </action>
  <verify>
    <automated>bash -c "grep -q '215' supabase/tests/revenue_boundary_test.sql && grep -q '135' supabase/tests/revenue_boundary_test.sql && grep -q 'v_revenue_daily_counts' supabase/migrations/0012_v_revenue.sql && grep -q 'v_revenue_tier_set_by_day' supabase/migrations/0012_v_revenue.sql && grep -cq 'security_invoker = on' supabase/migrations/0012_v_revenue.sql && grep -q \"created_at >= '2026-08-13\" supabase/migrations/0012_v_revenue.sql && ! grep -q 'filter (where authenticated' supabase/migrations/0012_v_revenue.sql && echo OK"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: test fixture asserts 215.0000 grand total and 80.0000 / 135.0000 per-tier values
    - Source assertion: v_revenue_daily_counts and v_revenue_tier_set_by_day exist with `security_invoker = on` and the 2026-08-13 cutoff
    - Source assertion: v_revenue_daily_counts does NOT filter by authenticated (D-02)
  </acceptance_criteria>
  <done>The failing boundary spec exists and the base count + tier-set-resolution views are in place, all RLS-safe and cutoff-correct.</done>
</task>

<task type="auto">
  <name>Task 2 (GREEN): Marginal-bracket revenue views + reset-window running count</name>
  <files>supabase/migrations/0012_v_revenue.sql</files>
  <read_first>
    - .planning/phases/03-revenue-sla-drill-down/03-RESEARCH.md (§Tiered Revenue SQL Pattern — the GREATEST/LEAST overlap formula and the recommended CTE chain; §Common Pitfalls 1 & 2; the "Open design question" on reset-window-at-window-start)
    - .planning/phases/03-revenue-sla-drill-down/03-CONTEXT.md (D-01 reset window setting; discretion note on window-vs-tier-set boundary — safe default already chosen)
    - supabase/migrations/0012_v_revenue.sql (the base views from Task 1 to build on)
  </read_first>
  <action>
    Extend 0012_v_revenue.sql. (1) Compute window_start per day from reset_window (resolved decisions): 'monthly' -> date_trunc('month', day_utc); 'quarterly' -> date_trunc('quarter', day_utc) (calendar quarters, RESEARCH Open Q1 default); 'none' -> a fixed epoch of 2026-08-13. Fix the window boundary using the reset_window value effective at the START of that window instance (hold constant across a mid-window tier-set change; the per-day RATE still comes from the day's effective tier set per D-04). (2) Compute, per day, c_before = running SUM of verification_count over the reset-window partition ordered by day, ROWS BETWEEN UNBOUNDED PRECEDING AND 1 PRECEDING (i.e. count accumulated before this day within the window), and c_after = c_before + this day's count. (3) v_revenue_by_tier (security_invoker = on): LATERAL join each day to its tier set's pricing_tiers, computing lower_bound as the previous tier's upper_bound (0 for tier_order 0) and overlap = GREATEST(0, LEAST(c_after, COALESCE(upper_bound, 'infinity'::numeric)) - GREATEST(c_before, lower_bound)); tier_revenue = overlap * rate (NUMERIC * NUMERIC). (4) v_revenue_daily (security_invoker = on) = SUM(tier_revenue) per day as revenue (do the grand SUM in SQL, not JS — Pitfall 2). All arithmetic stays NUMERIC; never introduce float/real. Add view comments explaining the marginal-bracket intent and the anti-pattern it prevents (never apply the reached tier's rate to the whole volume).
  </action>
  <verify>
    <automated>bash -c "b=\$(grep -v '^--' supabase/migrations/0012_v_revenue.sql); echo \"\$b\" | grep -q 'GREATEST' && echo \"\$b\" | grep -q 'LEAST' && echo \"\$b\" | grep -q 'v_revenue_by_tier' && echo \"\$b\" | grep -q 'v_revenue_daily' && echo \"\$b\" | grep -q 'date_trunc(.quarter' && echo \"\$b\" | grep -q 'rows between unbounded preceding' && ! echo \"\$b\" | grep -Eiq '(float|real|double precision)' && echo OK"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: v_revenue_by_tier uses GREATEST/LEAST overlap clamps and a per-tier lower/upper bound
    - Source assertion: a windowed running SUM (ROWS BETWEEN UNBOUNDED PRECEDING AND 1 PRECEDING) computes c_before within the reset-window partition
    - Source assertion: window_start handles monthly, quarterly (calendar), and none
    - Source assertion: no float/real/double precision anywhere; v_revenue_daily sums tier_revenue in SQL
    - `revenue_boundary_test.sql` encodes the 215.0000 / 80.0000 / 135.0000 expectations (executed authoritatively in 03-07)
  </acceptance_criteria>
  <done>Marginal-bracket revenue is fully expressed in exact NUMERIC SQL, structurally matching the boundary hand-calc; ready for the live boundary assertion in 03-07.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| session client → revenue views | Reads inherit verifications RLS via security_invoker |
| admin-supplied tier config → revenue math | Bad tier bounds could mis-price if not validated (handled in 03-01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-09 | Tampering | Float rounding error in the revenue chain | mitigate | Entire chain is NUMERIC; grep gate forbids float/real; grand total summed in SQL (Pitfall 2) |
| T-03-10 | Tampering | Reached-tier rate applied to whole volume (overstates revenue) | mitigate | GREATEST/LEAST marginal-overlap views (Pitfall 1); boundary test asserts 215.00 not the naive figure |
| T-03-11 | Information Disclosure | New view bypasses RLS (owner privileges) | mitigate | `security_invoker = on` on every view (Pitfall 4), copied from 0005 |
| T-03-12 | Tampering | Overlap/gap tiers mis-price a region | transfer | Contiguity enforced upstream by Zod (03-01, T-03-04) on client + server before rows ever reach these views |
</threat_model>

<verification>
- Grep gates confirm GREATEST/LEAST overlap, windowed running count, security_invoker on all views, no-authenticated-filter base view, no float, 2026-08-13 cutoff, quarterly calendar handling
- Boundary test fixture encodes 215.0000 / 80.0000 / 135.0000
- Authoritative live execution of revenue_boundary_test.sql happens in plan 03-07 after the push (must return the exact values)
</verification>

<success_criteria>
- v_revenue_daily and v_revenue_by_tier compute exact marginal-bracket revenue in NUMERIC, per-day against the date-effective tier set, all verifications counted (D-02)
- Reset-window boundaries fixed at window start; quarterly = calendar quarter
- Executable boundary hand-calc test committed, asserting the $215.00 worked example
</success_criteria>

<output>
Create `.planning/phases/03-revenue-sla-drill-down/03-03-SUMMARY.md` when done
</output>
