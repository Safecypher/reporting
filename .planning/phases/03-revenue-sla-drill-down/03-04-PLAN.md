---
phase: 03-revenue-sla-drill-down
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0013_v_sla_daily.sql
  - lib/dashboard/bucketing.ts
  - lib/dashboard/sla-bucketing.ts
  - lib/dashboard/__tests__/sla-bucketing.test.ts
  - components/dashboard/sla-chart.tsx
  - components/dashboard/sla-view-controls.tsx
  - components/dashboard/sla-breach-table.tsx
  - app/(dashboard)/sla/page.tsx
autonomous: true
requirements: [SLA-01]
must_haves:
  truths:
    - "The SLA view shows average verification duration over time with a dashed 750ms ReferenceLine; points above 750ms render in --error, not Cypher Blue (D-08)"
    - "A breach table enumerates individual breaching verifications (time, card ref, duration) using TanStack Table v8 (D-08 drillable breach table)"
    - "The view reuses the granularity + timezone toggle UX (D-09) and the freshness badge/4-state contract"
    - "A populated-but-zero-breaches state reads as good news ('No SLA breaches in this period.'), not as an error"
    - "SLA is verifications-only, average/trend (L-04); no p95/max"
  artifacts:
    - path: "supabase/migrations/0013_v_sla_daily.sql"
      provides: "v_sla_daily (avg duration + breach count) and v_sla_breaches (individual breaching rows)"
      contains: "security_invoker = on"
    - path: "components/dashboard/sla-chart.tsx"
      provides: "Recharts LineChart + ReferenceLine(750) + BreachAwareDot"
      contains: "ReferenceLine"
    - path: "app/(dashboard)/sla/page.tsx"
      provides: "SLA view Server Component (4-state + good-news-no-breaches)"
  key_links:
    - from: "app/(dashboard)/sla/page.tsx"
      to: "v_sla_daily / v_sla_breaches"
      via: "session-scoped supabase.from().select()"
      pattern: "v_sla_daily"
    - from: "components/dashboard/sla-chart.tsx"
      to: "750ms threshold"
      via: "ReferenceLine y=750 + conditional dot colour"
      pattern: "750"
---

<objective>
Deliver the SLA view end-to-end: a Postgres view for average duration + breach count (and one for individual breaching rows), a Recharts trend chart with a 750ms reference line and breach-coloured points, a drillable breach table, and the page wired with the reused granularity/timezone controls and the 4-state (plus good-news-no-breaches) contract.

Purpose: SLA-01 — verification response-time trend against the 750ms SLA with breaching records highlighted (verifications only, trend-focused per L-04).
Output: /sla route + sla views. (Views applied to live DB in 03-07; page verifiable via type-check + structural gates now.)

This slice depends only on the existing `verifications` table, so it runs in Wave 1 in parallel with the pricing foundation. The Revenue/SLA/Pricing sidebar items are added by plan 03-02 (single owner of sidebar-nav.tsx) — this plan does NOT touch nav.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-revenue-sla-drill-down/03-RESEARCH.md
@.planning/phases/03-revenue-sla-drill-down/03-PATTERNS.md
@.planning/phases/03-revenue-sla-drill-down/03-UI-SPEC.md
@app/(dashboard)/verifications/page.tsx
@components/dashboard/verifications-chart.tsx
@components/dashboard/view-controls.tsx
@components/dashboard/kpi-cards.tsx
@lib/dashboard/bucketing.ts
@components/ui/table.tsx
@supabase/migrations/0005_review_fixes_excluded_and_utc_view.sql

<interfaces>
From lib/dashboard/bucketing.ts (existing; this plan EXPORTS two currently-private members for reuse — see Task 2):
  bucketKeyAndLabel(dayUtc, granularity, timeZone)  // make exported
  DATA_WINDOW_START = "2026-08-13"                    // make exported
  export type Granularity, BucketTimeZone

New SLA row/point shapes (supabase-js returns numeric as string):
  export type SlaDailyRow = { day_utc: string; avg_duration_ms: number; breach_count: number }
  export interface SlaBucketPoint { bucketKey: string; label: string; avgDurationMs: number; breachCount: number }
  // breach table row (from v_sla_breaches): { created_at: string; external_card_reference: string; duration_ms: number }

TanStack Table v8 (@tanstack/react-table ^8.21.3, already installed): useReactTable + getCoreRowModel + getSortedRowModel + flexRender. Do NOT run npm install (bare install pulls v9).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 0013 — v_sla_daily + v_sla_breaches</name>
  <files>supabase/migrations/0013_v_sla_daily.sql</files>
  <read_first>
    - supabase/migrations/0005_review_fixes_excluded_and_utc_view.sql (exact security_invoker + UTC-bucket + cutoff template; drop+create style)
    - supabase/migrations/0002_verifications.sql (duration_ms numeric column; external_card_reference; created_at)
    - .planning/phases/03-revenue-sla-drill-down/03-PATTERNS.md (0013 pattern — copy 0005; SLA computes avg + breach count)
    - .planning/phases/03-revenue-sla-drill-down/03-RESEARCH.md (SLA view section; L-04 average/trend only, no p95/max)
  </read_first>
  <action>
    Create 0013_v_sla_daily.sql with a leading comment block. v_sla_daily (with security_invoker = on): select date_trunc('day', created_at at time zone 'UTC') as day_utc, avg(duration_ms) as avg_duration_ms, count(*) filter (where duration_ms > 750) as breach_count from verifications where created_at >= '2026-08-13T00:00:00Z' group by 1 order by 1. v_sla_breaches (with security_invoker = on): select created_at, external_card_reference, duration_ms from verifications where created_at >= '2026-08-13T00:00:00Z' and duration_ms > 750 order by created_at — the individual breaching rows for the breach table + drill-down. Verifications only (L-04). No p95/max (deferred to SLA-02 v2). Keep duration_ms as numeric.
  </action>
  <verify>
    <automated>bash -c "b=\$(grep -v '^--' supabase/migrations/0013_v_sla_daily.sql); echo \"\$b\" | grep -q 'v_sla_daily' && echo \"\$b\" | grep -q 'v_sla_breaches' && echo \"\$b\" | grep -cq 'security_invoker = on' && echo \"\$b\" | grep -q 'duration_ms > 750' && echo \"\$b\" | grep -q \"created_at >= '2026-08-13\" && echo OK"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: both v_sla_daily and v_sla_breaches exist with `security_invoker = on` and the 2026-08-13 cutoff
    - Source assertion: breach threshold is `duration_ms > 750`; avg_duration_ms computed; no percentile/p95 function used
  </acceptance_criteria>
  <done>The SLA aggregate + breach-row views exist, RLS-safe and cutoff-correct.</done>
</task>

<task type="auto">
  <name>Task 2: SLA re-bucketing (export shared helper) + SLA chart + view-controls</name>
  <files>lib/dashboard/bucketing.ts, lib/dashboard/sla-bucketing.ts, lib/dashboard/__tests__/sla-bucketing.test.ts, components/dashboard/sla-chart.tsx, components/dashboard/sla-view-controls.tsx</files>
  <read_first>
    - lib/dashboard/bucketing.ts (bucketKeyAndLabel + DATA_WINDOW_START are currently module-private — export them; do NOT duplicate the function)
    - components/dashboard/view-controls.tsx (the toggle scaffold + session-only useState to mirror for SLA; D-09)
    - components/dashboard/verifications-chart.tsx (ChartContainer/chartConfig wiring to mirror)
    - .planning/phases/03-revenue-sla-drill-down/03-RESEARCH.md (Pattern 3 BreachAwareDot + ReferenceLine; flag as MEDIUM-confidence Recharts API — smoke-check)
    - .planning/phases/03-revenue-sla-drill-down/03-UI-SPEC.md (Color: reference line always --fg-3 dashed, breach dots always --error never Cypher Blue; chart is the SLA focal point)
  </read_first>
  <action>
    (a) In lib/dashboard/bucketing.ts add `export` to bucketKeyAndLabel and DATA_WINDOW_START (no logic change) so siblings reuse them. (b) Create lib/dashboard/sla-bucketing.ts with SlaDailyRow/SlaBucketPoint types and rebucketSla(rows, granularity, timeZone): reuse the exported bucketKeyAndLabel; per bucket compute breachCount = sum of breach_count and avgDurationMs = a duration-weighted average across days (sum(avg*count)/sum(count) if a per-day count is available, else mean of daily averages — document the choice). Defensively drop rows before DATA_WINDOW_START. (c) Write lib/dashboard/__tests__/sla-bucketing.test.ts covering daily passthrough, weekly/monthly grouping, and breach-count summation. (d) Create components/dashboard/sla-chart.tsx ("use client"): a Recharts LineChart over SlaBucketPoint keyed avgDurationMs, stroke --cypher-blue, with a BreachAwareDot (renders --error when payload.avgDurationMs > 750 else --cypher-blue) and a ReferenceLine y=750 stroke --fg-3 strokeDasharray "4 4" label "750ms SLA". (e) Create components/dashboard/sla-view-controls.tsx ("use client") mirroring ViewControls (granularity + timezone ToggleGroups, session-only useState, UTC default) but calling rebucketSla and rendering SlaChart + a breach-count KPI (reuse the .metric card treatment, label "Verifications over 750ms", plain count, accent=false — secondary per UI-SPEC).
  </action>
  <verify>
    <automated>bash -c "grep -q 'export.*bucketKeyAndLabel' lib/dashboard/bucketing.ts && grep -q 'export const DATA_WINDOW_START' lib/dashboard/bucketing.ts && grep -q 'rebucketSla' lib/dashboard/sla-bucketing.ts && grep -q 'ReferenceLine' components/dashboard/sla-chart.tsx && grep -q '750' components/dashboard/sla-chart.tsx && grep -q 'rebucketSla' components/dashboard/sla-view-controls.tsx && echo OK"</automated>
    <automated>npx vitest run lib/dashboard/__tests__/sla-bucketing.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: bucketKeyAndLabel and DATA_WINDOW_START are exported from bucketing.ts (no duplication of the bucketing logic)
    - Source assertion: sla-chart.tsx uses ReferenceLine at 750 with --fg-3 dashed and a breach dot in --error (never Cypher Blue for breach)
    - Test command: `npx vitest run lib/dashboard/__tests__/sla-bucketing.test.ts` exits 0
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>SLA data re-buckets through the reused helper and renders as a trend chart with the 750ms threshold and breach highlighting.</done>
</task>

<task type="auto">
  <name>Task 3: SLA page (4-state + good-news-no-breaches) + breach table (TanStack v8)</name>
  <files>app/(dashboard)/sla/page.tsx, components/dashboard/sla-breach-table.tsx</files>
  <read_first>
    - app/(dashboard)/verifications/page.tsx (4-state shell to copy: PageHeader/FreshnessBadge/EmptyState/ErrorState/LoadingState + Suspense body + Promise.all read + freshness query)
    - components/ui/table.tsx (shadcn Table primitives to wrap TanStack headless output)
    - components/dashboard/sla-view-controls.tsx (rendered when data present)
    - .planning/phases/03-revenue-sla-drill-down/03-UI-SPEC.md (Copywriting: SLA headings/empty/error copy; the good-news no-breach copy "No SLA breaches in this period."; breach table heading "Breaching verifications")
    - .planning/phases/03-revenue-sla-drill-down/03-PATTERNS.md (SLA page pattern; breach table is first TanStack consumer — flexRender + getCoreRowModel/getSortedRowModel)
  </read_first>
  <action>
    (a) Create app/(dashboard)/sla/page.tsx as a Server Component mirroring verifications/page.tsx: reuse PageHeader/FreshnessBadge/LoadingState/ErrorState/EmptyState (heading "SLA", DATA_WINDOW_CAPTION reused, copy from UI-SPEC). Async body reads v_sla_daily (order day_utc asc), v_sla_breaches, and the ingested_files freshness row via one Promise.all on the session createClient. On any query error render ErrorState (never a silent zero). If v_sla_daily is empty render EmptyState. If populated, render SlaViewControls with the daily rows AND the breach table; when breach rows are empty render the good-news micro-state "No SLA breaches in this period." inside the populated branch (a conditional render, NOT a new top-level state). (b) Create components/dashboard/sla-breach-table.tsx ("use client"): a TanStack Table v8 (useReactTable + getCoreRowModel + getSortedRowModel, flexRender) over the breach rows with columns time (created_at, formatted), card ref (external_card_reference), duration (duration_ms, mono/tabular, --error emphasis); heading "Breaching verifications"; wrap with the shadcn Table primitives. Rows are made drillable in plan 03-06 — leave a clean row-render seam.
  </action>
  <verify>
    <automated>bash -c "grep -q 'v_sla_daily' 'app/(dashboard)/sla/page.tsx' && grep -q 'v_sla_breaches' 'app/(dashboard)/sla/page.tsx' && grep -q 'No SLA breaches in this period' 'app/(dashboard)/sla/page.tsx' && grep -q 'useReactTable' components/dashboard/sla-breach-table.tsx && grep -q 'Breaching verifications' components/dashboard/sla-breach-table.tsx && echo OK"</automated>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: page reads v_sla_daily and v_sla_breaches and follows the 4-state contract
    - Source assertion: good-news copy "No SLA breaches in this period." is a conditional inside the populated branch
    - Source assertion: breach table uses useReactTable (TanStack v8) with heading "Breaching verifications"
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>The SLA page renders the trend chart, the 750ms reference line, and a drillable breach table with correct empty/good-news/error handling.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| session client → SLA views | Reads inherit verifications RLS via security_invoker |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-13 | Information Disclosure | New SLA view bypasses RLS (owner privileges) | mitigate | `security_invoker = on` on v_sla_daily and v_sla_breaches (Pitfall 4), copied from 0005 |
| T-03-14 | Tampering | Silent $0/zero on read failure hides a fault | mitigate | Query error renders ErrorState, never a silent zero (4-state contract) |
| T-03-15 | Denial of Service | Unbounded breach-row fetch on large data | accept | Data volumes are tiny (daily batch); breach set is a small subset (duration_ms > 750); revisit if volume grows (SLA-02 tail-latency trigger) |
</threat_model>

<verification>
- Grep gates confirm both SLA views (security_invoker + cutoff + 750 threshold), exported bucket helper, ReferenceLine at 750 with --fg-3/--error colours, TanStack breach table, good-news copy
- `npx vitest run lib/dashboard/__tests__/sla-bucketing.test.ts` green
- `npx tsc --noEmit` clean
- Live view existence confirmed in plan 03-07 after the push
</verification>

<success_criteria>
- /sla shows average duration vs a dashed 750ms line, breaches in --error, verifications only
- Drillable breach table enumerates individual breaching verifications
- Reuses granularity/timezone controls + freshness + 4-state, plus a good-news zero-breach state
</success_criteria>

<output>
Create `.planning/phases/03-revenue-sla-drill-down/03-04-SUMMARY.md` when done
</output>
