---
phase: 06-dual-source-alignment-tsys-vs-bit-addict
plan: 06
type: execute
wave: 5
depends_on: ["06-04", "06-05"]
files_modified:
  - types/db.ts
autonomous: true
requirements: [ALIGN-01, ALIGN-02, ALIGN-03, ALIGN-04, ALIGN-05, ALIGN-06, ALIGN-07]

estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 2
  confidence: low

user_setup:
  - service: supabase
    why: "Final authoritative schema gate. tsc, vitest and next build all pass WITHOUT the migrations being live, because types come from the generated types/db.ts and the unit tests are pure — so the phase would verify green while v_alignment_daily, v_alignment_live_cards_daily and the alignment settings columns do not exist. This task asserts all four migrations are actually applied and applies any that are not."
    dashboard_config:
      - task: "No dashboard configuration is required. The established push path in this environment is the Supabase MCP apply_migration tool (no CLI and no access token exist here); human involvement is needed only if the MCP connection itself is unavailable."
        location: "Supabase MCP server connection"

must_haves:
  truths:
    - "All four Phase 6 migrations (0027, 0029, 0030 and 0028) are applied to the linked Supabase project in ascending order, and types/db.ts is regenerated from the live schema as the final authoritative state"
    - "ROADMAP SC1 is demonstrated live: all four metrics show a TSYS figure and a Bit Addict figure side by side for the selected period, with absolute and percentage variance and which side is short"
    - "ROADMAP SC2 is demonstrated live: the TSYS side derives from endpoint_category, live cards is a cumulative enrol-minus-unenrol derivation, and the derivation is stated on the card face"
    - "ROADMAP SC3 is demonstrated live: every comparison carries an explicit aligned, needs-review or mismatch badge, and a missing report day reads as needs-review rather than as zero"
    - "ROADMAP SC4 is demonstrated live: a user can drill from either side of any comparison to the contributing rows and their originating source file"
    - "ROADMAP SC5 is demonstrated live: a day where the two sources genuinely disagree is visibly flagged on the home page without opening the reconciliation page"
    - "Every one of ALIGN-01 through ALIGN-07 is demonstrated against live infrastructure and recorded with observed values, not asserted"
    - "supabase/migrations/0020_v_apigee_cross_check.sql, 0018, 0019, 0021, 0022 and lib/dashboard/reconciliation-status.ts all still hash to their committed pre-Phase-6 values — Phase 6 was strictly additive to the reconciliation chain (D-01)"
    - "The full unit suite, a clean type-check and a production build are all green after the final type regeneration"
    - "Supabase security advisors report no new RLS-bypass or mutable-search-path warning for anything added in 0027 through 0030"
    - "Every function added by this phase holds no EXECUTE grant for anon, and every view added by this phase reports security_invoker"
    - "The 4-state contract holds on every new surface: /alignment, each of the four paired cards, both drill levels, /alignment/[metric], the home strip and each of the three home tiles"
  artifacts:
    - path: "types/db.ts"
      provides: "Final regenerated Supabase types covering every Phase 6 view, function and column"
      contains: "v_alignment_live_cards_daily"
    - path: ".planning/phases/06-dual-source-alignment-tsys-vs-bit-addict/06-06-SUMMARY.md"
      provides: "The recorded live evidence for all five ROADMAP success criteria and all seven ALIGN requirements"
      contains: "ALIGN-07"
  key_links:
    - from: "supabase/migrations/0027 through 0030"
      to: "the linked Supabase project"
      via: "Supabase MCP apply_migration, applied in ascending order, asserted present rather than assumed"
      pattern: "v_alignment_daily"
    - from: "types/db.ts"
      to: "every Phase 6 RPC call site"
      via: "final regeneration from the live schema, with no narrow cast left on any alignment path"
      pattern: "alignment_totals_for_period"
---

<objective>
Close the phase against live infrastructure. Assert — rather than assume — that all four migrations
are applied, regenerate the types one final time, prove every one of the five ROADMAP success criteria
and all seven `ALIGN-*` requirements with observed values, and confirm that Phase 6 was strictly
additive to Phase 4's reconciliation chain.

Purpose: this phase's whole architecture verifies green without ever touching the database. The type
check reads a generated file, the unit tests are pure, and the production build never opens a
connection. That is exactly the failure mode this plan exists to close: a confidently green phase in
which the alignment views do not exist and every page renders its error state for a real user.
Output: a final applied and typed schema, a recorded live demonstration of each success criterion, and
the hash evidence that the binding Phase-4 decision was honoured.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-dual-source-alignment-tsys-vs-bit-addict/06-CONTEXT.md
@.planning/phases/06-dual-source-alignment-tsys-vs-bit-addict/06-UI-SPEC.md
@.planning/phases/06-dual-source-alignment-tsys-vs-bit-addict/06-01-SUMMARY.md
@.planning/phases/06-dual-source-alignment-tsys-vs-bit-addict/06-02-SUMMARY.md
@.planning/phases/06-dual-source-alignment-tsys-vs-bit-addict/06-03-SUMMARY.md
@.planning/phases/06-dual-source-alignment-tsys-vs-bit-addict/06-04-SUMMARY.md
@.planning/phases/06-dual-source-alignment-tsys-vs-bit-addict/06-05-SUMMARY.md
</context>

<artifacts_this_phase_produces>
This plan produces no new application artifact. It verifies, live, every artifact listed in
`06-01-PLAN.md`'s Phase 6 artifact table, and leaves `types/db.ts` in its final regenerated state.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] Authoritative schema gate — assert all four migrations are live, regenerate types, run the full regression</name>
  <files>types/db.ts</files>
  <precondition>The Supabase MCP server is connected and its `apply_migration`, `execute_sql`, `generate_typescript_types` and `get_advisors` tools are callable against the linked project. If no authenticated route to the project is available, halt and report the block — every claim this plan makes depends on reading the live schema.</precondition>
  <read_first>
    - supabase/migrations/0027_alignment_coverage_and_business_days.sql, 0028_v_alignment_daily.sql, 0029_app_settings_alignment.sql, 0030_v_alignment_live_cards.sql — the four migrations whose live presence is being asserted
    - .planning/phases/06-dual-source-alignment-tsys-vs-bit-addict/06-01-SUMMARY.md, 06-02-SUMMARY.md, 06-03-SUMMARY.md — the push records from the earlier plans, including any object that failed to apply
    - .planning/phases/05-time-periods-financial-year-settings/05-05-PLAN.md Task 2 — the established push and post-push verification procedure for this project
    - supabase/tests/alignment_truth_table_test.sql and alignment_live_cards_test.sql — the two oracles re-run here as a regression
    - CLAUDE.md — the type-generation convention
  </read_first>
  <action>
This task is marked [BLOCKING] because `npx tsc --noEmit`, `npm test` and `npm run build` all pass
whether or not a single migration ever reached the database.

(a) Query the live schema with Supabase MCP `execute_sql` and assert presence rather than assume it.
`pg_proc` must list `add_business_days`, `alignment_status`, `alignment_daily_for_period`,
`alignment_totals_for_period` and `alignment_live_cards_for_period`. `pg_views` must list
`v_apigee_coverage_daily`, `v_alignment_daily` and `v_alignment_live_cards_daily`. `app_settings` must
carry `tsys_live_cards_baseline_offset`, `tsys_live_cards_baseline_as_of` and `alignment_tolerance`,
and `app_settings_audit` must carry its four new columns. If any object is missing, apply the owning
migration with the Supabase MCP `apply_migration` tool — ascending order, `0027` then `0029` then
`0030` then `0028` only if an earlier one was skipped, otherwise strictly ascending by number. Do NOT
use `supabase db push`; this environment has no Supabase CLI and no access token.

(b) Assert the security posture of everything this phase added: every one of the five functions
reports the public search-path setting in `proconfig`; `has_function_privilege('anon', ..., 'EXECUTE')`
is false for all five; `has_function_privilege('authenticated', ..., 'EXECUTE')` is true for the four
caller-facing ones and false for `fn_app_settings_audit`; all three new views report `security_invoker`
in their reloptions; `app_settings` still holds exactly one row and neither settings table has gained
an insert or delete policy. Then run `get_advisors` and confirm no new RLS-bypass or
mutable-search-path warning is attributable to 0027 through 0030.

(c) Regenerate `types/db.ts` from the live schema with the Supabase MCP `generate_typescript_types`
tool. Re-run the full unit suite, a clean type-check and a production build. Any type error is fixed
in the owning file by correcting the call, never by loosening a type and never by reintroducing a
narrow cast around an RPC.

(d) Re-run both SQL oracles against the live database and record their output. Also re-run the
Phase-4 oracle `supabase/tests/reconciliation_no_source_data_test.sql` and the Phase-5 oracle
`supabase/tests/tsys_msa_tier_test.sql` — Phase 6 added views over the same base tables, so the
existing reconciliation and revenue behaviour must be proven unchanged, not assumed.

If any assertion fails, the defect belongs to the plan that owns the file — fix it there, re-apply,
and re-verify. Do not adjust an expected value to match the output.
  </action>
  <reversibility rating="costly">Any migration applied here changes the shared hosted project and cannot be undone by reverting a commit. All four are additive — views, functions and defaulted columns — so a corrective drop is cheap, but a stored offset or tolerance value would be lost with its column.</reversibility>
  <verify>
    <automated>awk '/v_alignment_daily/{a=1} /v_alignment_live_cards_daily/{b=1} /v_apigee_coverage_daily/{c=1} /alignment_totals_for_period/{d=1} /alignment_live_cards_for_period/{e=1} /alignment_tolerance/{f=1} END{ if(a&&b&&c&&d&&e&&f) print "SCHEMA_TYPES_COMPLETE"; else print "SCHEMA_TYPES_INCOMPLETE" }' types/db.ts</automated>
    <fails_when>the string SCHEMA_TYPES_COMPLETE is absent from stdout, or SCHEMA_TYPES_INCOMPLETE appears — a missing name means at least one migration never reached the live project, so the phase is green against a schema that does not exist</fails_when>
    <automated>npm test && npx tsc --noEmit && npm run build</automated>
    <fails_when>non-zero exit from any of the three, any "error TS" line, any failed-test line in the vitest summary, or a "Failed to compile" line in the build output</fails_when>
    <automated>bash -c 'h() { git hash-object "$1"; }; [ "$(h supabase/migrations/0018_v_reconciliation_billing.sql)" = 97571afd8680f1f6902c58db17c8c728c750ed1d ] && [ "$(h supabase/migrations/0019_v_reconciliation_inventory.sql)" = 70ed5440d4d3ced3e2f43cd818baaacf977b29df ] && [ "$(h supabase/migrations/0020_v_apigee_cross_check.sql)" = 96844b24999470c374259a0bd7632fe6a2ad1d1b ] && [ "$(h supabase/migrations/0021_fix_reconciliation_settling.sql)" = 5ce4468bfd4d1d6f93dca0a9094928babd07dc75 ] && [ "$(h supabase/migrations/0022_reconciliation_no_source_data.sql)" = f90fb5ffad9821cfb2f14bca82b88963f53144ca ] && [ "$(h lib/dashboard/reconciliation-status.ts)" = 3675eba6f5855f7d1004743e12c0fd6678e4b65b ] && echo RECON_CHAIN_UNTOUCHED'</automated>
    <fails_when>the string RECON_CHAIN_UNTOUCHED is absent from stdout — one of the five shipped reconciliation migrations or the status module was edited during Phase 6. D-01 is binding: Phase 6 builds alongside that chain, never amends it, and the most likely edit is a helpfully-added mismatch branch on the cross-check view</fails_when>
    <human-check>Live schema confirmed: all five functions and all three views exist, every function carries the public search path, anon holds EXECUTE on none of them, authenticated holds EXECUTE on the four caller-facing ones and not on the audit trigger function, all three views report security_invoker, app_settings holds exactly one row, and neither settings table has an insert or delete policy. get_advisors reports no new warning attributable to 0027 through 0030. Both new SQL oracles and both prior-phase oracles ran clean against live data with their output recorded.</human-check>
  </verify>
  <acceptance_criteria>
    - `types/db.ts` contains all three new view names, both new period RPC names and the tolerance column name.
    - `npm test`, `npx tsc --noEmit` and `npm run build` all exit 0 after the final regeneration.
    - All five reconciliation-chain hash pins match their recorded values.
    - Live query output recorded in the SUMMARY listing every Phase 6 function and view with its search-path setting, its `anon` and `authenticated` EXECUTE privileges, and each view's `security_invoker` flag.
    - `get_advisors` output recorded with no new warning attributable to this phase.
    - Output of `alignment_truth_table_test.sql`, `alignment_live_cards_test.sql`, `reconciliation_no_source_data_test.sql` and `tsys_msa_tier_test.sql` recorded, all passing.
    - No narrow cast remains around any alignment RPC call site.
  </acceptance_criteria>
  <done>The schema this phase describes is provably the schema that exists, the prior phases' behaviour is provably unregressed, and the security posture is measured rather than claimed.</done>
</task>

<task type="auto">
  <name>Task 2: Live demonstration of the five ROADMAP success criteria and the seven ALIGN requirements</name>
  <files>—</files>
  <read_first>
    - .planning/ROADMAP.md §"Phase 6" — the five success criteria being demonstrated
    - .planning/REQUIREMENTS.md — ALIGN-01 through ALIGN-07 as registered during planning
    - .planning/phases/06-dual-source-alignment-tsys-vs-bit-addict/06-UI-SPEC.md — the Interaction and State Contract and the UI Considerations section, the source of the state checklist below
    - .planning/phases/06-dual-source-alignment-tsys-vs-bit-addict/06-CONTEXT.md — D-07, D-08, D-12 and D-14, whose user-visible consequences are what this checklist looks for
  </read_first>
  <action>
Walk the app against live data and record an observation for every line below. This is a
demonstration, not a code review: record the numbers and the copy actually on screen, and record any
divergence between TSYS and Bit Addict as a finding rather than treating it as a defect — a real
divergence is the product working.

SC1 and ALIGN-01: on `/alignment` for the current month, record all four cards' TSYS figure, Bit
Addict figure, absolute delta, percentage (or em dash), which-side-is-short phrase, coverage statement
and badge.

SC2 and ALIGN-02: record the live-cards derivation caption exactly as rendered, confirming it names
the formula, the stored baseline offset, its as-of date and the 13 Aug 2026 window start, and confirm
it is visible without hovering anything. Record the status-meaning caption too, and confirm the two
sit in different positions on the card rather than merging into one paragraph.

SC3 and ALIGN-03: find or select a period containing a day one source did not cover, and record that
the card reads needs review with the incomplete-coverage clause on its coverage line, not aligned and
not a zero figure for the missing side. Then confirm no card ever shows the aligned badge while its own
coverage statement admits a gap.

SC4 and ALIGN-04: drill from a card badge into level one, record the per-day columns, click into a
day, and record the two labelled sub-tables and their From-file captions. Confirm the Sheet did not
close between levels, the back affordance returns to level one, and closing with the escape key
returns to the plain `/alignment` URL.

SC5 and ALIGN-05: on `/` confirm the alignment strip shows a rollup badge, the naming sentence and
four mini-badges; select a period with no comparison data and confirm the neutral not-yet-available
state rather than a green badge.

ALIGN-06: change the tolerance on `/settings/general`, confirm the toast, confirm the audit row at the
top of the shared Change history log, and confirm a card's verdict on `/alignment` changes accordingly
without a redeploy. Restore the value the team wants to run with and record it.

ALIGN-07: from a year-scope drill Sheet, confirm the bounded-window notice states its cap and links to
`/alignment/[metric]`; follow it and confirm the full period is listed uncapped with the same period
applied. Confirm a one-month scope shows no such notice.

4-state contract: for `/alignment`, each of the four cards, both drill levels, `/alignment/[metric]`,
the home strip and each of the three home tiles, record the loading, empty, populated and error
treatments, plus the period-empty state on every period-scoped surface. Force one home tile's read to
fail and confirm the other two tiles and the strip still render.

Responsive: at 375px confirm every card's figure pair is still side by side with no truncation and no
wrapping, no surface scrolls horizontally, and both new sidebar entries are reachable and operable
through the mobile top-bar trigger.
  </action>
  <verify>
    <human-check>All four cards show both figures, variance, which side is short, coverage and a badge. The live-cards derivation and status-meaning captions are permanently visible and separately placed. A period with an uncovered day reads needs review, never aligned and never zero. The two-level drill opens, swaps in place, names the source file on both sides and closes back to the plain URL. The home strip shows badge, sentence and four mini-badges, and shows the neutral state rather than green when nothing is computable. A tolerance change re-verdicts the cards and is audited. The bounded-window link lands on the uncapped route with the period preserved. Every new surface shows all four states plus period-empty, one failing home region does not blank the others, and everything holds at 375px.</human-check>
  </verify>
  <acceptance_criteria>
    - An observation is recorded in the SUMMARY for every one of the five ROADMAP success criteria, with the observed figures or copy quoted.
    - An observation is recorded for each of ALIGN-01 through ALIGN-07.
    - The live-cards derivation caption is quoted verbatim as rendered.
    - A needs-review-due-to-coverage case is recorded with the period and day that produced it, or its absence in the current data is recorded explicitly.
    - The tolerance change, its audit row and its effect on a card verdict are recorded, along with the final value left in place.
    - The 4-state observations cover all eleven new surfaces named above.
    - The 375px observations are recorded for the cards, the drill and the sidebar.
  </acceptance_criteria>
  <done>Every ROADMAP success criterion and every minted ALIGN requirement has a recorded live observation, and the phase's user-facing contract is demonstrated rather than asserted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local migration files -> live Postgres | The authoritative gate that the described schema is the schema that exists |
| Live Postgres -> every Phase 6 surface | Views and functions whose privilege posture is measured here rather than assumed |
| Operator -> live settings | A tolerance change made during the demonstration alters real displayed verdicts |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-39 | Spoofing | A green phase over an absent schema | high | mitigate | The schema gate asserts the presence of every function, view and column by querying the live catalogue rather than trusting the earlier plans' push records, and applies any missing migration before the phase can close |
| T-06-40 | Elevation of Privilege | Every function added by this phase | high | mitigate | `has_function_privilege` is evaluated for `anon` and `authenticated` on all five functions and recorded; `anon` must hold EXECUTE on none, and the SECURITY DEFINER audit trigger function must be callable by neither client role |
| T-06-41 | Elevation of Privilege | Every view added by this phase | high | mitigate | Each of the three new views' reloptions are read and recorded to confirm `security_invoker`, so none silently executes with owner privileges over RLS-protected base tables |
| T-06-42 | Tampering | Mutable search path on any new function | high | mitigate | `proconfig` is read for all five functions and `get_advisors` is run, with the output recorded; a mutable search path would let a caller shadow a referenced object |
| T-06-43 | Tampering | Silent regression of the Phase-4 reconciliation chain | high | mitigate | Six git hash pins across the five shipped reconciliation migrations and the status module are asserted in one gate, and both prior-phase SQL oracles are re-run against live data |
| T-06-44 | Repudiation | A settings change made during the demonstration | medium | mitigate | The audit row for the demonstration tolerance change is read back and recorded, and the final value left in place is recorded in the SUMMARY |
| T-06-45 | Information Disclosure | Live figures recorded in a committed SUMMARY | low | accept | The figures are aggregate counts and a card total from an internal tool in a private repository; no card reference, no cardholder data and no credential is recorded |
| T-06-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no package — `06-RESEARCH.md` `## Package Legitimacy Audit` records that Phase 6 adds no external dependency, so the Package Legitimacy Gate is not applicable |
</threat_model>

<verification>
- All four migrations are confirmed present in the live catalogue, with any missing one applied before the gate passes.
- `types/db.ts` regenerated from the live schema names all three new views, both new period RPCs and the tolerance column.
- `npm test`, `npx tsc --noEmit` and `npm run build` all exit 0 after regeneration.
- Six hash pins across the reconciliation chain and the status module all match.
- All five Phase 6 functions carry the public search path and hold no `anon` EXECUTE; all three new views report `security_invoker`; `get_advisors` reports no new warning.
- All four SQL oracles — the two new ones and the two prior-phase ones — run clean against live data.
- A recorded live observation exists for each of the five ROADMAP success criteria and each of ALIGN-01 through ALIGN-07.
- The 4-state contract and the 375px behaviour are recorded across all eleven new surfaces.
</verification>

<success_criteria>
- Phase 6 is proven on live infrastructure rather than against a generated type file.
- Every success criterion and every minted requirement has a recorded observation with real values.
- Phase 4's reconciliation chain is proven byte-identical, so D-01 held in practice and not just in intent.
</success_criteria>

<output>
Create `.planning/phases/06-dual-source-alignment-tsys-vs-bit-addict/06-06-SUMMARY.md` when done
</output>
