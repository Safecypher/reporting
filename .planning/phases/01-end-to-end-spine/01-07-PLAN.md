---
phase: 01-end-to-end-spine
plan: 07
type: execute
wave: 5
depends_on: ["01-05"]
files_modified:
  - scripts/seed-historical.ts
  - seed-data/README.md
  - package.json
autonomous: false
requirements: [DASH-01, DATA-07]

must_haves:
  truths:
    - "Running the seed ingests the on-hand historical verification CSV(s) through the same ingest() path (not a separate code path)"
    - "Re-running the seed does not double-count (idempotent — same row_hash / sha256 guarantees)"
    - "After seeding, the verifications-over-time chart shows real data on first login"
  artifacts:
    - path: "scripts/seed-historical.ts"
      provides: "Repeatable seed that calls ingest() with the Supabase writer for every CSV in the seed directory (D-07)"
      contains: "ingest("
    - path: "seed-data/README.md"
      provides: "Where to drop the historical daily-ver CSVs (13 Aug onward) pending Richard's files"
  key_links:
    - from: "scripts/seed-historical.ts"
      to: "lib/ingestion/index.ts"
      via: "ingest(input, createSupabaseWriter()) per file"
      pattern: "ingest\\("
---

<objective>
Provide a repeatable historical-data seed (D-07) so the verifications-over-time chart shows real multi-day data on first login — a confident demo for Mark — while the live upload path stays available to demonstrate ingestion. Critically, the seed reuses the exact `ingest()` pipeline (idempotent, same de-dup and cutoff), never a parallel code path.

Purpose: Demo readiness within the week, without forking ingestion logic.
Output: A `seed` npm script and a documented drop location for the historical CSVs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-end-to-end-spine/01-CONTEXT.md
@.planning/phases/01-end-to-end-spine/01-PATTERNS.md
@.planning/phases/01-05-SUMMARY.md
@.planning/phases/01-04-SUMMARY.md

<interfaces>
From lib/ingestion/index.ts:  export async function ingest(input, deps): Promise<IngestionResult>
From lib/ingestion/supabase-writer.ts:  export function createSupabaseWriter(): IngestDeps
IngestionInput requires uploadedBy (auth user id) — use a seed/service account id or a documented fixed id for seeded rows.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repeatable seed script over the historical CSV directory</name>
  <files>scripts/seed-historical.ts, seed-data/README.md, package.json</files>
  <read_first>
    - .planning/phases/01-end-to-end-spine/01-CONTEXT.md (D-07 — must call the same ingest() function; dependency: full historical set pending from Richard; currently only daily-ver-report_2026-08-13.csv is on hand)
    - .planning/phases/01-end-to-end-spine/01-PATTERNS.md (Non-UI Layer table row for scripts/seed-historical.ts — reuse ingest(), not a separate path)
    - .planning/phases/01-05-SUMMARY.md (createSupabaseWriter + IngestionInput/uploadedBy expectations)
  </read_first>
  <action>
    Create `scripts/seed-historical.ts` that reads every `*.csv` in a `seed-data/` directory, and for each file builds an `IngestionInput { fileName, bytes, contentType: 'text/csv', uploadedBy: <seed account id> }` and calls `ingest(input, createSupabaseWriter())`, logging a per-file summary line (accepted / duplicates / rejected / alreadyUploaded). Because it uses the same pipeline, re-running is idempotent via sha256 dup-file check + row_hash UNIQUE. Add an npm script `seed` that runs it with the env loaded (e.g. via `tsx`/`node --env-file=.env.local`). Create `seed-data/README.md` documenting: drop the daily verification CSVs (13 Aug 2026 onward) here; only `daily-ver-report_2026-08-13.csv` is currently available — a single day will look sparse, so chase the historical files from Richard before the demo. Do not commit real report data if it is sensitive — note that in the README.
  </action>
  <verify>
    <automated>grep -c 'ingest(' scripts/seed-historical.ts && grep -c 'createSupabaseWriter' scripts/seed-historical.ts && grep -c '"seed"' package.json</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/seed-historical.ts` iterates the `seed-data/` directory and calls `ingest(input, createSupabaseWriter())` per file (no bespoke insert/dedup logic)
    - An npm `seed` script exists and loads `.env.local`
    - `seed-data/README.md` documents the drop location and the pending-historical-files dependency
    - Re-running the seed does not change verification row counts (idempotent)
  </acceptance_criteria>
  <done>The seed script exists, reuses ingest(), and is idempotent.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify historical seed populates the dashboard idempotently</name>
  <action>Human runs the seed and verifies idempotency and a populated chart per the steps below. No code changes; approve or report failures.</action>
  <what-built>The historical-data seed that populates the dashboard via the real ingestion path.</what-built>
  <how-to-verify>
    1. Copy the available `daily-ver-report_2026-08-13.csv` into `seed-data/`.
    2. Run `npm run seed` → confirm the log shows the file ingested with accepted/duplicates/rejected counts.
    3. Run `npm run seed` again → confirm counts show duplicates/alreadyUploaded and the verification row count in the DB is unchanged.
    4. Sign in and open `/verifications` → the chart should show the seeded day's data on first login.
    5. Confirm with the team whether the full historical set (13 Aug onward) has arrived from Richard; if not, note the demo will look sparse until it does.
  </how-to-verify>
  <resume-signal>Type "approved" or describe the issue (e.g. seed double-counted, chart still empty).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local seed files → ingestion pipeline | Seed inputs are operator-provided report files, processed by the same validated pipeline |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | Seed double-counting | mitigate | Seed reuses ingest() → sha256 dup-file + row_hash UNIQUE make re-seeding idempotent (D-07) |
| T-07-02 | Information Disclosure | Committing real report data | mitigate | `seed-data/` documented as a local drop; README warns against committing sensitive report data (add to .gitignore) |
| T-07-03 | Information Disclosure | Secret key in a script | mitigate | The seed uses `createSupabaseWriter()` (server-only secret key) run locally from `.env.local`; never bundled into client code |
</threat_model>

<verification>
- `npm run seed` ingests the on-hand CSV via ingest(); re-running is idempotent.
- The dashboard shows seeded data.
- `npm run build` and `npm run lint` pass.
</verification>

<success_criteria>
- D-07: repeatable seed through the shared ingestion path; demo shows populated data on first login.
- DASH-01/DATA-07: seeded rows are real, de-duplicated, lineage-tracked verification records.
</success_criteria>

<output>
Create `.planning/phases/01-end-to-end-spine/01-07-SUMMARY.md` when done.
</output>
