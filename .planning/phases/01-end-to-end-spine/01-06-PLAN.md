---
phase: 01-end-to-end-spine
plan: 06
type: execute
wave: 3
depends_on: ["01-03", "01-02"]
files_modified:
  - app/(dashboard)/verifications/page.tsx
  - components/dashboard/verifications-chart.tsx
  - components/dashboard/kpi-cards.tsx
  - components/dashboard/view-controls.tsx
  - lib/dashboard/bucketing.ts
  - lib/dashboard/__tests__/bucketing.test.ts
autonomous: false
requirements: [DASH-01, DASH-04, DATA-06]

must_haves:
  truths:
    - "The verifications-over-time chart shows authenticated vs failed counts over time"
    - "A Daily/Weekly/Monthly toggle re-buckets the series, defaulting to UTC buckets (D-02: bucket Daily/Weekly/Monthly in UTC by default)"
    - "A session-only timezone toggle (UTC / Europe/London / America/Chicago) re-buckets and re-labels the view, defaulting to UTC each visit (D-03: session-only, resets to UTC on each visit, nothing persisted; D-04: exactly these three options for v1, no full IANA picker)"
    - "An 'as of last import' timestamp and the pre-13-Aug data-window caption are visible"
    - "The view renders all four states (loading / empty / populated / error)"
  artifacts:
    - path: "components/dashboard/verifications-chart.tsx"
      provides: "Recharts authenticated-vs-failed time series (blue/amber, never red/green)"
      contains: "recharts"
    - path: "lib/dashboard/bucketing.ts"
      provides: "Pure daily→weekly/monthly re-bucketing + timezone re-bucketing (date-fns / date-fns-tz)"
      exports: ["rebucket"]
    - path: "app/(dashboard)/verifications/page.tsx"
      provides: "Server Component reading v_verifications_daily + freshness timestamp"
      contains: "v_verifications_daily"
  key_links:
    - from: "app/(dashboard)/verifications/page.tsx"
      to: "v_verifications_daily"
      via: "server client select"
      pattern: "v_verifications_daily"
    - from: "components/dashboard/view-controls.tsx"
      to: "lib/dashboard/bucketing.ts"
      via: "granularity + timezone selection re-buckets the daily series client-side"
      pattern: "rebucket"
---

<objective>
Build the verifications-over-time dashboard view — the reason this screen exists (DASH-01): a Recharts time series of authenticated vs failed counts read from `v_verifications_daily`, a Daily/Weekly/Monthly granularity toggle, a session-only 3-way timezone toggle (UTC/London/Chicago), KPI metric cards in the brand `.metric` treatment, the "as of last import" freshness badge (DASH-04), the data-window caption, and the mandatory 4-state contract. Re-bucketing is pure and unit-tested.

Purpose: Give leadership a confident, correctly-bucketed view of verification volume that is honest about currency (freshness badge) and scope (13-Aug caption).
Output: The populated verifications dashboard with granularity + timezone controls.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-end-to-end-spine/01-UI-SPEC.md
@.planning/phases/01-end-to-end-spine/01-RESEARCH.md
@.planning/phases/01-end-to-end-spine/01-PATTERNS.md
@.planning/phases/01-03-SUMMARY.md
@.planning/phases/01-02-SUMMARY.md

<interfaces>
From types/db.ts:  Database — v_verifications_daily row = { day_utc: string; authenticated_count: number; failed_count: number }
From lib/supabase/server.ts:  export async function createClient(): Promise<SupabaseClient>
Freshness (DASH-04): select max(uploaded_at) from ingested_files where status = 'done'
Data-viz palette (UI-SPEC): Authenticated = #382aff (Cypher Blue), Failed = #d97706 (amber-600). Never red/green.
Timezone toggle options (D-04): 'UTC' (default), 'Europe/London', 'America/Chicago'. Session-only (D-03) — resets to UTC each visit; nothing persisted.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure re-bucketing (daily→weekly/monthly + timezone) with unit tests</name>
  <files>lib/dashboard/bucketing.ts, lib/dashboard/__tests__/bucketing.test.ts</files>
  <read_first>
    - .planning/phases/01-end-to-end-spine/01-RESEARCH.md (Pitfall 5 — date-fns v4 needs date-fns-tz for IANA zones, use toZonedTime/formatInTimeZone; Claude's Discretion — client-side re-bucketing is fine at these volumes; Assumption A2)
    - .planning/phases/01-end-to-end-spine/01-CONTEXT.md (D-02/D-03/D-04 — UTC default, session-only toggle, exactly three zones)
    - ./CLAUDE.md (date-fns 4.4.0; prefer over hand-rolled offset math)
  </read_first>
  <behavior>
    - rebucket(dailyRows, 'daily', 'UTC') returns one point per day_utc unchanged
    - rebucket(dailyRows, 'weekly', tz) groups days into ISO weeks and sums authenticated/failed
    - rebucket(dailyRows, 'monthly', tz) groups by calendar month and sums
    - Changing tz from 'UTC' to 'America/Chicago' can move a boundary day's counts into an adjacent bucket (DST-safe via date-fns-tz, not manual offsets)
    - No row before 2026-08-13 ever appears (defensive filter mirroring the view)
  </behavior>
  <action>
    Create `lib/dashboard/bucketing.ts` exporting `rebucket(rows, granularity, timeZone)` where granularity is `'daily'|'weekly'|'monthly'` and timeZone is one of the three D-04 IANA strings. Use `date-fns` (`startOfWeek`, `startOfMonth`) and `date-fns-tz` (`toZonedTime`/`formatInTimeZone`) to interpret each `day_utc` in the selected zone before grouping — do NOT hand-roll offset arithmetic (Pitfall 5). Sum `authenticated_count` and `failed_count` per bucket; keep the pre-13-Aug defensive exclusion. Write `lib/dashboard/__tests__/bucketing.test.ts` covering daily passthrough, weekly/monthly grouping sums, and a timezone-boundary case.
  </action>
  <verify>
    <automated>npm run test -- lib/dashboard/__tests__/bucketing.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rebucket` groups correctly for daily/weekly/monthly and sums both series
    - Timezone re-bucketing uses date-fns-tz (no literal offset numbers in the code)
    - `npm run test` passes for the bucketing test
  </acceptance_criteria>
  <done>Re-bucketing is pure, timezone-correct, and tested.</done>
</task>

<task type="auto">
  <name>Task 2: Server Component page + chart + KPI cards + granularity/timezone controls + 4 states</name>
  <files>app/(dashboard)/verifications/page.tsx, components/dashboard/verifications-chart.tsx, components/dashboard/kpi-cards.tsx, components/dashboard/view-controls.tsx</files>
  <read_first>
    - .planning/phases/01-end-to-end-spine/01-UI-SPEC.md (Visual Hierarchy → chart is the focal point, KPI `.metric` band secondary; Color → Data-Visualisation Palette (blue/amber); Copywriting → `Daily`·`Weekly`·`Monthly`, `Authenticated`·`Failed`, `Data as of last import: {timestamp}`, `Excludes data before 13 Aug 2026.`, empty-state `No verifications yet` + body + inline `Upload report` link; Interaction & State Contract → loading skeleton / empty / populated / error; Typography → `.metric` 48–64px Cypher Blue with 2px underline)
    - .planning/phases/01-end-to-end-spine/01-PATTERNS.md (`app/(dashboard)/verifications/page.tsx` KPI `.metric` analog; chart has no analog — follow UI-SPEC palette + shadcn chart wrapper)
    - lib/dashboard/bucketing.ts (rebucket from Task 1)
  </read_first>
  <action>
    Create `app/(dashboard)/verifications/page.tsx` as a Server Component: read `v_verifications_daily` via the server client and the freshness timestamp (`max(uploaded_at) where status='done'`); pass the daily rows + freshness to client children. Handle the four states: loading (shadcn `skeleton` for chart + cards), empty (`No verifications yet` + body + inline `Upload report` link to `/uploads`), populated, and error (the upload-failed-style error copy). Create `components/dashboard/verifications-chart.tsx` (client) using Recharts via the shadcn `chart` wrapper — a bar/line time series with two series: Authenticated (`#382aff`) and Failed (`#d97706`), axis labels, a legend, and tooltips showing exact integer counts with tabular numerals; never rely on colour alone (distinct labels). Create `components/dashboard/kpi-cards.tsx` rendering total verifications / authenticated / failed as brand `.metric` cards (Poppins 700, Cypher Blue, 2px underline; one accent-teal underline max). Create `components/dashboard/view-controls.tsx` (client) with a shadcn `toggle-group` for Daily/Weekly/Monthly and a second control for the timezone (UTC/Europe/London/America/Chicago, default UTC, session-only per D-03 — held in component state, not persisted). Controls call `rebucket` and update the chart. Always show the `Data as of last import: {timestamp}` badge and `Excludes data before 13 Aug 2026.` caption.
  </action>
  <verify>
    <automated>grep -c 'v_verifications_daily' app/(dashboard)/verifications/page.tsx && grep -c '#382aff' components/dashboard/verifications-chart.tsx && grep -c '#d97706' components/dashboard/verifications-chart.tsx && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - The page reads `v_verifications_daily` and the `max(uploaded_at)` freshness in a Server Component
    - The chart uses `#382aff` for Authenticated and `#d97706` for Failed (no red/green), with axis labels and a legend
    - Daily/Weekly/Monthly and the three timezone options are present; timezone defaults to UTC and is not persisted across a reload
    - All four states render; empty state links to `/uploads`; the freshness badge and data-window caption are always visible
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>The verifications dashboard renders the focal chart, KPI band, controls, freshness, and caption across all four states.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify verifications-over-time dashboard + toggles + freshness</name>
  <action>Human verifies the chart, granularity/timezone toggles, freshness badge, and data-window caption per the steps below. No code changes; approve or report failures.</action>
  <what-built>The verifications-over-time dashboard: authenticated-vs-failed chart, Daily/Weekly/Monthly toggle, session-only timezone toggle, KPI metric cards, freshness badge, and data-window caption.</what-built>
  <how-to-verify>
    1. Run `npm run dev`, sign in, go to `/verifications`.
    2. With no data yet: confirm the empty state `No verifications yet` with a working `Upload report` link.
    3. Upload the sample verification CSV (via `/uploads`, from Plan 05) or run the seed (Plan 07), then return: the chart should show authenticated (blue) vs failed (amber) counts; KPI cards show totals in the brand metric style.
    4. Toggle Daily → Weekly → Monthly: the buckets should change (a single-day sample will look sparse — expected until historical files arrive).
    5. Toggle timezone UTC → Europe/London → America/Chicago: labels/buckets should re-compute; reload the page → it should reset to UTC (session-only, D-03).
    6. Confirm `Data as of last import: {timestamp}` and `Excludes data before 13 Aug 2026.` are visible.
  </how-to-verify>
  <resume-signal>Type "approved" or describe the issue (e.g. red/green used, timezone persisted across reload, freshness missing).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated user → verification data | Reads must go through the session-scoped server client so RLS applies |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Information Disclosure | Dashboard data reads | mitigate | Page reads via the session-scoped server client (RLS from Plan 03 applies); route gated by proxy.ts + layout guard |
| T-06-02 | Tampering | Misleading bucketing/timezone | mitigate | Re-bucketing unit-tested; UTC is the canonical default (D-02); freshness badge + 13-Aug caption prevent a "confidently wrong" read; raw_created_at (Plan 04) allows correcting the A1 zone assumption without re-upload |
| T-06-03 | Information Disclosure | Data-viz colour semantics | accept | Blue/amber CVD-safe palette with distinct labels; never colour-alone — accessibility, not a security threat, but tracked for the trust contract |
</threat_model>

<verification>
- Chart renders authenticated vs failed; toggles re-bucket; timezone resets to UTC on reload.
- Freshness badge + data-window caption always visible; empty/loading/error states render.
- `npm run build`, `npm run lint`, and `npm run test` pass.
</verification>

<success_criteria>
- DASH-01: verifications over time with daily/weekly/monthly, authenticated vs failed.
- DASH-04: "as of last import" timestamp shown.
- DATA-06: pre-13-Aug data excluded (view + defensive client filter) and captioned.
</success_criteria>

<output>
Create `.planning/phases/01-end-to-end-spine/01-06-SUMMARY.md` when done.
</output>
