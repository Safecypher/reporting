# Continue Here — Phase 1 (End-to-End Spine)

**Paused:** 2026-08-19
**Status:** Phase 1 fully PLANNED (7 plans, 5 waves) and verified. Execution NOT started — 0 plans complete. STATE.md = "Ready to execute".

## Why paused
Execution is blocked on **user provisioning of Supabase** (external, user-owned). User chose "I'll provision first", then left to sort out the Supabase MCP separately.

## Resume action
Once preconditions below are green, just run: `/gsd:execute-phase 1`
It resumes cleanly from Wave 1 (nothing to undo).

## Preconditions to satisfy before executing (all were MISSING at pause)
- [ ] Supabase project created (cloud) — Project URL + publishable key + secret key
- [ ] `supabase` CLI installed — `brew install supabase/tap/supabase`
- [ ] Docker running (needed for local Supabase stack)
- [ ] `.env.local` created in repo root with EXACT names the plans reference:
  - `NEXT_PUBLIC_SUPABASE_URL`
  - `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`  (publishable, not legacy anon)
  - `SUPABASE_SECRET_KEY`  (server-only, never in browser)
  - `SUPABASE_ACCESS_TOKEN`  (personal access token, for `supabase db push`)
- Node v20.20.1 already present ✓

## Execution shape (from phase-plan-index)
| Wave | Plans | Mode |
|------|-------|------|
| 1 | 01-01 | autonomous — scaffold Next 16 + 3 supabase clients + proxy.ts + brand tokens |
| 2 | 01-02 (checkpoint), 01-03 (checkpoint) | manual — login/session; migrations + [BLOCKING] `supabase db push` + gen types |
| 3 | 01-04 (auto), 01-06 (checkpoint) | ingestion core (offline-OK); dashboard view |
| 4 | 01-05 (checkpoint) | upload wired to live DB |
| 5 | 01-07 (checkpoint) | historical seed |

Runtime: claude, use_worktrees: true (parallel worktree execution available). executor/verifier model: sonnet.

## Manual actions that will surface at checkpoints
- **Seed ~4 internal accounts** (Mark W, Richard, Andy, …) directly in Supabase — Phase 1 has NO signup UI by design (decision D-01). Happens at plan 01-02 checkpoint.
- Drive `supabase db push` at plan 01-03 (BLOCKING) once CLI + project linked; regenerate `types/db.ts` after.

## ⚠ Open assumption to confirm before the demo (research A1)
Verification report `CreatedAt` timestamps are naive (no offset). Plans store them as **UTC** and keep `raw_created_at` for re-derivation. CONFIRM true source timezone with Joachim/Chris/Richard — a chart "confidently 5 hours wrong" is worse than none.

## Supabase MCP note (separate from execution)
User hit `{"message":"Unrecognized client_id"}` on OAuth — known DCR bug (2026), server-side. Workaround = PAT header config (bypasses OAuth). MCP is OPTIONAL — execution uses the supabase CLI + env vars, NOT the MCP. See memory: supabase-mcp-oauth-workaround.

## No blocking anti-patterns
(Intentionally none — this file is a resume breadcrumb, not an anti-pattern record. execute-phase's blocking-antipattern gate will find no `blocking` rows and proceed.)
